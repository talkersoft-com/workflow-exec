# Task 0007 — Parity pass + release smoke test + ship

## Goal
Prove observable 1:1 with the reference and that the release artifact works, then ship.

## Steps
1. **Per-surface parity checklist** vs `agent-arcade-studio`: every menu item, every shortcut
   (`⌘⌥A ⌘D ⌘F ⌘W ⌘←/→ ^C Esc`), dictation flow, sync mode, workspace shell, macro picker
   (`select`/`text`/`flag`), agent switching, and **draft preservation**. Record results in
   `Results/PARITY.md`.
2. **Release-artifact smoke test:** `npm pack` the package, install the tarball into a temp
   prefix, launch `agent-arcade`, and confirm: menu-bar icon appears, a **dictation round-trip**
   works, and a **`⌘W` workspace shell** spawns (the `spawn-helper` path).
3. Write `Results/RESULT.md` and `Retro/LESSONS.md` **before** integrating.
4. Ship: `hv_integrate` (final phase PR), then — after merge — `hv_release` to promote hive→main.
   Bump `package.json` version and cut a `v<version>` GitHub Release to trigger the publish
   Action (tag must equal package.json version).

## Definition of done
Parity checklist passes for every surface; the smoke test passes (icon + dictation + `⌘W`);
`Results/` + `Retro/LESSONS.md` written; phase shipped via `hv_integrate`; release promoted via
`hv_release`.
