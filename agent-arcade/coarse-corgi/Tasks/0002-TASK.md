# Task 0002 — Freeze the preload / IPC contract

## Goal
Lock the preload surface both renderers depend on **before** building either, including the Go
NDJSON→renderer event forwarding and the `wezterm-bridge` exec-result shape.

## Steps
1. Document every IPC channel in a written contract (`docs/IPC.md` or similar): config read/write
   (same YAML schema), agent/command data, dictation job dispatch, the `dictation:event` stream
   (Go `result`/`error`/`status`/`ready`/`health_result`, keyed by `job_id`), `wezterm-bridge`
   command invocations and their **result shape with exit code surfaced**, and the workspace-shell
   (node-pty) channels.
2. Ensure `main` forwards Go NDJSON events to the renderer unchanged, and surfaces
   `wezterm-bridge` exit codes (so a non-zero exit is visible to the renderer).
3. Provide a single typed `preload` per surface exposing only the contracted channels.

## Definition of done
A written IPC contract exists; both preloads compile and expose exactly the contracted surface;
`main` forwards `dictation:event` and command results (with exit codes) unchanged. No renderer
state logic yet.
