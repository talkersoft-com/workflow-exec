# Test 0002 — Preload / IPC contract frozen

## TC-001 — Contract documented
Pass: a written IPC contract lists every channel: config R/W (same YAML), agent/command data,
dictation dispatch, the `dictation:event` stream (result/error/status/ready/health_result by
`job_id`), `wezterm-bridge` invocations + result shape **with exit code**, and node-pty shell
channels.

## TC-002 — Go events forwarded unchanged
Trigger a dictation/health event from `dictation-go`. Pass: `main` forwards `dictation:event` to
the renderer with the same fields/`job_id`.

## TC-003 — wezterm-bridge exit code surfaced
Invoke a `wezterm-bridge` command that exits non-zero. Pass: the renderer-facing result carries a
non-zero exit indicator (not a silent success).

## TC-004 — Preloads expose only the contract
Pass: each surface's preload exposes exactly the contracted channels; both compile.
