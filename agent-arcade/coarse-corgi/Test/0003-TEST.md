# Test 0003 — Studio (React + Zustand) 1:1

## TC-001 — Screen parity
Against an identical config, compare each Studio screen to the reference: agents, systems,
groups, servers, app settings/shortcuts, `@`-macro authoring (`select`/`text`/`flag`). Pass: same
fields, controls, and flows.

## TC-002 — YAML round-trip unchanged
Create/edit agents + a macro, save. Pass: the written `~/.hv/agent-arcade.yaml` matches the
reference's schema (agent/command normalization, arg types) — no schema drift.

## TC-003 — Paradigm boundary
Pass: React/Zustand/JSX appear only under `studio/`; `grep -r` finds none in `arcade/`.

## TC-004 — Build green
`npm run build`. Pass: exits 0; `studio/dist/**` produced.
