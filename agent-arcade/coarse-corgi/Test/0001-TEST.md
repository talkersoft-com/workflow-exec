# Test 0001 — Scaffold + pipeline + Go + Electron shell

## TC-001 — Build succeeds
`cd agent-arcade && npm run build`. Pass: exits 0; runs build:js, vendor:wezterm, build:go,
build:wezterm. Fail: any stage errors.

## TC-002 — App launches with stubs
Launch `agent-arcade`. Pass: menu-bar launcher appears; stub Studio + Arcade windows open.

## TC-003 — Package contents
`npm pack --dry-run`. Pass: includes `go/bin/**`, `wezterm-bridge/bin/**`, `vendor/wezterm/**`,
`studio/dist/**`, `arcade/dist/**`.

## TC-004 — No DMG / correct identity
Pass: no electron-builder `build` block, no `dist`/`release`/`pack` scripts; package name is
`@talkersoft-com/agent-arcade`, bin `agent-arcade`; `scripts/fix-node-pty.js` postinstall present.
