# Test 0007 — Parity pass + release smoke test

## TC-001 — Parity checklist complete
`Results/PARITY.md` exists and every row passes: each menu item, each shortcut
(`⌘⌥A ⌘D ⌘F ⌘W ⌘←/→ ^C Esc`), dictation, sync, shell, macros (`select`/`text`/`flag`), agent
switching, draft preservation — all matching the reference.

## TC-002 — Release artifact installs + launches
`npm pack` → install the tarball into a temp prefix → run `agent-arcade`. Pass: menu-bar icon
appears.

## TC-003 — Dictation round-trip on the installed artifact
Pass: a dictation completes end-to-end (Go round-trip → text into a pane).

## TC-004 — ⌘W shell on the installed artifact
Pass: `⌘W` spawns a workspace shell (spawn-helper path intact in the packed/installed package).

## TC-005 — Outputs + ship
Pass: `Results/RESULT.md` + `Retro/LESSONS.md` written before integrate; phase shipped via
`hv_integrate`; release promoted via `hv_release`.
