# Test 0004 — Arcade foundation

## TC-001 — Durable draft across navigation (the headline fix)
Type a message in agent A's prompt, `⌘→` to agent B, `⌘←` back to A. Pass: A's draft is intact,
unchanged. Repeat across 3+ agents.

## TC-002 — No DOM desync
Switch agents rapidly and toggle views. Pass: rendered DOM always matches machine state (agent
name, view, draft) — no stale/duplicated UI.

## TC-003 — UI verbatim
Pass: `arcade/renderer/index.html` + CSS are byte-identical to the reference (diff is empty).

## TC-004 — Paradigm + build
Pass: no React in `arcade/`; `node --check` clean on edited JS; `npm run build` green;
`arcade/dist/**` produced.
