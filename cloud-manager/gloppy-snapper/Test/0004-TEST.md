# Test 0004 — Shipped

## TC-001 — Outputs written before ship
Pass: `Results/RESULT.md` and `Retro/LESSONS.md` exist and are included in the shipped workflow-exec commit (not left uncommitted or landed on a later branch).
Fail: either file missing from the PR.

## TC-002 — PRs match the deck manifest
`hv_list_pulls deck: "cloud-manager"`
Pass: open/merged PRs from `gloppy-snapper` exist for exactly the repos listed in `../deck.md`'s table; no PR from any unlisted repo.
Fail: missing PR for a listed repo, or a surprise PR from an unlisted one.

## TC-003 — Result records the evidence
Read `Results/RESULT.md`.
Pass: it contains the branch name, per-phase summary, PR URLs, and the operator reminder that the database-toolkit MCP needs an `/mcp` reload.
Fail: placeholder TBDs remain after PRs were opened.

## TC-004 — Deck transitioned after merge
After all PRs merge: `hv_status deck: "cloud-manager"`.
Pass: all repos clean on a fresh auto-generated branch (post `hv_next`).
Fail: deck still sitting on `gloppy-snapper` with merged PRs.
