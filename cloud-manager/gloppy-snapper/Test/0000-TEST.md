# Test 0000 — Workspace Initialized

## TC-001 — All repos on feature branch
Run `hv_status deck: "cloud-manager"`.
Pass: every repo shows branch `gloppy-snapper` (or the replacement branch recorded in `../deck.md`).
Fail: any repo is on `main` or a different feature branch.

## TC-002 — All repos clean
Pass: hv_status shows no dirty repos, excluding `workflow-exec` dirt that consists solely of this workflow's own `cloud-manager/gloppy-snapper/` scaffolding (committed later by hv_ship in Task 0004).
Fail: any other repo shows uncommitted changes, unpushed commits, or stash entries.
