# Test 0003 — SSH secret shape findings

## TC-001 — Findings doc exists in the plan folder
`ls /Users/talker/workspace/cloud-manager/planning/workflow-plans/cloud-manager/beaming-messenger/FINDINGS-0003-ssh-secret-shape.md`
Pass: file exists.
Fail: missing.

## TC-002 — Writer identified with evidence
Pass: the doc names the writer repo and file:line, and shows (described, not pasted secrets) the marshalling step that produces the double-encoded `ssh_key` field. If the writer is vorch-lib, the vorch no-change convention is explicitly flagged.
Fail: writer unidentified or asserted without a file reference.

## TC-003 — Reader map complete
Pass: the doc lists every reader found — at minimum covering `cloud-manager-cli` `internal/vaultclient`, the porch/vorch-service search result, and `cloud-manager-api/scripts/` — each with file:line and whether it single- or double-decodes. Searches that came up empty are stated as searched-and-empty, not omitted.
Fail: a named suspect from PLAN.md is neither mapped nor explicitly cleared.

## TC-004 — Options, blast radius, recommendation
Pass: both option (a) normalize-with-migration and option (b) document-as-contract appear with per-option blast radius, and the doc commits to one recommendation; if (a), it states the work belongs in a separate plan.
Fail: no explicit recommendation.

## TC-005 — No code changes
`hv_status deck: "cloud-manager"` after this task.
Pass: no repo became dirty due to this task; vorch-lib, vorch-service untouched. No secret material (keys, tokens, passwords) appears in the findings doc.
Fail: any code modification attributable to this task, or secret material in the doc.
