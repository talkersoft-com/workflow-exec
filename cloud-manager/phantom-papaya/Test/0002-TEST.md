# Test 0002 — Diagnosis written

## TC-001 — DIAGNOSIS.md exists
Run:
```
test -f Retro/DIAGNOSIS.md
```
Pass: file exists.
Fail: file missing.

## TC-002 — Diagnosis names a file:line
Run:
```
grep -E "\.(cs|ts|tsx|go):[0-9]+|\.(cs|ts|tsx|go) line [0-9]+" Retro/DIAGNOSIS.md
```
Pass: at least one match.
Fail: no file:line reference anywhere in the diagnosis.

## TC-003 — Diagnosis names the failure layer
Run:
```
grep -iE "layer:|failure layer|repo:" Retro/DIAGNOSIS.md
```
Pass: at least one match.
Fail: no layer / repo identified.

## TC-004 — No source code edits during diagnosis
Run:
```
git -C vm-infra/cloud-manager/cloud-manager-web status --porcelain
git -C vm-infra/cloud-manager/cloud-manager-api status --porcelain
git -C vm-infra/cloud-manager/vorch-service status --porcelain
git -C vm-infra/cloud-manager/vorch-lib status --porcelain
```
Pass: all four empty.
Fail: any unintended edit.
