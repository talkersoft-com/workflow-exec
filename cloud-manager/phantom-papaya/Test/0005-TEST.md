# Test 0005 — Shipped

## TC-001 — Results/RESULT.md and Retro/LESSONS.md present
Run:
```
test -s Results/RESULT.md && test -s Retro/LESSONS.md
```
Pass: both files exist and are non-empty.
Fail: either is missing or empty.

## TC-002 — hv_ship succeeded
Inspect the output of the `hv_ship` call in Task 0005.
Pass: ship output names at least one PR URL and reports merged/open status. No "DIRTY" warnings for repos not intended to ship.
Fail: ship failed, or shipped a repo not named in DIAGNOSIS.md.

## TC-003 — Deck advanced to next branch
Run `hv_status` on the deck.
Pass: branch is no longer `phantom-papaya` (deck has rolled forward).
Fail: still on `phantom-papaya`.
