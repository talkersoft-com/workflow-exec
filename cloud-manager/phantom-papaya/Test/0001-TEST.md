# Test 0001 — Reproduction captured

## TC-001 — REPRODUCTION.md exists with required content
Run:
```
test -f Retro/REPRODUCTION.md && grep -E "HTTP|status" Retro/REPRODUCTION.md
```
Pass: file exists and contains at least one HTTP-status reference.
Fail: file missing, or contains no HTTP status / error string.

## TC-002 — All required artifacts present
Run:
```
ls Retro/
```
Pass: at least one of `network.json` / `network.md` / `curl.txt` is present (request/response captured), and one of `api.log`, `vorch.log` is present (journal captured).
Fail: neither request capture nor journal capture exists.

## TC-003 — No source code edits
Run:
```
git -C vm-infra/cloud-manager/cloud-manager-web status --porcelain
git -C vm-infra/cloud-manager/cloud-manager-api status --porcelain
git -C vm-infra/cloud-manager/vorch-service status --porcelain
git -C vm-infra/cloud-manager/vorch-lib status --porcelain
```
Pass: all four output empty.
Fail: any output indicates an unintended source-code edit during reproduction.

## TC-004 — Services are still active
Run:
```
sudo systemctl is-active cloud-manager-api vorch_service cloud-manager-web rabbitmq-server
```
Pass: all four print `active`.
Fail: any service is not active. (If a service was inactive AS the bug, that's a Phase-1 finding — note it in REPRODUCTION.md and re-mark this TC pass.)
