# Test 0003 — Fix applied + builds clean

## TC-001 — CHANGE.md exists
Run:
```
test -f Retro/CHANGE.md
```
Pass: file exists.
Fail: file missing.

## TC-002 — Only DIAGNOSIS-named repos are dirty
Run `git status --porcelain` against each of the four code repos. Then read `Retro/DIAGNOSIS.md`'s "repos that need PRs" list.
Pass: every dirty repo is named in DIAGNOSIS.md.
Fail: a repo is dirty that DIAGNOSIS.md did NOT name (= scope creep).

## TC-003 — Per-repo verifier passes
For each repo named in DIAGNOSIS.md:
- `cloud-manager-web` → `cd vm-infra/cloud-manager/cloud-manager-web && npx tsc --noEmit && npm run build`
- `cloud-manager-api` → `cd vm-infra/cloud-manager/cloud-manager-api/src && dotnet build CloudManager.sln -c Release`
- `vorch-service` → `cd vm-infra/cloud-manager/vorch-service/vorch-service && go build ./... && go vet ./...`
- `vorch-lib` → `cd vm-infra/cloud-manager/vorch-lib && go build ./... && go vet ./...`

Pass: every verifier exits 0 with no errors.
Fail: any verifier reports errors.
