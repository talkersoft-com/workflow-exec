# Task 0004 — Deploy + verify

## Goal
Deploy the fixed binaries to `ubuntu-server.talkersoft.com` via the standard `install-*.py` scripts, re-run the Phase 1 reproduction, and prove the bug is gone. Output: `Results/RESULT.md` populated with before/after.

## Target repo(s)
Same set as Task 0003.

## Steps

1. **Run the install scripts in dependency order** for whichever repos were touched. Always run web LAST so the bundle reflects any backend changes.

   - **API** (if touched):
     ```
     cd vm-infra/cloud-manager/cloud-manager-api
     sudo python3 scripts/api/install-api-service.py
     ```

   - **Vorch** (if `vorch-service` or `vorch-lib` was touched):
     ```
     cd vm-infra/cloud-manager/cloud-manager-api
     sudo python3 scripts/vorch/install-vorch-service.py
     ```

   - **Web** (if touched):
     ```
     sudo rm -rf vm-infra/cloud-manager/cloud-manager-web/dist
     cd vm-infra/cloud-manager/cloud-manager-api
     sudo python3 scripts/web/install-web-app.py
     ```

2. **Confirm each touched service is active**:
   ```
   sudo systemctl is-active cloud-manager-api vorch_service cloud-manager-web
   ```
   Expected: `active` for each. If any shows `failed`, capture journal and write `Retro/FIX-NNN.md` before re-deploying.

3. **Re-run the reproduction** from Task 0001 — same steps, same form values (use a fresh VM name to avoid name-unique violations):
   - UI path or curl per Task 0001 step 3 or 4.
   - Capture the new network response (status, body) and new journal excerpts.

4. **Confirm the success path in the DB**:
   ```
   mcp__database-toolkit-mcp__db_execute_sql_query
     database: clouddb
     sql: "SELECT virtual_machine_id, name, orchestration_status, created
           FROM vm.virtual_machines
           WHERE name = '<new-repro-name>'
           ORDER BY created DESC LIMIT 1;"
   ```
   Expected: row exists; `orchestration_status` is `Initialized` → progresses → `Completed` within a reasonable window. (If `Completed` is too slow because libvirt is slow on this host, `Started` or `Booting` is acceptable evidence that the orchestrator picked up the message.)

5. **Write `Results/RESULT.md`** with:
   - The before/after network response
   - The before/after journal excerpts (one or two key lines each)
   - The post-fix DB row showing the VM was created
   - Confirmation that `systemctl is-active` returns `active` for all touched services

6. **Cleanup the reproduction VM(s)** if they're cluttering up the system (optional — leave a note in RESULT.md if you skip).

## Definition of done
- All touched services are `active`.
- The reproduction from Task 0001 now succeeds (HTTP 2xx, VM row exists, no error in UI).
- `Results/RESULT.md` documents before/after evidence.
- No leftover dirty config files (ASPNETCORE_ENVIRONMENT restored if it was changed in Task 0001).
