# Test 0007 — Force-delete removed (cloud-manager-api)

## TC-001 — Build clean
Pass: `dotnet build src/CloudManager.sln` succeeds with 0 errors.

## TC-002 — No `ForceDelete` references remain
Run: `grep -rn "ForceDelete\|force-delete" cloud-manager-api/src`
Pass: no matches (excluding the changelog / git history).

## TC-003 — Route is gone
Hit `POST /api/v1/vm/{publicId}/force-delete` with a valid public_id.
Pass: 404 (route does not exist).

## TC-004 — Soft-delete path unchanged
`DELETE /api/v1/vm/{publicId}` on a real VM.
Pass: VM is soft-deleted (deleted_at stamped), `destroy_requested` event recorded, `destroy-vm` published to vorch (the normal flow).
