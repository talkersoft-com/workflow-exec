# Test 0008 — `cloud_vm_force_delete` removed (cloud-manager-mcp)

## TC-001 — npm build clean
`cd cloud-manager-mcp && npm ci && npm run build` succeeds.

## TC-002 — Tool absent from source
Run: `grep -rn "force_delete\|forceDelete\|cloud_vm_force_delete" cloud-manager-mcp/src`
Pass: no matches.

## TC-003 — Tool absent from dist
Run: `grep -rn "force_delete\|forceDelete" cloud-manager-mcp/dist`
Pass: no matches.

## TC-004 — MCP loads without errors
Start the MCP locally (`node dist/index.js`).
Pass: starts cleanly, lists tools, no `cloud_vm_force_delete` in the registered tools list.
