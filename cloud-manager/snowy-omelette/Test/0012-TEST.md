# Test 0012 — Ship

## TC-001 — Results + Lessons exist BEFORE ship
Before calling `hv_ship`, confirm:
- `Results/RESULT.md` exists and is filled in
- `Retro/LESSONS.md` exists and is filled in

## TC-002 — `hv_ship` succeeds
Run the ship call from Task 0012. Pass: each affected repo (cloud-manager-api, cloud-manager-mcp, cloud-manager-web) gets a PR; PR URLs are printed.

## TC-003 — Deck auto-advances
After ship, `hv_status` shows all repos on a new branch (not `snowy-omelette`).
The new branch name is recorded in `Results/RESULT.md`.

## TC-004 — Operator notified
The user-facing summary includes the reminder to restart the MCP via `/mcp` so the new cloud-manager-mcp dist (without `cloud_vm_force_delete`) is picked up.
