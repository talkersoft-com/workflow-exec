# Test 0010 — Web retry-teardown button

## TC-001 — Build clean
`cd cloud-manager-web && npm ci && npm run build` succeeds.

## TC-002 — Force-delete button is gone
Load a VM detail page.
Pass: no "Force delete" button anywhere in the page.

## TC-003 — Retry button hidden on healthy VMs
Load a VM whose latest event is `created` or `teardown_succeeded`.
Pass: Retry teardown button is not rendered.

## TC-004 — Retry button visible on `teardown_failed`
Load a VM whose latest event is `teardown_failed`.
Pass: Retry teardown button is rendered and enabled.

## TC-005 — Click triggers POST + timeline refresh
Click the Retry button. Confirm:
Pass: `POST /api/v1/vm/{publicId}/retry-teardown` is sent, response is 200, timeline refreshes and shows a new `retry_teardown_requested` event at the top.

## TC-006 — 400 handling
Force a 400 (e.g. race the VM into `teardown_succeeded` state between page load and click).
Pass: the server's error message is surfaced inline; no full-page error or navigation.
