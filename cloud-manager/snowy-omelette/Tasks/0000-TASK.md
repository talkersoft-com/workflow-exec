# Task 0000 — Initialize Workspace

## Goal
Verify the workspace is clean and on `snowy-omelette` across every repo in the cloud-manager deck.

## Steps
1. Run `hv_status` on the deck (see `../deck.md` for the exact call)
2. Confirm every repo is on branch `snowy-omelette` and clean
3. If any repo is dirty → STOP. Investigate before proceeding.
   - Exception: dirty repo with a merged PR → `hv_stash` → `hv_next` → `hv_unstash`

## Definition of done
All 15 repos on `snowy-omelette`, all clean.
