# Test 0005 — Final Verification

## TC-001 — Full test suite passes
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro && CGO_ENABLED=0 go test ./...
```
Pass: 0 failures.

## TC-002 — make install works
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro && make install 2>&1
```
Pass: exits 0. Fail: any error.

## TC-003 — CONTRACT.md not gitignored
```bash
git -C /Users/talker/workspace/hive-deck-pro/hive-deck-pro check-ignore -v ops/contract/CONTRACT.md
```
Pass: no output. Fail: path printed.

## TC-004 — Results and Lessons exist
```bash
ls /Users/talker/workspace/cloud-manager/planning/workflow-exec/cloud-manager/enormous-kookaburra/Results/RESULT.md
ls /Users/talker/workspace/cloud-manager/planning/workflow-exec/cloud-manager/enormous-kookaburra/Retro/LESSONS.md
```
Pass: both files exist.

## TC-005 — hive-deck-pro shipped
`hv_ship deck: "hive-deck-pro"` — Pass: PR merged. Fail: error.

## TC-006 — cloud-manager planning shipped
`hv_ship deck: "cloud-manager"` — Pass: workflow-exec PR merged. Fail: error.
