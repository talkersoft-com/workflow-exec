# Test 0001 — ops/ Skeleton + Contract

## TC-001 — ops/ package builds
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro && CGO_ENABLED=0 go build ./ops/...
```
Pass: exits 0. Fail: any error.

## TC-002 — All input structs present
```bash
grep -c "type.*Input struct" /Users/talker/workspace/hive-deck-pro/hive-deck-pro/ops/contract.go
```
Pass: 14 (one per operation). Fail: fewer.

## TC-003 — dispatch.go has all ops
```bash
grep -c "case \"" /Users/talker/workspace/hive-deck-pro/hive-deck-pro/ops/dispatch.go
```
Pass: ≥ 14. Fail: fewer.

## TC-004 — CONTRACT.md exists and is not ignored
```bash
ls /Users/talker/workspace/hive-deck-pro/hive-deck-pro/ops/contract/CONTRACT.md
git -C /Users/talker/workspace/hive-deck-pro/hive-deck-pro check-ignore -v ops/contract/CONTRACT.md
```
Pass: file exists, check-ignore prints nothing. Fail: file missing or ignored.

## TC-005 — schema.json exists
```bash
ls /Users/talker/workspace/hive-deck-pro/hive-deck-pro/ops/contract/schema.json
```
Pass: exists. Fail: missing.
