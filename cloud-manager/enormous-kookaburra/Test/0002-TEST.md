# Test 0002 — Handlers Ported

## TC-001 — No "not implemented" stubs remain
```bash
grep -r "not implemented" /Users/talker/workspace/hive-deck-pro/hive-deck-pro/ops/
```
Pass: no output. Fail: any match.

## TC-002 — Build passes
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro && CGO_ENABLED=0 go build ./...
```
Pass: exits 0. Fail: any error.

## TC-003 — Tests pass
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro && CGO_ENABLED=0 go test ./...
```
Pass: all pass, 0 failures. Fail: any failure.
