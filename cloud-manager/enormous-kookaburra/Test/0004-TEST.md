# Test 0004 — MCP Runner Updated

## TC-001 — No arg-array runHv calls remain
```bash
grep -rn "runHv(\"" /Users/talker/workspace/hive-deck-pro/hive-deck-pro/mcp/src/tools/
```
Pass: no output. Fail: any match (old string-arg pattern still present).

## TC-002 — MCP build passes
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro/mcp && npm run build 2>&1
```
Pass: exits 0, no TypeScript errors.

## TC-003 — runner.ts uses stdin pipe
```bash
grep -c "stdio.*pipe" /Users/talker/workspace/hive-deck-pro/hive-deck-pro/mcp/src/runner.ts
```
Pass: ≥ 1. Fail: 0 (still using "ignore" for stdin).

## TC-004 — JSON smoke tests via CLI
```bash
echo '{"op":"decks"}' | hv
echo '{"op":"workflow","deck":"cloud-manager","name":"planning"}' | hv 2>&1 | head -3
```
Pass: both return valid output. Fail: error message.
