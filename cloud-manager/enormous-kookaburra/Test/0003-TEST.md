# Test 0003 — Cobra Wired + JSON stdin

## TC-001 — Build passes
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro && CGO_ENABLED=0 go build ./...
```
Pass: exits 0.

## TC-002 — Tests pass
```bash
CGO_ENABLED=0 go test ./...
```
Pass: 0 failures.

## TC-003 — cobra output unchanged
```bash
CGO_ENABLED=0 go install ./cmd/hv/ && hv status cloud-manager 2>&1 | head -5
```
Pass: output contains "=== " section headers and clean/dirty status — same format as before.

## TC-004 — JSON stdin dispatch works
```bash
echo '{"op":"status","deck":"cloud-manager"}' | hv 2>&1 | head -5
```
Pass: same output as TC-003. Fail: error or different output.

## TC-005 — No inline logic in cobra RunE bodies
```bash
grep -c "inline logic" /Users/talker/workspace/hive-deck-pro/hive-deck-pro/cmd/hv/main.go
```
Pass: 0. (All RunE bodies delegate to ops.Run*.)
