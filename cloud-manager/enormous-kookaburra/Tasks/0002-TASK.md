# Task 0002 — Port Handlers

## Goal

Move the actual operation logic from `cmd/hv/main.go` cobra RunE bodies into `ops/*.go` handler functions. Wire each handler into `dispatch.go`. Cobra still calls inline logic at this phase (not yet switched to ops calls — that's Phase 3).

## Target repo

`hive-deck-pro` at `/Users/talker/workspace/hive-deck-pro/hive-deck-pro/`

## Steps

1. For each operation, read the existing RunE body in `cmd/hv/main.go` and move the logic into the corresponding `ops/<op>.go` file, replacing the stub. The handler receives the typed input struct and returns `(string, error)`. Any output currently printed via `fmt.Print` should instead be returned as the string.

2. Update `ops/dispatch.go` — each case now calls the real handler (already wired from stubs, just the implementations change).

3. Do NOT modify `cmd/hv/main.go` yet — cobra RunE bodies still have their original inline logic. The goal of this phase is only to get the ops package working correctly in isolation.

4. Run `CGO_ENABLED=0 go build ./...` — must pass.
5. Run `CGO_ENABLED=0 go test ./...` — must pass.

## Definition of done

`go build ./...` and `go test ./...` both pass. Every `ops.Run*` function contains real logic (no more "not implemented" stubs). `cmd/hv/main.go` is unchanged.
