# Task 0005 — Final Tests + Ship

## Goal

Full test pass, install verification, write Results + Lessons, ship both decks.

## Steps

1. Run full test suite:
   ```bash
   cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro
   CGO_ENABLED=0 go test ./...
   ```

2. Verify install:
   ```bash
   CGO_ENABLED=0 go install ./cmd/hv/
   hv status cloud-manager
   hv decks
   echo '{"op":"status","deck":"cloud-manager"}' | hv
   ```

3. Verify CONTRACT.md is not gitignored:
   ```bash
   git -C /Users/talker/workspace/hive-deck-pro/hive-deck-pro check-ignore -v ops/contract/CONTRACT.md
   ```
   Pass: no output (not ignored). Fail: path printed (is ignored).

4. Write `../Results/RESULT.md`
5. Write `../Retro/LESSONS.md`

6. Ship hive-deck-pro:
   ```
   hv_ship  deck: "hive-deck-pro"
            message: "refactor: ops/ package + JSON CLI dispatch + MCP runner update"
            title:   "refactor: ops/ package + JSON CLI dispatch + MCP runner update"
   ```

7. Ship cloud-manager planning:
   ```
   hv_ship  deck: "cloud-manager"
            message: "planning: cloud-manager/enormous-kookaburra"
            title:   "planning: cloud-manager/enormous-kookaburra"
   ```

## Definition of done

All tests pass. Both ships complete. Results and Lessons written before cloud-manager ship.
