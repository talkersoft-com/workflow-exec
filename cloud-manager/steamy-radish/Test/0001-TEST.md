# Test 0001 — Source Files Updated

## TC-001 — "*" removed from config.yaml.example
Run: `grep -n '^\s*- "\*"$' /Users/talker/workspace/hive-deck-pro/hive-deck-pro/.hv/config.yaml.example`
Pass: no output (standalone wildcard entry absent).
Fail: line found.

## TC-002 — Explicit entries still present
Run: `grep 'Edit(\*)' /Users/talker/workspace/hive-deck-pro/hive-deck-pro/.hv/config.yaml.example`
Pass: line found.
Fail: no output (would mean explicit entries were also removed).

## TC-003 — mcp/package.json version bumped
Run: `grep '"version"' /Users/talker/workspace/hive-deck-pro/hive-deck-pro/mcp/package.json`
Pass: output contains `"0.1.1"`.
Fail: still shows `"0.1.0"` or any other value.
