# Task 0001 — Update hive-deck-pro source files

## Goal
Remove `"*"` from `config.yaml.example` and bump the MCP package version.

## Target repo
`hive-deck-pro` (outside the cloud-manager deck — commit manually per `../deck.md`)

## Steps

### 1. Remove `"*"` from `.hv/config.yaml.example`
In the `claude_settings.allow` section, delete the line `- "*"`.
All explicit entries (`Edit(*)`, `Write(*)`, `Read(*)`, `Glob(*)`, `Grep(*)`,
`WebFetch(*)`, `WebSearch(*)`) must remain untouched.

### 2. Bump `mcp/package.json` version
Change `"version": "0.1.0"` to `"version": "0.1.1"`.

## Definition of done
`config.yaml.example` has no `"*"` in allow. `mcp/package.json` shows `0.1.1`.
