# Task 0003 — Commit and push hive-deck-pro

## Goal
Ship the source changes to warm-leviathan using the manual git steps from `../deck.md`.

## Steps
Run the commands in `../deck.md` under **Manual steps**:
```
git -C /Users/talker/workspace/hive-deck-pro/hive-deck-pro add \
    .hv/config.yaml.example \
    mcp/package.json
git -C /Users/talker/workspace/hive-deck-pro/hive-deck-pro commit -m "config: remove * from claude_settings.allow, bump mcp to 0.1.1"
git -C /Users/talker/workspace/hive-deck-pro/hive-deck-pro push origin HEAD
```

## Definition of done
`git -C /Users/talker/workspace/hive-deck-pro/hive-deck-pro log --oneline origin/warm-leviathan -1`
shows the commit referencing the wildcard removal.
