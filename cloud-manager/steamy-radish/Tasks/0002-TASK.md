# Task 0002 — Update live user config

## Goal
Remove `"*"` from `claude_settings.allow` in `~/.hv/config.yaml`.

## Steps
1. Open `~/.hv/config.yaml`
2. In the `claude_settings.allow` section, delete the line `- "*"`
3. All other allow entries must remain untouched

## Definition of done
`grep '"\\*"' ~/.hv/config.yaml` returns no output (or only commented lines).
