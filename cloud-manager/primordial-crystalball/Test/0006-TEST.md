# Test 0006 — End-to-end, deploy, ship

## TC-001 — Secret sealed in the guest
Instantiate a blueprint whose binding maps a managed Secret to `cred_name` (e.g. DBPASSWD).
Pass: on the VM, `/etc/credstore.encrypted/<credName>` exists and decrypts to the expected value
(via a `LoadCredentialEncrypted` test unit or on-host `systemd-creds decrypt`). Fail: absent/wrong.

## TC-002 — No plaintext left
Pass: `/run/cm-seed/<credName>` is gone after first boot; no plaintext secret on the guest disk.
Fail: plaintext present.

## TC-003 — No value leaked
Inspect api logs, vorch logs, cloud-init logs, run output.
Pass: the secret value appears nowhere. Fail: any leak.

## TC-004 — Usage row + gate
Pass: `vm.vm_secret_bindings` has the row; deleting the binding is blocked. Fail: no row / deletes.

## TC-005 — No-binding regression
Provision a VM from a blueprint with no binding.
Pass: succeeds exactly as before; user-data has no injection. Fail: changed/broken.

## TC-006 — Build + deploy healthy
Pass: api + vorch builds clean; api deployed (`cloud_health_check` green); vorch rebuilt + restarted
on the hypervisor (command recorded in RESULT). Fail: build/deploy/restart issue.

## TC-007 — Outputs + PRs
Pass: `Results/RESULT.md` + `Retro/LESSONS.md` exist before integrate; `hv_integrate` opened PRs
(links reported). Fail: missing outputs or no PRs.
