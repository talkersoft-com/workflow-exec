# Test 0003 — Contract: create-vm secrets payload (C# + Go lockstep)

## TC-001 — C# struct carries Secrets
`VmCommandData` has `List<SecretCredData> Secrets` (`CredName`, `B64Value`); populated at publish.
Pass: present + serialized into the message YAML. Fail: missing.

## TC-002 — Go struct matches
`createvm.VM`/`CommandData` has the matching `Secrets` field with identical YAML tags.
Pass: tags match exactly. Fail: tag/casing mismatch.

## TC-003 — Round-trip
Serialize a message with two secrets on the C# side; deserialize on the Go side (and a Go→struct
fixture test).
Pass: secrets intact (credName + b64Value) both directions. Fail: dropped/garbled.

## TC-004 — Backward compatible
Deserialize a legacy message with no `secrets` key on the Go side.
Pass: empty/no secrets, no error. Fail: deserialization error.

## TC-005 — Builds
Pass: `--target api` clean + `vorch-lib` Go build clean. Fail: either errors.
