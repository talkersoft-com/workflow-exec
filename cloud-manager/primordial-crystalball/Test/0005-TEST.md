# Test 0005 — API: populate vm_secret_bindings

## TC-001 — Usage row written on provision
Provision a VM from a blueprint with one attached binding.
Pass: exactly one `vm.vm_secret_bindings` row `{ virtual_machine_id, secret_binding_id, cred_name }`
with a `vsb_…` public_id. Fail: missing/duplicate/wrong fields.

## TC-002 — Idempotent on retry
Re-run provision / retry for the same VM+binding.
Pass: still exactly one row (no duplicate). Fail: duplicate rows.

## TC-003 — Gate enforced (L1)
Attempt to delete the binding now in use.
Pass: blocked by RESTRICT (DB error / API 409). After removing the usage row, delete succeeds.
Fail: binding deletes while used.

## TC-004 — Build
`python3 .cicd/build-cloud-manager.py --target api`. Pass: clean. Fail: error.
