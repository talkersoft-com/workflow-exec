# Test 0001 — API: Vault KV read

## TC-001 — Reads a known secret
Write a test value to `cloudmanager/data/manual/t-read` (via #1.5 Secret Manager or vault toolkit),
then call `ReadKvSecretAsync("cloudmanager/data/manual/t-read")`.
Pass: returns the field map with the expected value. Fail: error or wrong data.

## TC-002 — 404 surfaces distinctly
Call `ReadKvSecretAsync` on a non-existent path.
Pass: a distinct "not found" error (not a generic transport exception). Fail: indistinguishable.

## TC-003 — No value in logs
Inspect logs after TC-001.
Pass: the secret value never appears in any log line. Fail: value present.

## TC-004 — Build
`python3 .cicd/build-cloud-manager.py --target api`. Pass: clean. Fail: compile error.
