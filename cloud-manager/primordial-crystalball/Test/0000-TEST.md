# Test 0000 — Workspace Initialized + #1.5 dependency present

## TC-001 — All repos on feature branch
Run `hv_status`. Pass: every repo on the same feature branch, none on default. Fail: otherwise.

## TC-002 — All repos clean
Pass: no dirty repos. Fail: any uncommitted/unpushed/stash.

## TC-003 — Branch name recorded
Open `../deck.md`. Pass: Branch field filled (not TBD). Fail: still TBD.

## TC-004 — #1.5 schema present
Via db-toolkit schema tools: `marketplace.secrets` exists, `marketplace.secret_bindings.secret_id`
column exists, `vm.vm_secret_bindings` exists.
Pass: all three present. Fail: any missing → STOP (workflow #1.5 not merged).
