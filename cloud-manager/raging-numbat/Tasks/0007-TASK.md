# Task 0007 — Remove force-delete from cloud-manager-api

## Goal
Remove the force-delete endpoint, the `IVirtualMachineService.ForceDelete…` method, and the service implementation. No deprecation shim.

## Files
- `cloud-manager-api/src/CloudManager.API/Controllers/<VirtualMachineController>.cs` — remove the force-delete action.
- `cloud-manager-api/src/Services/CloudManager.Data.Services/Interface/IVirtualMachineService.cs` — remove `ForceDelete…` signature(s).
- `cloud-manager-api/src/Services/CloudManager.Data.Services/VirtualMachineService.cs` — remove the implementation.
- Any AMQP producer code that exists solely to support force-delete (audit before removing — `destroy-vm` is shared with the normal soft-delete path, do NOT touch it).

## Steps
1. Identify every `ForceDelete` / `force-delete` reference in cloud-manager-api source.
2. Delete the controller action, the interface method, and the service method.
3. Verify nothing else in the solution references the removed symbols.
4. Build clean.

## Notes
- The normal `DeleteVirtualMachineAsync` (soft-delete) and its `destroy-vm` publish stay untouched. Only the force-variant is removed.
- If a test references the old method, delete the test rather than leaving a hollow assertion.

## Definition of done
Build clean. `grep -rn "ForceDelete\|force-delete" cloud-manager-api/src` returns no results.
