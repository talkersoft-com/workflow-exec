# Task 0008 — Remove `cloud_vm_force_delete` from cloud-manager-mcp

## Goal
Drop the `cloud_vm_force_delete` tool registration and its handler from cloud-manager-mcp.

## Files
- `cloud-manager-mcp/src/tools/<the force-delete tool file>` — delete.
- `cloud-manager-mcp/src/index.ts` (or wherever tools are registered) — remove the registration call.
- `cloud-manager-mcp/src/runner.ts` or equivalent — remove the API client method if it exists solely for force-delete.

## Steps
1. Identify the tool registration and handler.
2. Delete the files / registration / handler.
3. `cd cloud-manager-mcp && npm ci && npm run build` — must succeed.
4. Verify no other tool references the removed symbols.

## Notes
- The user will need to restart the MCP after this PR merges and deploys (`/mcp` reload). Mention this in `Results/RESULT.md`.

## Definition of done
`npm run build` clean. `grep -rn "force_delete\|forceDelete\|cloud_vm_force_delete" cloud-manager-mcp/src` returns no results.
