# Test 0011 — E2E verify

Per Task 0011 steps.

## TC-001 — Happy path complete
Create → destroy → vorch acks → timeline shows `created`, `destroy_requested`, `teardown_succeeded` in correct order. Retry button is NOT visible.

## TC-002 — Sad path complete
Failed destroy → timeline shows `created`, `destroy_requested`, `teardown_failed`. Retry button IS visible.
Click retry → timeline gains `retry_teardown_requested`, then a follow-up terminal event from the new ack.

## TC-003 — Guard rejects illegal retries
`POST /retry-teardown` on a healthy VM → 400 with the documented message.

## TC-004 — No orphan rows
After both paths, query `vm.vm_events` for the test VMs: every row has a parent VM (no FK violations possible because soft-delete preserves the parent).
