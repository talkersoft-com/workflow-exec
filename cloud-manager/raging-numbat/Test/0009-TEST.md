# Test 0009 — Web timeline view

## TC-001 — Build clean
`cd cloud-manager-web && npm ci && npm run build` succeeds.

## TC-002 — Timeline renders on VM detail page
Load a VM detail page in the running web app for a VM with events.
Pass: VM activity panel is visible; rows are present; most-recent first.

## TC-003 — Empty state
Load a VM detail page for a VM with no events yet.
Pass: a neutral empty-state message is shown (not an error, not a spinner).

## TC-004 — `payload` is expandable
Click an event with a non-empty payload.
Pass: payload expands to a readable JSON block.

## TC-005 — No internal UUIDs in the DOM
Inspect the rendered timeline.
Pass: only public_ids appear; no raw UUIDs leak through.
