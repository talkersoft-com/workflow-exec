# Test 0004 — Ack consumer

## TC-001 — Build clean
Pass: 0 errors.

## TC-002 — Successful ack → `teardown_succeeded`
Soft-delete a VM that vorch can destroy. Wait for the OrchestrationCompletionQueue ack.
Pass: exactly one `teardown_succeeded` row appears for the VM; `payload` contains the ack body; `actor = "vorch-service"`.

## TC-003 — Failed ack → `teardown_failed`
Trigger a destroy on a VM with no libvirt domain (or otherwise broken). Wait for ack.
Pass: exactly one `teardown_failed` row appears; `payload` contains vorch's error message.

## TC-004 — One ack → one event (idempotency)
Replay the same ack message manually. Pass: no duplicate event row inserted.
