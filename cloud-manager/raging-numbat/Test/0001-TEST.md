# Test 0001 — Schema + entity

## TC-001 — Build clean
Run `dotnet build src/CloudManager.sln` from `cloud-manager-api`.
Pass: 0 errors.

## TC-002 — Migration produces expected DDL
Run `dotnet ef migrations script <previous-migration> AddVmEvents` from `src/Models/CloudManager.Entities/`.
Pass: output contains:
- `CREATE TABLE vm.vm_events`
- `CREATE INDEX … ON vm.vm_events (virtual_machine_id, occurred_at DESC)`
- `CREATE INDEX … ON vm.vm_events (event_type, occurred_at DESC)`
- `FOREIGN KEY (virtual_machine_id) REFERENCES vm.virtual_machines(id) ON DELETE RESTRICT`

## TC-003 — Entity column mapping matches PLAN
Inspect the generated migration: every column from PLAN is present with the documented type. `payload` is `jsonb NOT NULL DEFAULT '{}'::jsonb`. `event_type` is `varchar(64) NOT NULL`. `occurred_at` is `timestamptz NOT NULL`.
Fail: any column missing or wrong type.
