# Test 0005 — Retry-teardown service + endpoint

## TC-001 — Build clean
Pass: 0 errors.

## TC-002 — 400 when last terminal event is not `teardown_failed`
On a VM whose latest event is `created`: `POST /api/v1/vm/{publicId}/retry-teardown`.
Pass: 400 with the documented message; no event row inserted.

## TC-003 — 400 when last terminal event is `teardown_succeeded`
On a VM whose latest event is `teardown_succeeded`: POST as above.
Pass: 400; no event row inserted.

## TC-004 — 200 + new event when last terminal event is `teardown_failed`
On a VM whose latest terminal event is `teardown_failed`: POST as above.
Pass: 200; response is a `VmEvent` DTO with `event_type=retry_teardown_requested`; a row was inserted; vorch's `destroy-vm` queue received a fresh message (confirm via vorch's `provision.log` or queue stats).

## TC-005 — 404 on unknown VM
POST with a bogus public_id.
Pass: 404, no event row.
