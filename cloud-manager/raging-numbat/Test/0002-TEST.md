# Test 0002 — VmEventService

## TC-001 — Build clean
Pass: `dotnet build` 0 errors.

## TC-002 — Append-only surface
Inspect `IVmEventService`. Pass: only `RecordEventAsync` + `GetTimelineByVmPublicIdAsync` are public. No Update / Delete methods.

## TC-003 — `RecordEventAsync` round-trips
With the migration applied, call `RecordEventAsync(vmId, VmEventType.Created, new { name = "test" }, "todd@x")`. Query the DB directly: row exists with the supplied event_type, payload, and actor, and `occurred_at` is within a second of UtcNow.

## TC-004 — `GetTimelineByVmPublicIdAsync` ordering
Insert three events with controlled `occurred_at`. Call `GetTimelineByVmPublicIdAsync(publicId, 10)`. Pass: results are ordered by `occurred_at DESC`.

## TC-005 — Surfaces events for soft-deleted VMs
Insert events on a VM, soft-delete it, call `GetTimelineByVmPublicIdAsync`. Pass: events still returned (verifies `IgnoreQueryFilters`).
