# Task 0001 — Contracts

## Steps
1. RESEARCH (read, no changes): CloudManager.Entities models + EntityPrefixRegistry (current
   prefixes), controller route conventions, cloud-manager-mcp tool naming + profiles, web page
   structure, porch run pipeline (cmd/porch + internal/ansible). Note what the marketplace series
   added (Blueprint*, sequencer) — the nine plans build ON this.
2. Write /home/todd/workspace/cloud-manager/planning/workflow-plans/master-plans/
   ANSIBLE-EPIC-CONTRACTS.md:
   - Entity table: every NEW entity across P1–P9 with public-id prefix (collision-checked against
     the registry), schema namespace, owning plan
   - API route shapes per area; MCP tool name patterns; event types (append-only pattern)
   - Variable/secret typing contract (P4) that P5/P8 consume; resolution API contract (P5) that
     P8/P9 consume; editor component contract (P7) that P8 consumes
   - Inter-plan dependency table (who consumes whose contracts)
   - Conventions restated: Audit base, camelCase + public ids, soft-delete, append-only events,
     feature flags (one flag for the epic? decide and record), no-plaintext-secrets invariant

Done when: contracts file complete enough that nine authors could work in parallel without drift.
