# Test 0002
TC-001 five plan folders complete (PLAN/deck/init each). TC-002 spot-check conformance: every
entity named in P1–P5 appears in the contracts entity table with matching prefix; P5 consumes
P4's typing contract verbatim. TC-003 each plan's phases are proportional (no plan exceeds ~6
implementation phases; P2 may declare an internal split instead).
