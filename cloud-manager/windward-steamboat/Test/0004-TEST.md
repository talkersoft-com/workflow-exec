# Test 0004
TC-001 ledger complete with coverage map; every epic § maps to a plan or an explicit deferral.
TC-002 consistency: no plan contradicts the contracts file (spot-check the three cross-consumed
contracts). TC-003 ship opened exactly one workflow-plans PR (stage plan, left OPEN); zero code
repo diffs; RESULT/LESSONS committed in the ship; run STOPPED after announcing.
