# Test 0002 — MCP tools
## TC-001 build — `build-cloud-manager.py --target mcp` exits 0.
## TC-002 create+list — `cloud_secret_binding_create` makes a binding; `cloud_secret_binding_list` returns it (camelCase, public_id).
## TC-003 attach — `cloud_blueprint_secret_binding_attach` + `_list` round-trips against a blueprint.
## TC-004 contract — tool inputs take public_ids (bp_/sb_), never Guids; outputs match the API shape.
