# Test 0003 — Web
## TC-001 build — `build-cloud-manager.py --target web` exits 0.
## TC-002 placeholder scan — typing `.../{vm_public_id}/postgres` adds one param row (name vm_public_id, source defaults to vm); clearing to `/manual/secret/i/stored` removes all rows.
## TC-003 author both — create the manual and postgres bindings via the form; they appear in the list.
## TC-004 blueprint attach — on a blueprint, attach/detach/reorder a Secret Binding; order persists after reload.
