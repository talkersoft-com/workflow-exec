# Test 0001 — API entities + migration + endpoints
## TC-001 migration reversible — `dotnet ef migrations add` then verify Up creates both tables; `database update` then `migrations remove`/down is clean.
## TC-002 CRUD round-trip — create the manual binding (params `[]`) and the postgres binding (one `vm` param); GET returns them with camelCase + `sb_` public_ids; no Guid in any response.
## TC-003 attach — attach the postgres binding to a blueprint with cred_name `DBPASSWD`, position 0; list-by-blueprint returns it; unique-position constraint enforced.
## TC-004 soft-delete — delete a binding; it disappears from list (global filter) but the row remains with deleted_at set.
## TC-005 build — `build-cloud-manager.py --target api` exits 0.
