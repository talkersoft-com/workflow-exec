# Test 0004 — End-to-end + ship
## TC-001 deploy — migration applied before api restart; `--target all` deploy succeeds; `cloud_health_check` Healthy; web reachable.
## TC-002 round-trip — both example bindings exist and agree across UI, API, and MCP (camelCase, public_ids, no Guid leak).
## TC-003 attach persists — the postgres binding is attached to a blueprint with cred_name DBPASSWD and survives a reload.
## TC-004 outputs — Results/RESULT.md + Retro/LESSONS.md exist; hv_integrate returned PR URLs recorded in RESULT.md; RESULT notes the `/mcp` reload requirement.
