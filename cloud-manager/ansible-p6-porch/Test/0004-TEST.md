# Test 0004
TC-001 ordering: e2e run with >16KB output — concatenating chunks by Seq per target equals
the full redacted output with no gaps or duplicates; every chunk ≤32KB. TC-002 tail parity:
the periodic PATCH payload during the same run carries the same fields and last-16KB
truncation as pre-change (capture and compare shape against a pre-change fixture). TC-003
paging: `GET …/log/chunk?afterSeq=N&max=M` returns only Seq>N, at most M, in order. TC-004
redaction: grep all stored chunk content for `hvs.` tokens and the test Vault secret — zero
hits. TC-005 failure tolerance: unit test with chunk endpoint erroring — the run still
reaches its normal terminal status and tail PATCHes continue. TC-006 monotonic Seq per
target under a two-target run (no cross-target Seq interference). TC-007 terminal flush:
the final lines of output appear in chunks even when the last buffer is under 32KB/2s.
TC-008 `go build ./... && go test ./... && go vet ./...` green.
