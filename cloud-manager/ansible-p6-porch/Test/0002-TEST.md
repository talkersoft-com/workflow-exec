# Test 0002
TC-001 invocation: unit test proves the assembled ansible-runner command contains
`--cmdline "--check"` exactly once when the fetched run has `mode: "check"`, and never for
`apply` or absent mode. TC-002 e2e no-mutation: check-mode run of a mutating playbook against
a TEST VM reaches a terminal status and the target file/package/service it would change is
verifiably unchanged on the VM. TC-003 e2e apply parity: the same playbook in apply mode (or
no mode) runs to Succeeded exactly as pre-change; per-target output PATCHes look identical in
shape. TC-004 event: a `playbook_check_completed` event is present on the VM timeline after
the check run; no such event after the apply run. TC-005 contracts untouched: git diff of
vorch-lib shows zero changes in this task; `PlaybookRunMessage` and the five status strings
byte-identical. TC-006 `go build ./... && go test ./... && go vet ./...` green for the
vorch-service module (both binaries).
