# Test 0006
TC-001 migration applied: the `vm` schema shows the run `Mode` column and the `plint` /
`logc` tables on the live DB. TC-002 services healthy post-deploy: `systemctl is-active
vorch_service porch` both active; porch journal shows the lint consumer attached to
`playbook-lints` and no errors in the last 20 lines; `cloud_health_check` OK. TC-003 smoke
green end-to-end through the DEPLOYED stack: check run (terminal, no mutation, event), lint
run (terminal, findings), chunk read (ordered, complete), apply run (Succeeded, familiar
tail) — each with HTTP codes recorded in RESULT.md. TC-004 RESULT.md complete: outcome,
branch, PR table filled from ship output (or verified via `gh`), phase summary, the operator
`/mcp` restart instruction, provisioning + version-tag follow-up notes. TC-005 LESSONS.md
exists with FIX list or explicit clean line. TC-006 ship verified: `gh pr list` shows PRs
(open or auto-merged) for exactly the repos ahead of main — including cloud-manager-api,
vorch-lib, vorch-service, cloud-manager-mcp, workflow-exec — and none for cloud-manager-web.
