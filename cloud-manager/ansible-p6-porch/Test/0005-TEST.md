# Test 0005
TC-001 probe additive: with ansible-lint present, `ansible-studio` flag health reports
healthy via the FeatureFlags health route; simulate absence (PATH without ansible-lint in a
unit test) → degraded health, porch run pipeline still functional. TC-002 `playbooks` probe
untouched: its code path and reported payload are unchanged (diff-level check + existing
probe test still green). TC-003 MCP tools exist: dist rebuild succeeds and the three tools
(`cloud_playbook_lint`, `cloud_playbook_check`, `cloud_run_log_chunks`) are present in the
built tool list with schemas matching contracts §4; `cloud_playbook_check` sends
`mode: "check"` on the run trigger. TC-004 parity gate: apply-mode run PATCH payloads match
the pre-change fixture (fields, truncation); no new required field is read from
`PlaybookRunMessage`. TC-005 builds green: vorch-service build/test/vet and mcp dist build
both pass. TC-006 no web changes: `git -C cloud-manager-web status` / diff vs origin shows
zero changes (deck.md must-not-change).
