# Test 0003
TC-001 additive-only: git diff of vorch-lib touches ONLY the new `PlaybookLintMessage` +
queue const — zero lines changed in existing structs, JSON tags, queue names, status
strings. TC-002 consumer pattern: redelivery of a message whose lint run is already terminal
acks without re-running lint (unit test with mocked API). TC-003 findings parse: ansible-lint
JSON fixture round-trips to the POSTed payload preserving rule id, severity, path, line,
message; zero-findings output marks the run Succeeded, not Failed. TC-004 e2e: lint trigger
on a playbook with a seeded violation reaches a terminal run with ≥1 `plint` finding whose
rule id matches the seeded violation, and a `playbook_lint_completed` event exists; a clean
playbook lints to Succeeded with zero findings. TC-005 redaction: grep the lint run's logs
and persisted findings for any `hvs.` token or fixture secret value — zero hits. TC-006 both
module suites green: vorch-lib and vorch-service `go build ./... && go test ./...` (+ vet on
the service) pass.
