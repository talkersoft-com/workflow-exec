# Task 0005 — Health probe, MCP tools, regression (vorch-service + cloud-manager-mcp repos)

## Steps
1. Health probe (vorch-service): extend the existing porch binary-health probe to also run
   `ansible-lint --version`; PATCH health for the `ansible-studio` flag via the existing
   `…/FeatureFlags/{key}/health` route shape (contracts §11). The existing `playbooks`
   probe is UNTOUCHED — ansible-runner health continues reporting exactly as before.
   ansible-lint missing → `ansible-studio` health degraded, porch still starts and serves
   runs (lint runs fail with the clear message from Task 0003; probe is the guard —
   PLAN.md open question 1 default). If ansible-lint is not installed on this host,
   install it (pipx/pip per host convention) and record the step as a provisioning
   follow-up note in `Results/RESULT.md` — NOT as an eng-dev-tools/os-image diff.
2. MCP (cloud-manager-mcp): extend `ansible.ts` with three tools per contracts §4:
   - `cloud_playbook_lint` — POST lint, return 202 + runId
   - `cloud_playbook_check` — sugar for `cloud_playbook_run` with `mode: "check"`
   - `cloud_run_log_chunks` — read chunks, paged by `afterSeq` (+ `max`)
   Follow the file's existing tool/schema conventions; rebuild dist.
3. Regression — tail-PATCH parity test (plan phase 0005's named gate): an apply-mode run
   with none of the new features exercised produces per-target PATCH payloads
   byte-identical in shape and truncation to pre-change behavior, and `PlaybookRunMessage`
   consumption is unchanged (no new required fields read).
4. Builds: vorch-service `go build ./... && go test ./... && go vet ./...`;
   mcp `python3 .cicd/build-cloud-manager.py --target mcp` (or the repo's standard build)
   must pass.

Done when: `ansible-studio` health reflects ansible-lint presence, `playbooks` probe
untouched, three MCP tools build into dist, parity test green.
