# Task 0003 — Build artifacts, write Results + Lessons, ship

## Goal
Rebuild Go binary and MCP dist, write Results/RESULT.md and Retro/LESSONS.md, then ship hive-deck-pro.

## Steps

1. `CGO_ENABLED=0 go build ./...` — must pass
2. `CGO_ENABLED=0 go test ./...` — must pass
3. `CGO_ENABLED=0 go install ./cmd/hv/` — installs new binary
4. `cd mcp && npm run build` — rebuilds TypeScript dist
5. Instruct user to restart MCP via `/mcp`
6. Write `../Results/RESULT.md`
7. Write `../Retro/LESSONS.md`
8. `hv_ship deck: "hive-deck-pro" message: "feat: await_merge infinite loop + open_browser flag" title: "feat: await_merge infinite loop + open_browser flag"`

## Definition of done
All tests pass. Results and Lessons written. PR opened on hive-deck-pro.
