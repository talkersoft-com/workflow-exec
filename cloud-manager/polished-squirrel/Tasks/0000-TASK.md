# Task 0000 — Initialize Workspace

## Goal
Verify hive-deck-pro is clean and on the correct branch.

## Steps
1. Run `hv_status deck: "hive-deck-pro"`
2. All repos should be clean on `interstellar-beaver` — if on a merged branch run `hv_next`
3. Confirm all 3 repos (hive-deck-pro, workflow-configuration, workflow-fragments) are clean

## Definition of done
All repos on `interstellar-beaver`, all clean.
