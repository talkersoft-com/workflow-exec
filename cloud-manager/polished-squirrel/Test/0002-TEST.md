# Test 0002 — open_browser flag

## TC-001 — OpenBrowser field in config struct
```bash
grep "OpenBrowser" ~/workspace/hive-deck-pro/hive-deck-pro/internal/config/config.go
```
Pass: shows `OpenBrowser bool`. Fail: not found.

## TC-002 — open_browser in config.yaml
```bash
grep "open_browser" ~/workspace/hive-deck-pro/config/workflow-configuration/config.yaml
```
Pass: shows `open_browser: true`. Fail: not found.

## TC-003 — Browser open call in ship.go
```bash
grep "OpenBrowser\|browserCmd\|xdg-open" ~/workspace/hive-deck-pro/hive-deck-pro/ops/ship.go
```
Pass: matches found. Fail: not found.

## TC-004 — Build passes
```bash
cd ~/workspace/hive-deck-pro/hive-deck-pro && CGO_ENABLED=0 go build ./...
```
Pass: exit 0. Fail: any error.
