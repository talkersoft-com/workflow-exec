# Test 0001 — continue_transaction removed; infinite loop in place

## TC-001 — ContinueTransaction gone from contract.go
```bash
grep "ContinueTransaction" ~/workspace/hive-deck-pro/hive-deck-pro/ops/contract.go
```
Pass: no output. Fail: field still present.

## TC-002 — continue_transaction gone from ship.ts
```bash
grep "continue_transaction" ~/workspace/hive-deck-pro/hive-deck-pro/mcp/src/tools/ship.ts
```
Pass: no output. Fail: still present.

## TC-003 — Infinite loop present in ship.go
```bash
grep "for {" ~/workspace/hive-deck-pro/hive-deck-pro/ops/ship.go
```
Pass: at least one match. Fail: no match.

## TC-004 — Build passes
```bash
cd ~/workspace/hive-deck-pro/hive-deck-pro && CGO_ENABLED=0 go build ./...
```
Pass: exit 0, no output. Fail: any error.
