# Test 0003 — Build + Ship

## TC-001 — All tests pass
```bash
cd ~/workspace/hive-deck-pro/hive-deck-pro && CGO_ENABLED=0 go test ./...
```
Pass: all `ok`. Fail: any FAIL.

## TC-002 — Results and Lessons exist
```bash
ls /Users/talker/workspace/cloud-manager/planning/workflow-exec/cloud-manager/polished-squirrel/Results/RESULT.md
ls /Users/talker/workspace/cloud-manager/planning/workflow-exec/cloud-manager/polished-squirrel/Retro/LESSONS.md
```
Pass: both exist. Fail: either missing.

## TC-003 — Ship opens PR
`hv_ship deck: "hive-deck-pro"` — Pass: PR URL returned. Fail: error.
