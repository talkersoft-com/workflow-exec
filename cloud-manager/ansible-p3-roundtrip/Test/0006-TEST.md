# Test 0006
TC-001 `dotnet ef database update` applied cleanly — `ydoc` table present in the live
`clouddb`. TC-002 sidecar container running; `/healthz` 200. TC-003 deploy script (`--target
api`) succeeded; API smoke poll passed; `cloud_health_check` healthy. TC-004 one decompose
call through the deployed API returns a span map (HTTP 200, flag on). TC-005
`Results/RESULT.md` and `Retro/LESSONS.md` exist and were committed by the ship (not left for
the next branch). TC-006 hv_ship opened PRs for exactly the repos ahead of main
(cloud-manager-api, workflow-plans, workflow-exec) — and NO PR for cloud-manager-mcp,
cloud-manager-web, vorch-lib, or vorch-service. TC-007 PR table in RESULT.md filled with real
PR links.
