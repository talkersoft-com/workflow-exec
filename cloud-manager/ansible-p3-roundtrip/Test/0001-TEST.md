# Test 0001
TC-001 sidecar Docker image builds; container starts; `GET /healthz` returns 200. TC-002
pytest golden-file corpus passes: `recompose(decompose(x)) == x` byte-identical for every
corpus file (anchors/aliases, block scalars, comments, flow style, odd indentation all
covered — verify the corpus actually contains each category). TC-003 a structured edit (e.g.
set `hosts: all` on one play) changes ONLY the intended span — surrounding comments, key
order, and styles byte-identical. TC-004 `POST /recompose` with a stale base text/checksum
returns 409. TC-005 no porch/vorch/queue changes — `git status` in vorch-lib, vorch-service
shows no modifications; no new RabbitMQ message types introduced anywhere. TC-006 tooling is
uv + ruff and confined to `roundtrip-service/` — no changes to files outside that folder in
this phase except compose wiring.
