# Test 0002
TC-001 build (`--target api`) passes clean. TC-002 EF migration adds the `ydoc` table in
schema `ansible` matching contracts §1 (checksum + jsonb decomposition map, FK to revision).
TC-003 saving a playbook or role-file revision writes exactly one `ydoc` row for the new
revision; the checksum matches the stored text. TC-004 write-time fidelity check: simulate a
recompose mismatch (or corrupt the sidecar response in a test) — the save FAILS with an
explicit error; no revision/ydoc row is half-written. TC-005 `api/v1/roundtrip/decompose|
recompose` proxy to the sidecar, require auth, and return 404/disabled behaviour when the
`ansible-studio` flag is off. TC-006 responses are camelCase and contain public ids only — no
internal Guids in any new payload.
