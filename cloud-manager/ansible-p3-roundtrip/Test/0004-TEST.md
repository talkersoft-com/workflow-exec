# Test 0004
TC-001 build (`--target api`) passes clean. TC-002 saving a revision with the sidecar up
produces `ptask` rows whose SourceSpan matches the `ydoc` map spans (spot-check a corpus
playbook with comments where the two parsers' marks differ). TC-003 a revision with no `ydoc`
row (simulate sidecar-down or pre-P3 revision) still decomposes — YamlDotNet fallback spans
populated, `decompositionState` unaffected. TC-004 structured-edit saves (Task 0003 path)
trigger re-decomposition exactly like raw-text saves — record counts and spans correct for the
new revision. TC-005 records are still never recomposed to YAML — no write path from record
entities to revision content was added.
