# Test 0003
TC-001 build (`--target api`) passes clean. TC-002 PATCH play (e.g. `hosts: webservers` →
`hosts: all`) on a playbook with comments and custom key order: new revision differs from the
old text ONLY in the edited value — diff shows exactly one changed span, comments and order
intact (the PLAN.md before/after example). TC-003 PATCH task behaves the same. TC-004 the
edit produced a normal `pbrev`/`rfrev` revision: revision count incremented, P2 records
rebuilt for the new revision, the usual events recorded — verified identical side-effects to
a raw-text save of the same content. TC-005 editing against a stale revision returns 409.
TC-006 endpoints are auth-scoped and gated on `ansible-studio`; payloads camelCase with
public ids only. TC-007 no alternative save path was introduced — grep confirms the PATCH
handlers call the existing revision-cutting service, not a new writer.
