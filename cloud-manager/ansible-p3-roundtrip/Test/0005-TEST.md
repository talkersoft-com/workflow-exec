# Test 0005
TC-001 build (`--target api`) passes clean. TC-002 raw-text save of unchanged content is
byte-identical pre/post-P3 — stored revision bytes equal the submitted text exactly. TC-003
with `ansible-studio` OFF: roundtrip routes and PATCH endpoints are hidden/disabled; playbook
and role-file flows behave exactly as pre-P3. TC-004 full pytest golden-file corpus green on
final code. TC-005 deployment docs exist in the api repo and cover compose wiring, healthz,
internal-only exposure, and sidecar-down behaviour. TC-006 deck guardrails hold:
cloud-manager-mcp, cloud-manager-web, vorch-lib, vorch-service show NO changes (`hv_status` /
git status clean in those repos apart from the feature branch itself).
