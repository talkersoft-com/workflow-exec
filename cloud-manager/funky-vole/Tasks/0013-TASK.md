# Task 0013 — Finalize Results + Lessons; ship

## Goal
Finalize Results/RESULT.md + Retro/LESSONS.md, then hv_ship.

## Target repo(s)
All repos touched in Phases 1–10 plus `workflow-exec`.

## Steps

1. **Finalize `Results/RESULT.md`**:
   - Outcome: SHIPPED | PARTIAL | FAILED
   - Branch: from Task 0000
   - PRs: TBD until hv_ship returns
   - Phase summary, one paragraph per phase
   - Pointers to screenshots and `verify.md` from Phase 12

2. **Write `Retro/LESSONS.md`** with what would have helped + FIX files + deviations from plan.

3. **Run `hv_status`** — confirm only `cloud-manager-api`, `vorch-service`, `cloud-manager-web`, `workflow-exec` are dirty.

4. **Run `hv_ship`**:
   ```
   hv_ship  deck: "cloud-manager"
            message: "feat: Vault scoped tokens + requirement enforcement + multi-controller surface"
            title:   "feat: Vault scoped tokens + requirement enforcement + multi-controller surface"
   ```

5. **Stop.**

## Definition of done
- `Results/RESULT.md` and `Retro/LESSONS.md` populated.
- `hv_ship` returned with PR URLs.
- All ORCH.md checkboxes checked.
