# Test 0007 — Host.IsController plumbing

## TC-001 — DTO has IsController
Run:
```
grep -n "IsController" cloud-manager-api/src/Models/CloudManager.DTO/Models/Host.cs
```
Pass: 1 match.

## TC-002 — ResolveControllerHostIdAsync exists
Run:
```
grep -rn "ResolveControllerHostIdAsync" cloud-manager-api/src/Services/CloudManager.Data.Services/
```
Pass: at least 2 matches (interface + impl).

## TC-003 — Build clean
Pass: dotnet build → 0 errors.

## TC-004 — CHANGE-0007.md
Pass: exists, non-empty.
