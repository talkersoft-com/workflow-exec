# Test 0009 — Required collections panel + modal + banner

## TC-001 — Slice exists
Run:
```
ls cloud-manager-web/src/store/slices/playbookCollectionRequirementsSlice.ts
```
Pass: file exists.

## TC-002 — Slice registered
Run:
```
grep -n "playbookCollectionRequirements" cloud-manager-web/src/store/index.ts
```
Pass: 2+ matches.

## TC-003 — API helpers added
Run:
```
grep -nE "playbookRequirements|collection-requirements" cloud-manager-web/src/services/api.ts
```
Pass: at least 3 matches.

## TC-004 — Panel + banner on detail page
Run:
```
grep -nE "Required collections|Missing required collections" cloud-manager-web/src/pages/PlaybookDetailPage.tsx
```
Pass: both strings present.

## TC-005 — Modal Add wired
Run:
```
grep -nE "Add requirement|Add Requirement" cloud-manager-web/src/pages/PlaybookDetailPage.tsx
```
Pass: 1+ match.

## TC-006 — tsc + build clean
Pass: both clean.

## TC-007 — CHANGE-0009.md
Pass: exists, non-empty.
