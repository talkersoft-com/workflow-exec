# Test 0006 — Requirements enforcement in TriggerMultiVmAsync

## TC-001 — RequirementsUnmetException class exists
Run:
```
grep -rn "class RequirementsUnmetException" cloud-manager-api/src/
```
Pass: 1 match.

## TC-002 — SatisfiesVersion present
Run:
```
grep -n "SatisfiesVersion" cloud-manager-api/src/Services/CloudManager.Data.Services/PlaybookRunService.cs
```
Pass: 2+ matches (definition + call).

## TC-003 — Enforcement runs before run-row insert
Run:
```
grep -nE "RequirementsUnmetException|PlaybookCollectionRequirements" cloud-manager-api/src/Services/CloudManager.Data.Services/PlaybookRunService.cs
```
Pass: at least 1 match for each.

## TC-004 — Controller maps to 400
Run:
```
grep -nE "RequirementsUnmetException|requirements_unmet|BadRequest" cloud-manager-api/src/CloudManager.API/Controllers/PlaybookController.cs
```
Pass: at least one BadRequest path tied to RequirementsUnmetException.

## TC-005 — Build clean
Pass: dotnet build → 0 errors.

## TC-006 — CHANGE-0006.md
Pass: exists, non-empty.
