# Test 0004 — Vorch token forwarding

## TC-001 — runDTO has vaultRunToken
Run:
```
grep -nE 'VaultRunToken.*json:"vaultRunToken' vorch-service/vorch-service/handlers/playbook_run_handler.go
```
Pass: 1 match.

## TC-002 — Env injection wired
Run:
```
grep -nE 'VAULT_TOKEN|VAULT_ADDR' vorch-service/vorch-service/handlers/playbook_run_exec.go
```
Pass: 2+ matches.

## TC-003 — Token not logged at value
Run:
```
grep -E "log\.Print.*\+\\s*run\.VaultRunToken|VAULT_TOKEN\s*=\\s*%s" vorch-service/vorch-service/handlers/
```
Pass: zero matches.
Fail: any code that prints the token literal.

## TC-004 — go build + vet clean
Run:
```
cd vorch-service/vorch-service && go build ./... && go vet ./...
```
Pass: both clean.

## TC-005 — CHANGE-0004.md
Pass: exists, non-empty.
