# Test 0011 — Build + deploy

## TC-001 — Migration applied (columns exist)
Run:
```
PGPASSWORD='P@ssw0rd!' psql -h localhost -U cloudmanager -d clouddb -tAc "SELECT count(*) FROM information_schema.columns WHERE (table_schema='vm' AND column_name='vault_run_token') OR (table_schema='bare_metal' AND column_name='is_controller');"
```
Pass: 2.

## TC-002 — All services active
Run:
```
sudo systemctl is-active cloud-manager-api vorch_service cloud-manager-web
```
Pass: all 3 print `active`.

## TC-003 — Hosts API exposes isController
Run:
```
curl -sk https://ubuntu-server.talkersoft.com/api/v1/host/list | python3 -c "import json,sys;d=json.load(sys.stdin);print(any('isController' in h for h in d))"
```
Pass: `True`.

## TC-004 — Requirements endpoint exists
Run:
```
curl -sk -o /dev/null -w "%{http_code}\n" "https://ubuntu-server.talkersoft.com/api/v1/ansible/playbooks/pb_EMEJCH08GG/collection-requirements"
```
Pass: `200`.

## TC-005 — Web bundle has new strings
Run:
```
sudo grep -l "Required collections" /opt/cloud-manager-web/assets/*.js | head -1
```
Pass: at least one file.
