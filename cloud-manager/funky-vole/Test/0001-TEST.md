# Test 0001 — Migration applied

## TC-001 — Migration file present
Run:
```
ls cloud-manager-api/src/Models/CloudManager.Entities/Migrations/*AddVaultRunTokenAndControllerHost*
```
Pass: 2 files (.cs + .Designer.cs).

## TC-002 — Build clean
Run:
```
cd cloud-manager-api/src && dotnet build CloudManager.sln -c Release
```
Pass: 0 errors.

## TC-003 — vault_run_token column exists
Run:
```
PGPASSWORD='P@ssw0rd!' psql -h localhost -U cloudmanager -d clouddb -tAc "SELECT column_name FROM information_schema.columns WHERE table_schema='vm' AND table_name='playbook_runs' AND column_name='vault_run_token';"
```
Pass: `vault_run_token`.

## TC-004 — is_controller column exists
Run:
```
PGPASSWORD='P@ssw0rd!' psql -h localhost -U cloudmanager -d clouddb -tAc "SELECT column_name FROM information_schema.columns WHERE table_schema='bare_metal' AND table_name='hosts' AND column_name='is_controller';"
```
Pass: `is_controller`.

## TC-005 — ubuntu-server backfilled to true
Run:
```
PGPASSWORD='P@ssw0rd!' psql -h localhost -U cloudmanager -d clouddb -tAc "SELECT is_controller FROM bare_metal.hosts WHERE name='ubuntu-server';"
```
Pass: `t`.

## TC-006 — CHANGE-0001.md recorded
Pass: file exists, non-empty.
