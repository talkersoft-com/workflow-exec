# Test 0008 — Web controller selector + Collections integration

## TC-001 — selectedControllerSlice exists
Run:
```
ls cloud-manager-web/src/store/slices/selectedControllerSlice.ts
```
Pass: file exists.

## TC-002 — Registered in store
Run:
```
grep -n "selectedController" cloud-manager-web/src/store/index.ts
```
Pass: 2+ matches (import + reducer entry).

## TC-003 — localStorage wired
Run:
```
grep -nE "localStorage|cm\.selectedControllerHostId" cloud-manager-web/src/store/slices/selectedControllerSlice.ts
```
Pass: at least 2 matches.

## TC-004 — Collections list page uses selector
Run:
```
grep -nE "isController|selectedController" cloud-manager-web/src/pages/AnsibleCollectionsPage.tsx
```
Pass: at least 2 matches.

## TC-005 — Detail page renders All-controllers summary
Run:
```
grep -nE "All controllers" cloud-manager-web/src/pages/AnsibleCollectionDetailPage.tsx
```
Pass: 1 match.

## TC-006 — tsc + build clean
Run:
```
cd cloud-manager-web && npx tsc --noEmit && npm run build
```
Pass: both clean.

## TC-007 — CHANGE-0008.md
Pass: exists, non-empty.
