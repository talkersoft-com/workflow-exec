# Test 0005 — Requirements CRUD

## TC-001 — DTO + Service + Controller exist
Run:
```
ls cloud-manager-api/src/Models/CloudManager.DTO/Models/PlaybookCollectionRequirement.cs
ls cloud-manager-api/src/Services/CloudManager.Data.Services/PlaybookCollectionRequirementService.cs
ls cloud-manager-api/src/CloudManager.API/Controllers/PlaybookCollectionRequirementsController.cs
```
Pass: all three.

## TC-002 — Routes registered
Run:
```
grep -nE '\[Http(Get|Post|Delete)' cloud-manager-api/src/CloudManager.API/Controllers/PlaybookCollectionRequirementsController.cs | wc -l
```
Pass: 3.

## TC-003 — DI registration
Run:
```
grep -n "PlaybookCollectionRequirementService" cloud-manager-api/src/Services/CloudManager.Data.Services/ConfigureServices.cs
```
Pass: 1 match.

## TC-004 — Build clean
Pass: dotnet build → 0 errors.

## TC-005 — CHANGE-0005.md
Pass: exists, non-empty.
