# Test 0010 — AnsibleRunPage blocks on unmet requirements

## TC-001 — Run blocked logic present
Run:
```
grep -nE "Run blocked|missing required" cloud-manager-web/src/pages/AnsibleRunPage.tsx
```
Pass: 1+ match.

## TC-002 — Requirements fetched on playbook select
Run:
```
grep -nE "fetchForPlaybook|playbookCollectionRequirements" cloud-manager-web/src/pages/AnsibleRunPage.tsx
```
Pass: 1+ match.

## TC-003 — Run button disable wired
Run:
```
grep -nE "disabled.*missing|missing.*length" cloud-manager-web/src/pages/AnsibleRunPage.tsx
```
Pass: 1+ match showing disable condition.

## TC-004 — tsc + build clean
Pass: both clean.

## TC-005 — CHANGE-0010.md
Pass: exists, non-empty.
