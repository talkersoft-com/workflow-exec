# Test 0003 — API mint + revoke + sweeper + DTO scrub

## TC-001 — DTO has VaultRunToken
Run:
```
grep -n "VaultRunToken" cloud-manager-api/src/Models/CloudManager.DTO/Models/PlaybookRun.cs
```
Pass: 1 match.

## TC-002 — Mint call wired in TriggerMultiVmAsync
Run:
```
grep -nE "CreateChildTokenAsync|WritePolicyAsync|VaultRunToken =" cloud-manager-api/src/Services/CloudManager.Data.Services/PlaybookRunService.cs
```
Pass: at least 3 matches.

## TC-003 — RevokeRunVaultAsync exists
Run:
```
grep -n "RevokeRunVaultAsync" cloud-manager-api/src/Services/CloudManager.Data.Services/PlaybookRunService.cs
```
Pass: at least 2 matches (definition + call site).

## TC-004 — Sweeper hosted service registered
Run:
```
grep -nE "AddHostedService<VaultRunTokenSweeper>|class VaultRunTokenSweeper" cloud-manager-api/src/CloudManager.API/
```
Pass: at least 1 match for the class definition and 1 for `AddHostedService<...>`.

## TC-005 — List endpoint scrub
Run:
```
grep -nE "VaultRunToken = null" cloud-manager-api/src/Services/CloudManager.Data.Services/PlaybookRunService.cs
```
Pass: 1+ match.

## TC-006 — Vault-down returns 503
Run:
```
grep -nE "503|StatusCode\(503\)|VaultException" cloud-manager-api/src/CloudManager.API/Controllers/PlaybookController.cs
```
Pass: 503 or VaultException handling visible.

## TC-007 — Build clean
Pass: dotnet build → 0 errors.

## TC-008 — CHANGE-0003.md
Pass: exists, non-empty.
