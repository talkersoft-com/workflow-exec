# Test 0012 — End-to-end acceptance

## TC-001 — A.6: pg run gets a scoped Vault token + revoked on terminal
- Trigger `pg` via UI. After completion:
  - `SELECT vault_secrets_prefix, vault_run_token IS NOT NULL AS has_token FROM vm.playbook_runs ORDER BY created DESC LIMIT 1;` → prefix matches `cloudmanager/data/playbook-secrets/<runId>`, has_token = `f` (revoked).
  - API journal during the run shows `hvs.<REDACTED>` strings, never raw tokens.
- Pass: both checks succeed and `Results/A6-vault-audit.md` exists.

## TC-002 — B.5: unmet requirement disables Run
- Add a requirement for `community.postgresql` to `pg`.
- Remove `community.postgresql` from Collections page.
- Confirm:
  - Banner appears on PlaybookDetailPage.
  - Run page Run button disabled with inline missing-collection message.
- Re-install `community.postgresql`; banner clears, Run button re-enabled.
- Pass: all three artifacts present (`B5-banner-unmet.png`, `B5-run-blocked.png`, `B5-run-enabled.png`).

## TC-003 — C.5: controller selector renders and persists
- Collections page shows a controller `<select>` populated from `isController=true` hosts.
- After picking `ubuntu-server` and reloading: same selection is restored.
- Pass: screenshot `Results/C5-selector.png` proves the dropdown + persisted selection.

## TC-004 — verify.md written
Run: `test -s Results/verify.md`. Pass.

## TC-005 — No unexpected dirty files
Run: `git status --porcelain` per repo. Pass: only the four expected repos dirty.
