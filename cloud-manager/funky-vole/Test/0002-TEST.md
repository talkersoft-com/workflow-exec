# Test 0002 — Vault client project + redaction enricher

## TC-001 — Project + interface
Run:
```
test -f cloud-manager-api/src/Services/CloudManager.Vault.Client/CloudManager.Vault.Client.csproj
grep -n "interface IVaultClient" cloud-manager-api/src/Services/CloudManager.Vault.Client/*.cs
```
Pass: both succeed.

## TC-002 — DI registration
Run:
```
grep -rn "IVaultClient\|VaultClient" cloud-manager-api/src/Services/CloudManager.Vault.Client/ | grep -i "addhttp\|addsingleton\|addtransient"
```
Pass: at least one DI line.

## TC-003 — Project referenced
Run:
```
grep -E "CloudManager\.Vault\.Client" cloud-manager-api/src/CloudManager.API/CloudManager.API.csproj cloud-manager-api/src/Services/CloudManager.Data.Services/CloudManager.Data.Services.csproj
```
Pass: at least 2 references.

## TC-004 — Redaction enricher present
Run:
```
grep -rnE 'hvs\\\\?\\.|REDACTED|hvs[^a-zA-Z]' cloud-manager-api/src/CloudManager.API/ cloud-manager-api/src/Services/CloudManager.Vault.Client/ | grep -iE "enrich|redact|log"
```
Pass: at least one match for the redaction logic.

## TC-005 — Build clean
Run:
```
cd cloud-manager-api/src && dotnet build CloudManager.sln -c Release
```
Pass: 0 errors.

## TC-006 — CHANGE-0002.md
Pass: file exists, non-empty.
