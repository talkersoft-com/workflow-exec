# Test 0001 — HV_HOME Direct Path

## TC-001 — Build passes
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro && CGO_ENABLED=0 go build ./...
```
Pass: exits 0. Fail: any error.

## TC-002 — HV_HOME direct path works
```bash
HV_HOME=/Users/talker/workspace/cloud-manager/config/workflow-configuration hv decks 2>&1 | head -5
```
Pass: lists deck names (not an error). Fail: "config not found" or similar error.

## TC-003 — Tests pass
```bash
CGO_ENABLED=0 go test ./...
```
Pass: 0 failures.
