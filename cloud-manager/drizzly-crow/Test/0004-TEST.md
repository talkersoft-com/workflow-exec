# Test 0004 — End-to-End Verify + Ship

## TC-001 — HV_HOME set from .env
```bash
source ~/.hv/.env && echo $HV_HOME
```
Pass: prints the workflow-configuration repo path. Fail: empty.

## TC-002 — hv uses repo config
```bash
source ~/.hv/.env && hv status cloud-manager 2>&1 | tail -3
```
Pass: shows clean repos count. Fail: parse error or config-not-found.

## TC-003 — Results and Lessons exist
```bash
ls /Users/talker/workspace/cloud-manager/planning/workflow-exec/cloud-manager/drizzly-crow/Results/RESULT.md
ls /Users/talker/workspace/cloud-manager/planning/workflow-exec/cloud-manager/drizzly-crow/Retro/LESSONS.md
```
Pass: both exist. Fail: either missing.

## TC-004 — Ships complete
`hv_ship deck: "hive-deck-pro"` → Pass: PR opened.
`hv_ship deck: "cloud-manager"` → Pass: PR opened.
