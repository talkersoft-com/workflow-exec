# Test 0003 — make configure Script

## TC-001 — Script exists and is executable
```bash
ls -l /Users/talker/workspace/hive-deck-pro/hive-deck-pro/scripts/configure-hv-home.sh
```
Pass: file exists with execute bit. Fail: missing or not executable.

## TC-002 — make configure succeeds
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro && make configure
```
Pass: exits 0, prints "hv: wrote HV_HOME=...". Fail: error or "not provisioned".

## TC-003 — ~/.hv/.env written
```bash
cat ~/.hv/.env
```
Pass: contains `export HV_HOME=`.  Fail: file missing or empty.
