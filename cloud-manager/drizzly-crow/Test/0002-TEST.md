# Test 0002 — workflow-configuration Repo

## TC-001 — Repo exists on GitHub
```bash
gh repo view talkersoft-com/workflow-configuration --json name 2>&1
```
Pass: returns JSON with name. Fail: "not found".

## TC-002 — Repo provisioned on disk
```bash
ls ~/workspace/cloud-manager/config/workflow-configuration/config.yaml
```
Pass: file exists. Fail: not found.

## TC-003 — config.yaml present in repo
```bash
ls ~/workspace/cloud-manager/config/workflow-configuration/decks/ | head -3
```
Pass: lists deck yaml files. Fail: directory missing or empty.

## TC-004 — Appears in hv status
```bash
hv status cloud-manager 2>&1 | grep "workflow-configuration"
```
Pass: repo listed. Fail: missing.
