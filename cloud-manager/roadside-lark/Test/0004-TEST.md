# Test 0004 — cutover
## TC-001 — backups exist
Vault snapshot file + pg_dump file exist and are non-empty; paths recorded in RESULT.md.
## TC-002 — services active
`systemctl is-active cloud-manager-api vorch_service porch` -> all `active`.
## TC-003 — API healthy on creds
`cloud_health_check` -> Healthy; API connected to PG/RabbitMQ with the rotated creds (no auth errors in journal).
