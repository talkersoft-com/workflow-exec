# Test 0005 — end-to-end + ship
## TC-001 — clean of secrets
`systemctl cat` of the three services has no secret/token substrings.
## TC-002 — VM provisions
A freshly created Jammy VM reaches `orchestrationStatus: 2` with an IP within ~90s (vorch wrote the SSH key to Vault via the cred token). Tear down after.
## TC-003 — outputs + PRs
`Results/RESULT.md` and `Retro/LESSONS.md` exist; `hv_integrate` returned PR URLs recorded in RESULT.md.
