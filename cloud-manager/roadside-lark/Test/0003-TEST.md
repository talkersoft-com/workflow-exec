# Test 0003 — unit wiring
## TC-001 — creds materialize
After daemon-reload + restart, for each service `ls -l $CREDENTIALS_DIRECTORY` shows the expected cred files at 0400, owned by the service user.
## TC-002 — no plaintext in units
`systemctl cat cloud-manager-api vorch_service porch | grep -iE 'P@ssw0rd|DBPASSWD=|RABBITMQ_PASSWORD=|hvs\.'` -> no matches.
## TC-003 — no plaintext RabbitMQ for vorch/porch
`grep -riE 'P@ssw0rd|password' config-server.yaml` shows no plaintext RabbitMQ password (cred path or absent).
