# Test 0002 — API AddKeyPerFile
## TC-001 — builds
`python3 .cicd/build-cloud-manager.py --target api` exits 0.
## TC-002 — cred overrides env
With a temp dir containing a file `DBPASSWD` and `CREDENTIALS_DIRECTORY` set to it, the API resolves `DBPASSWD` to the file content (not the env value). Verify via a focused test or a startup log of the resolved source.
## TC-003 — backward compatible
With no `CREDENTIALS_DIRECTORY`, the API still reads `DBPASSWD` from env (no regression).
