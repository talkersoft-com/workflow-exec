# Test 0001 — cm-seed-secrets
## TC-001 — creds produced
Run the seeder on the controller. Pass: `/etc/cloud-manager/creds/{vault-token,db-password,rabbitmq-password}.cred` exist, mode 0600.
## TC-002 — idempotent
Run it a second time. Pass: PG/RabbitMQ password unchanged (login with the same value still works); no error; creds re-rendered identically in meaning.
## TC-003 — no secret leak
Capture stdout/stderr. Pass: no password or `hvs.` token substring printed.
## TC-004 — Vault is source of truth
`vault kv get cloudmanager/controller/postgres` and `.../rabbitmq` return the stored values.
