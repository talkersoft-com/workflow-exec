# Test 0004 — Browser verification

## TC-001 — Flow A: list → editor → save → close lands on list
Pass: `verify.md` records URL `/ansible/playbooks` after Close, and the matching screenshot is at `Results/flow-a-on-list.png`.
Fail: URL is anything else, or screenshot missing.

## TC-002 — Flow B: detail → editor → dirty close → confirm → land on detail
Pass: `verify.md` records that the confirmation appeared, that Cancel kept the user on the editor, and that confirming returned to `/ansible/playbooks/<pid>`. Screenshot at `Results/flow-b-on-detail.png`.
Fail: any step doesn't match the expected URL or no confirmation observed.

## TC-003 — Flow C: cold-load `/edit` → close → land on detail (fallback)
Pass: `verify.md` records URL `/ansible/playbooks/<pid>` after Close. Screenshot at `Results/flow-c-on-detail-fallback.png`.
Fail: URL is `/` or any non-detail page.

## TC-004 — Sub-nav stays on Playbooks during editing
Pass: `Results/sub-nav-during-edit.png` shows the Playbooks tab with the active visual treatment while the editor is open.
Fail: a different tab is highlighted or the sub-nav is missing.

## TC-005 — Save created a new revision
Pre-run: capture `count_before = SELECT count(*) FROM vm.playbook_revisions WHERE playbook_id = (SELECT id FROM vm.playbooks WHERE public_id = 'pb_EMEJCH08GG');`.
Post-Flow-A: `count_after = same query`.
Pass: `count_after = count_before + 1`.
Fail: any other delta.

## TC-006 — verify.md written
Run:
```
test -s Results/verify.md
```
Pass: exists and non-empty.
Fail: missing or empty.

## TC-007 — No source code edits during verification
Run:
```
git -C cloud-manager-web status --porcelain
git -C cloud-manager-api status --porcelain
git -C vorch-service status --porcelain
git -C vorch-lib status --porcelain
```
Pass: same dirty set as end of Phase 2 (only `cloud-manager-web` files from Phases 1 + 2).
Fail: any additional repo or any unexpected file dirty.
