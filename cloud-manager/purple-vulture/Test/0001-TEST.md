# Test 0001 — Editor close + referrer + dirty-guard + layout

## TC-001 — `RoleFileEditor` exposes `onDirtyChange`
Run:
```
grep -nE "onDirtyChange" cloud-manager-web/src/components/RoleFileEditor/RoleFileEditor.tsx
```
Pass: at least one match (interface/prop declaration AND the firing site).
Fail: no match → prop not added.

## TC-002 — `AnsiblePlaybookEditorPage` has Close + referrer logic
Run:
```
grep -nE "navigate\(|window\.confirm\(|location\.state\?\.from|from\.pathname" cloud-manager-web/src/pages/AnsiblePlaybookEditorPage.tsx
```
Pass: at least one match for `navigate(` AND one referencing `from` AND one matching `window.confirm`.
Fail: any of the three is missing.

## TC-003 — Defensive `/ansible/` guard present
Run:
```
grep -n "startsWith(\"/ansible/\")" cloud-manager-web/src/pages/AnsiblePlaybookEditorPage.tsx
```
Pass: 1+ match.
Fail: 0 matches → defensive guard skipped.

## TC-004 — `beforeunload` listener present
Run:
```
grep -n "beforeunload" cloud-manager-web/src/pages/AnsiblePlaybookEditorPage.tsx
```
Pass: 1+ match.
Fail: 0 matches.

## TC-005 — Magic-number height removed
Run:
```
grep -n "calc(100vh - 120px)" cloud-manager-web/src/pages/AnsiblePlaybookEditorPage.tsx
```
Pass: 0 matches.
Fail: 1+ matches → height was not replaced.

## TC-006 — Typecheck + build clean
Run:
```
cd cloud-manager-web && npx tsc --noEmit && npm run build
```
Pass: both commands exit 0 with no error output.
Fail: any errors.

## TC-007 — CHANGE-0001.md recorded
Run:
```
test -s Retro/CHANGE-0001.md
```
Pass: file exists and non-empty.
Fail: missing or empty.

## TC-008 — Out-of-scope files not edited
Run:
```
git -C cloud-manager-web status --porcelain | grep -vE "RoleFileEditor\.tsx|AnsiblePlaybookEditorPage\.tsx"
```
Pass: zero output (only the two expected files dirty in this phase).
Fail: any other file appears.
