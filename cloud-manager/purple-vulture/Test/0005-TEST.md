# Test 0005 — Shipped

## TC-001 — Results/RESULT.md and Retro/LESSONS.md present
Run:
```
test -s Results/RESULT.md && test -s Retro/LESSONS.md
```
Pass: both non-empty.
Fail: either missing or empty.

## TC-002 — hv_ship succeeded
Inspect Task 0005's `hv_ship` output.
Pass: at least one PR URL is printed; no DIRTY warnings for repos outside scope.
Fail: ship returned an error, or shipped repos not named in deck.md.

## TC-003 — Deck advanced
Run `hv_status`.
Pass: branch is no longer the execution branch from Task 0000 (deck has rolled forward).
Fail: still on the execution branch.
