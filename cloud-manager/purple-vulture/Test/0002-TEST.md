# Test 0002 — Entry points

## TC-001 — Detail page has Edit button with `from` state
Run:
```
grep -nE "from: \{ pathname|/edit|navigate\(" cloud-manager-web/src/pages/PlaybookDetailPage.tsx
```
Pass: matches show a `navigate(...)/edit` call AND a `from: { pathname` payload.
Fail: missing either.

## TC-002 — Playbooks list has per-row pencil with `stopPropagation`
Run:
```
grep -nE "stopPropagation\(\)|/edit|from: \{ pathname" cloud-manager-web/src/pages/PlaybooksPage.tsx
```
Pass: all three concerns appear (a `/edit` navigate, a `from:` payload, a `stopPropagation()` to prevent row-click bubble).
Fail: any missing.

## TC-003 — Typecheck + build clean
Run:
```
cd cloud-manager-web && npx tsc --noEmit && npm run build
```
Pass: both exit 0.
Fail: any error.

## TC-004 — CHANGE-0002.md recorded
Run:
```
test -s Retro/CHANGE-0002.md
```
Pass: exists, non-empty.
Fail: missing or empty.

## TC-005 — Out-of-scope files not edited
Run:
```
git -C cloud-manager-web status --porcelain | grep -vE "PlaybookDetailPage\.tsx|PlaybooksPage\.tsx|RoleFileEditor\.tsx|AnsiblePlaybookEditorPage\.tsx"
```
Pass: zero output.
Fail: any other file appears.
