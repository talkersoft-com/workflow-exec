# Lessons: cloud-manager/purple-vulture

## What would have helped
TBD — filled at Task 0005. Be specific: missing MCP method, ambiguous task instruction, test that gave a false positive, doc that should exist. If nothing went wrong, write a single line saying so.

## Fix files written
TBD — one line per `FIX-NNN.md`. None expected on the happy path.

## Deviations from plan
TBD — if the implementer chose differently than the plan recommended (e.g. used a `ConfirmDialog` instead of `window.confirm`, or added an icon-only Close instead of a text label), record it here with a one-line justification.
