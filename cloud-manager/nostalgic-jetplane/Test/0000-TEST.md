# Test 0000 — Workspace Initialized

## TC-001 — All repos on feature branch
Run `hv_status` on the deck.
Pass: every repo shows the same feature branch name, no repo is on the default branch.
Fail: any repo is on the default branch or a different branch.

## TC-002 — All repos clean
Pass: hv_status shows no dirty repos (no uncommitted changes, unpushed commits, or stash entries).
Fail: any repo dirty.

## TC-003 — Branch name recorded
Open `../deck.md`.
Pass: the Branch field is filled in with the generated branch name (`rakish-coldfront`), not "TBD".
Fail: Branch field still says TBD or is missing.

## TC-004 — Dependency surface present
Pass: `vm.vm_secret_bindings` table exists on `clouddb`, AND the existing
`PATCH /api/v1/blueprint/{blueprintId}/secret-binding/{bsbId}` endpoint is present in the api source.
Fail: either is missing (→ STOP, #2/#1.5 not landed).
