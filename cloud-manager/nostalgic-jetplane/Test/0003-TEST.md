# Test 0003 — MCP: `cloud_blueprint_secret_binding_update`

## TC-001 — Updates credName
Attach a binding to a blueprint, then
`cloud_blueprint_secret_binding_update({ id, bsbId, credName: "NEWNAME" })`.
Pass: `cloud_blueprint_secret_binding_list` shows the new `credName`; `position` unchanged. Fail:
no change or position clobbered.

## TC-002 — Updates position (reorder)
`cloud_blueprint_secret_binding_update({ id, bsbId, position: N })`.
Pass: the attachment's position is N; `credName` unchanged. Fail: no change or credName clobbered.

## TC-003 — Send-only-provided
Call with only one of `credName` / `position`.
Pass: the omitted field is untouched (the PATCH body omits it). Fail: the omitted field is reset.

## TC-004 — Wraps the existing endpoint (no API change)
Pass: the tool calls `PATCH /api/v1/blueprint/{id}/secret-binding/{bsbId}` — no new/modified API
code in this task. Fail: required an API change.

## TC-005 — Build
`python3 .cicd/build-cloud-manager.py --target mcp`. Pass: clean. Fail: build error.
