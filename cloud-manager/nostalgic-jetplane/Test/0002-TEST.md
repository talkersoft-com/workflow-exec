# Test 0002 — MCP: `cloud_vm_secret_binding_list`

## TC-001 — Lists usage rows against the live API
`cloud_vm_secret_binding_list({ id: "vm_…" })` for a VM with a usage row.
Pass: returns the usage rows (`publicId`, `credName`, `secretBindingId`, `secretBindingName`,
`resolvedVaultPath`, `createdAt`), matching the Task 0001 endpoint. Fail: error or mismatch.

## TC-002 — Empty list for a VM with no bindings
Pass: returns `[]`. Fail: error or non-empty.

## TC-003 — No secret value
Inspect the tool output.
Pass: only metadata + the Vault path; no secret value field. Fail: any value present.

## TC-004 — Input shape
Pass: the tool takes a single `id` (`vm_…`) and rejects/handles a missing id cleanly. Fail:
wrong/extra required inputs.

## TC-005 — Build
`python3 .cicd/build-cloud-manager.py --target mcp`. Pass: clean. Fail: build error.
