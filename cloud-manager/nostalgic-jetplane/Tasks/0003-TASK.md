# Task 0003 — MCP: `cloud_blueprint_secret_binding_update`

## Goal
Close the MCP gap on the blueprint↔binding attachment: attach/detach/list exist, but the existing
`PATCH` (change `credName` / reorder) has no tool. Add it, mirroring `cloud_blueprint_playbook_reorder`
and the existing attach tool.

## Reference
`../../../workflow-plans/cloud-manager/nostalgic-jetplane/PLAN.md` — Design §3.

## Steps
- Add `cloud_blueprint_secret_binding_update` to `cloud-manager-mcp`, wrapping the **existing**
  `PATCH /api/v1/blueprint/{blueprintId}/secret-binding/{bsbId}` (no API change — verified present in
  Task 0000).
- Inputs:
  - `id` — blueprint public id (the `{blueprintId}` path segment), mirroring how
    `cloud_blueprint_secret_binding_attach` names the blueprint arg
  - `bsbId` — the attachment row id (`bsb_…`)
  - `credName` — optional
  - `position` — optional
- **Send only the provided fields** in the PATCH body (omit `credName`/`position` when not given) —
  mirror how `cloud_blueprint_playbook_reorder` / `cloud_vm_update` build a sparse PATCH body.
- Return the API's updated attachment response verbatim (camelCase, public_ids only).
- Register alongside the other `cloud_blueprint_secret_binding_*` tools; keep wording consistent.

## Build
`python3 .cicd/build-cloud-manager.py --target mcp` must pass cleanly.

## Definition of done
`cloud_blueprint_secret_binding_update` updates an attachment's `credName` and/or `position` against
the live API, sends only provided fields, returns the updated row, and the mcp builds. Test 0003
passes.
