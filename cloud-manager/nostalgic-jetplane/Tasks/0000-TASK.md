# Task 0000 — Initialize Workspace

## Goal
Verify the workspace is clean, ensure the feature branch is live across all repos, and confirm the
#2/#1.5 surface this workflow builds on is present.

## Steps
1. Run `hv_status` on the deck (see `../deck.md` for the exact call)
2. If all repos are clean and on the default branch → run `hv_init`
3. If all repos are clean and on a feature branch with all PRs merged → run `hv_next`
   - (The warmup already minted `rakish-coldfront` via `hv_next`; if every repo already shows it,
     no transition is needed.)
4. If any repo is dirty → STOP. Investigate before proceeding.
   - Exception: dirty repo with a merged PR → hv_stash → hv_next → hv_unstash
5. Run `hv_status` again to confirm all repos are on the same feature branch, all clean
6. Record the generated branch name in `../deck.md` (Branch field — `rakish-coldfront`)
7. **Verify the dependency surface is present** before any code work:
   - `vm.vm_secret_bindings` table exists (use the database-toolkit `db_list_tables` on `clouddb`,
     schema `vm`, or psql). Source of the usage rows this workflow reads (#2).
   - The existing `PATCH /api/v1/blueprint/{blueprintId}/secret-binding/{bsbId}` endpoint exists on
     `SecretBindingController` / `BlueprintController` (grep the api source). The update tool wraps it.
   - If either is missing → STOP and surface that #2/#1.5 has not landed.

## Definition of done
All repos on the new feature branch, all clean; branch name recorded in deck.md;
`vm.vm_secret_bindings` table and the blueprint↔binding PATCH endpoint confirmed present.
