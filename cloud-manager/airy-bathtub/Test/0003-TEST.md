# Test 0003 — E2E + build + deploy

## TC-001 — vorch redeployed; command recorded
Pass: vorch was built and manually redeployed on the hypervisor, and the exact command is
recorded in `Results/RESULT.md`.
Fail: not redeployed, or command not recorded.

## TC-002 — Plain VM is SSH-only
Provision a plain VM. From the off-subnet Mac:
Pass: `22` reachable (SSH connects); all other probed inbound ports blocked.
Fail: any non-22 inbound port reachable from off-subnet.

## TC-003 — Postgres VM port rules
Provision a Postgres VM.
Pass: SSH ok; `5432` reachable from an on-subnet peer VM; `5432` blocked from the off-subnet
Mac.
Fail: `5432` reachable from off-subnet, or unreachable from an on-subnet peer.

## TC-004 — DB playbook matches seed
Pass: the Postgres VM's applied playbook content matches the seed YAML (no DB drift).
Fail: drift.

## TC-005 — No existing VM modified
Pass: no already-running VM was changed (verified via `cloud_vm_events` / state of
pre-existing VMs).
Fail: any pre-existing VM shows firewall/config changes from this run.

## TC-006 — Throwaway VMs cleaned up
Pass: the plain + Postgres E2E test VMs were destroyed.
Fail: leftover test VMs remain.

## TC-007 — Outputs written before integrate
Pass: `Results/RESULT.md` and `Retro/LESSONS.md` exist and are filled in before `hv_integrate`
was called.
Fail: missing or empty outputs, or written after integrate.

## TC-008 — Integrated
Pass: `hv_integrate` completed; PRs for `vorch-lib`, `cloud-manager-api`, and `workflow-exec`
are surfaced (or confirmed via `gh pr list` / `hv_list_pulls`).
Fail: integrate did not run or PRs cannot be confirmed.
