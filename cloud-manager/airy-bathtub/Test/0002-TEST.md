# Test 0002 — Seed playbooks: per-service subnet rules

## TC-001 — Postgres seeds add the ufw 5432 task
Inspect `seed/playbooks/pg-14-jammy.yaml` and `pg-16-noble.yaml`.
Pass: each contains a `community.general.ufw` task with `rule: allow`, `to_port: "5432"`,
`proto: tcp`, `from_ip: "{{ item }}"`, looping `pg_hba_sources`.
Fail: task missing, wrong port, or not looped over the trusted-sources var.

## TC-002 — Postgres still subnet-listening
Pass: `listen_addresses='*'` and `pg_hba_sources` are intact (NOT reverted to localhost-only).
Fail: postgres was reverted to localhost-only.

## TC-003 — RabbitMQ seeds tightened
Inspect `rabbitmq-jammy.yaml` and `rabbitmq-noble.yaml`.
Pass: the existing ufw task now includes `from_ip: "{{ item }}"` looped over the trusted
sources, covering both `5672` and `15672`. No rule opens these ports to anywhere anymore.
Fail: any rule still opens 5672 or 15672 with no `from_ip` (open to anywhere).

## TC-004 — Trusted sources value correct
Pass: the looped var resolves to `10.0.150.0/24` + `10.0.144.140/32` (same values as
`pg_hba_sources`). No third copy with divergent values.
Fail: different or hardcoded values.

## TC-005 — import-playbooks.py sync
Run `python3 cloud-manager-mcp/.cicd/import-playbooks.py` against the live API.
Pass: the pg + rabbit playbooks report **PATCH** (updated) or no-op-if-already-synced; no
errors; created-from-scratch is NOT expected (they exist).
Fail: errors, or unexpected create.

## TC-006 — DB matches seed YAML
Use `cloud_playbook_get` (by name) for each edited playbook and diff against the seed YAML.
Pass: DB content matches the seed YAML for all four playbooks.
Fail: any drift between DB and seed.
