# Task 0010 — Write Results + LESSONS; hv_ship

## Goal
Finalize `Results/RESULT.md` and `Retro/LESSONS.md` BEFORE `hv_ship`, then ship the deck.

## Target repo(s)
`vorch-service`, `cloud-manager-api`, `workflow-exec`

## Steps
1. Fill out `Results/RESULT.md` with:
   - Outcome (SHIPPED / PARTIAL / FAILED).
   - Branch name (`jazzy-herring`).
   - PR table (left as "TBD" — `hv_ship` fills these in via its output).
   - One paragraph per phase summarizing what shipped, linking `Retro/CHANGE-NNNN.md` if written.
   - Constraints honored (carry the funky-vole-style checklist).
2. Fill out `Retro/LESSONS.md`:
   - What surprised us during execution (module rename pain, healthprobe endpoint state, assignment-tracking decision, anything from FIX-NNN files).
   - Deviations from PLAN.md.
   - Follow-ups (link as `[[follow-up-name]]`).
3. Call `hv_ship`:
   ```
   hv_ship deck: "cloud-manager"
           message: "porch: carve ansible orchestrator out of vorch-service; retire Python worker"
           title:   "porch: carve ansible orchestrator out of vorch-service; retire Python worker"
   ```
4. Record the returned PR URLs in `Results/RESULT.md`.

## Definition of done
`hv_ship` completes successfully. Two PRs opened (`vorch-service`, `cloud-manager-api`) plus this deck (`workflow-exec`). Both PR titles + URLs recorded in `Results/RESULT.md`.
