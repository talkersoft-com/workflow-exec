# Task 0000 — Initialize Workspace

## Goal
Verify the deck is on the `jazzy-herring` execution branch (it was advanced there by the planning ship of charmed-panda) and confirm all repos clean. Read the source PLAN.md so subsequent phases have full context.

## Target repo(s)
(no code changes — workspace verification only)

## Steps
1. Run `hv_status` on the `cloud-manager` deck.
2. If every repo is already on `jazzy-herring` and clean → done.
3. If on the default branch or a stale branch → run `hv_next` (NOT `hv_init`; planning already shipped).
4. If any repo is dirty → STOP. Investigate.
5. Read `../../../../workflow-plans/cloud-manager/charmed-panda/PLAN.md` end to end.
6. Confirm branch name `jazzy-herring` matches `../deck.md`.

## Definition of done
All 15 repos on `jazzy-herring`, all clean. PLAN.md read.
