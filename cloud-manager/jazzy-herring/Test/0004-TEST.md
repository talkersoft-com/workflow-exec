# Test 0004 — Vorch loses ansible

## TC-001 — No ansible-related calls in cmd/vorch-service/main.go
```bash
grep -iE 'playbook|healthprobe|collection-(install|remove)|ansible' vorch-service/cmd/vorch-service/main.go
```
Pass: zero output.
Fail: any line printed.

## TC-002 — Vorch import graph clean
```bash
cd vorch-service && go list -deps ./cmd/vorch-service | grep -E 'internal/ansible|internal/ansiblesub'
```
Pass: zero output.

## TC-003 — Porch import graph clean
```bash
cd vorch-service && go list -deps ./cmd/porch | grep -E 'internal/vm($|sub)'
```
Pass: zero output.

## TC-004 — Both binaries still build
```bash
cd vorch-service && go build ./cmd/vorch-service ./cmd/porch
```
Pass: exit 0.
