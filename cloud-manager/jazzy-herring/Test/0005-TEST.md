# Test 0005 — Assignment tracking + log redaction parity

## TC-001 — Assignment tracking decision recorded
```bash
test -f Retro/CHANGE-0005.md && cat Retro/CHANGE-0005.md
```
Pass: file exists; either says "n/a — already implemented" (with reference to where vorch already does the UPDATE) OR shows the new UPDATE was added with a file:line reference.

## TC-002 — If UPDATE was added, it's in a terminal-status code path
Only run if CHANGE-0005.md said the UPDATE was added.
```bash
grep -rnE 'vm_playbook_assignments|last_run_id' vorch-service/internal/ansible/ | head -10
```
Pass: at least one match in a file handling terminal status (Succeeded / Failed / Cancelled).

## TC-003 — Log redaction wired in porch's main
```bash
grep -E 'hvs|REDACTED|SetOutput' vorch-service/cmd/porch/main.go
```
Pass: at least one match showing the scrubber is wired (regex declaration, custom Writer, or `log.SetOutput(...)` call).
Fail: zero matches.

## TC-004 — No direct token logging
```bash
grep -rnE 'log\.(Printf|Println|Print).*[Vv]aultRunToken|log\.(Printf|Println|Print).*child_token' vorch-service/internal/ansible/
```
Pass: zero matches.

## TC-005 — Build clean
```bash
cd vorch-service && go build ./...
```
Pass: exit 0.
