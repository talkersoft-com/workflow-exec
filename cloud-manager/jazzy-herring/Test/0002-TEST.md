# Test 0002 — Package carve-out

## TC-001 — Old directories gone
```bash
cd vorch-service && ls handlers messagesub messagepub healthprobe models vault 2>&1
```
Pass: every one of those listings prints "No such file or directory".
Fail: any of those directories still exist (files weren't moved).

## TC-002 — New directories present
```bash
cd vorch-service && ls -d cmd/vorch-service cmd/porch internal/{vm,ansible,vmsub,ansiblesub,amqp,pub,models,vault}
```
Pass: all 10 paths exist (`ls -d` does not error).
Fail: any path missing.

## TC-003 — cmd/porch stub exists
```bash
cd vorch-service && test -f cmd/porch/main.go && head -2 cmd/porch/main.go
```
Pass: file exists and begins with `package main`.

## TC-004 — Build clean
```bash
cd vorch-service && go build ./...
```
Pass: exit 0.

## TC-005 — Vet clean
```bash
cd vorch-service && go vet ./...
```
Pass: exit 0.

## TC-006 — Assignment-tracking verification recorded
```bash
test -f Retro/CHANGE-0002.md && grep -iE 'assignment|last_run|last_applied' Retro/CHANGE-0002.md | head -3
```
Pass: file exists, contains verification finding (yes or no).
Fail: file missing or no mention.
