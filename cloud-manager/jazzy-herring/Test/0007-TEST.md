# Test 0007 — Cutover

## TC-001 — Exactly one consumer on playbook-runs
```bash
sudo rabbitmqctl list_consumers | grep playbook-runs | wc -l
```
Pass: output is exactly `1`.

## TC-002 — Consumer is porch
```bash
sudo rabbitmqctl list_consumers | grep playbook-runs
```
Pass: the consumer-tag contains `porch` (e.g. `ctag-/kvm-automator/porch-1`).

## TC-003 — Porch + vorch_service both running
```bash
systemctl is-active porch && systemctl is-active vorch_service
```
Pass: both print `active`.

## TC-004 — Python worker stopped (not yet removed)
```bash
systemctl is-active cloud-manager-worker
```
Pass: prints `inactive` or `failed` (anything but `active`).

## TC-005 — No exception traces in recent journal
```bash
sudo journalctl -u porch -u vorch_service --since "30 seconds ago" | grep -iE 'error|exception|panic|fatal' | head -5
```
Pass: zero matches (or only known-benign warnings).
