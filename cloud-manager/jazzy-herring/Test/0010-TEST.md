# Test 0010 — Ship

## TC-001 — Results/RESULT.md filled out
```bash
test -f Results/RESULT.md && ! grep -E '^TBD$|^TBD —' Results/RESULT.md | grep -v 'PR.*TBD'
```
Pass: file exists, no remaining `TBD` placeholders except in the PR table.

## TC-002 — Retro/LESSONS.md filled out
```bash
test -f Retro/LESSONS.md && ! grep -E '^TBD$' Retro/LESSONS.md
```
Pass: file exists, no `TBD` lines.

## TC-003 — hv_ship succeeds
Pass: PRs returned for at least `vorch-service`, `cloud-manager-api`, `workflow-exec`. Each PR URL captured in `Results/RESULT.md`.
