# Test 0003 — Porch wires consumers + healthprobe

## TC-001 — cmd/porch/main.go registers all four consumers
```bash
grep -E 'playbook-runs|playbook-run-cancel|collection-installs|collection-removes|healthprobe' vorch-service/cmd/porch/main.go
```
Pass: all four queue names appear, plus a healthprobe reference (function call or import).
Fail: any missing.

## TC-002 — Both binaries build
```bash
cd vorch-service && go build ./cmd/porch ./cmd/vorch-service
```
Pass: exit 0.

## TC-003 — Healthprobe endpoint verification recorded
```bash
test -f Retro/CHANGE-0003.md && grep -iE 'feature-flag|healthprobe|patch' Retro/CHANGE-0003.md | head -3
```
Pass: file exists, mentions endpoint check outcome.

## TC-004 — If API was edited, API builds
Skip if the verification in TC-003 said the endpoint already existed. Otherwise:
```bash
cd cloud-manager-api && dotnet build src/CloudManager.API/CloudManager.API.csproj -c Release -nologo -v q 2>&1 | tail -5
```
Pass: build succeeds, 0 errors.
