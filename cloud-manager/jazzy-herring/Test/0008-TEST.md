# Test 0008 — Python worker fully retired

## TC-001 — Systemd unit removed
```bash
systemctl status cloud-manager-worker 2>&1 | head -2
```
Pass: prints "Unit cloud-manager-worker.service could not be found."

## TC-002 — Install directory removed
```bash
ls /opt/cloud-manager-worker 2>&1
```
Pass: "No such file or directory".

## TC-003 — Env file removed
```bash
sudo ls /etc/cloud-manager/worker.env 2>&1
```
Pass: "No such file or directory".

## TC-004 — pipx venv removed
```bash
sudo -u cloudmanager pipx list 2>&1 | grep cloud-manager-worker
```
Pass: zero output.

## TC-005 — Venv directory removed
```bash
ls /home/cloudmanager/.local/pipx/venvs/cloud-manager-worker 2>&1
```
Pass: "No such file or directory".

## TC-006 — Still exactly one consumer on playbook-runs (porch survived the removal)
```bash
sudo rabbitmqctl list_consumers | grep playbook-runs | wc -l
```
Pass: `1`.
