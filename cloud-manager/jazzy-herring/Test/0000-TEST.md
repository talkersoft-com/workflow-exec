# Test 0000 — Workspace Initialized

## TC-001 — All repos on jazzy-herring
Run `hv_status` on the `cloud-manager` deck.
Pass: every repo shows `[jazzy-herring]`.
Fail: any repo on a different branch or on the default branch.

## TC-002 — All repos clean
Pass: `hv_status` reports no uncommitted changes, no unpushed commits, no stash entries.
Fail: any repo dirty.

## TC-003 — PLAN.md read
Pass: implementer can describe the porch / vorch carve-out in their own words. (Self-attested — proceed if confident.)
