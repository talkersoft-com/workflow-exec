# Test 0001 — Module rename

## TC-001 — go.mod has new module path
```bash
head -1 vorch-service/go.mod
```
Pass: shows `module github.com/talkersoft-com/vorch-service` (or whichever path was confirmed by operator).
Fail: still shows `module main`, or shows a different path than the one in Task 0001.

## TC-002 — Zero `"main/..."` imports remain
```bash
grep -rn '"main/' vorch-service --include='*.go'
```
Pass: zero output.
Fail: any line printed.

## TC-003 — Build clean
```bash
cd vorch-service && go build ./...
```
Pass: exit code 0, no output.
Fail: any compile error.

## TC-004 — Vet clean
```bash
cd vorch-service && go vet ./...
```
Pass: exit code 0, no output.
Fail: any vet warning.
