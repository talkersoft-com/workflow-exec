# Test 0004 — MCP tools

## TC-001 — Build + registration
`python3 .cicd/build-cloud-manager.py --target mcp` green; spawn a FRESH server from dist over
stdio (do not rely on the session's running MCP — it is stale until operator /mcp reload) and
`tools/list`.
Pass: all 12 marketplace tools present alongside the existing surface.

## TC-002 — Round-trips against the live API (flag enabled)
Via the fresh stdio server: `cloud_marketplace_list` shows the seeds;
`cloud_blueprint_create` → `cloud_blueprint_playbook_attach` → `cloud_blueprint_publish` →
appears in `cloud_marketplace_list`; `cloud_blueprint_archive` → disappears.
Pass: every call succeeds with camelCase/public-id shapes; no internal GUIDs anywhere in output.

## TC-003 — Lockfile stable
`git status --porcelain package-lock.json` in cloud-manager-mcp.
Pass: unchanged.
