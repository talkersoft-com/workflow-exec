# Test 0005 — E2E + ship

## TC-001 — jammy (image-only) end-to-end
Marketplace-provisioned jammy VM reaches OrchestrationStatus=Completed with an IP; zero
assignments; events show `instantiated` and `blueprint_run_chain_completed`.

## TC-002 — postgres-jammy ordered chain end-to-end
Marketplace-provisioned postgres-jammy VM: provision completes → run auto-enqueued (no manual
apply) → run Succeeded → events show chain_step(position 0) then chain_completed. Postgres
actually answers on the VM (the playbook's own success criteria / a connection probe).

## TC-003 — Direct-flow regression
Non-blueprint VM + manual attach/apply works identically to before; with `marketplace` flag
disabled, blueprint/marketplace endpoints are gated while everything pre-existing still works.

## TC-004 — Deploy verified
API smoke (poll endpoint) passed post-restart; live DB migration applied BEFORE api deploy; MCP
dist rebuilt with stable lockfile; RESULT.md carries the operator /mcp reload instruction.

## TC-005 — Shipped
hv_ship opened PRs for exactly cloud-manager-api, cloud-manager-mcp, workflow-exec (and nothing
else — explicitly NOT cloud-manager-web/vorch-lib/vorch-service); RESULT.md and LESSONS.md were
committed in the ship (written before it).
