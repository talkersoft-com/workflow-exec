# Test 0004 — End-to-end, deploy, ship

## TC-001 — End-to-end create
Create a manual secret via the live API/UI.
Pass: `vault_path = cloudmanager/data/manual/<slug>`, DB `status=Active`, value present in Vault.
Fail: any of these wrong.

## TC-002 — Path-lock enforced both layers
Attempt an out-of-namespace path via SQL (CHECK) and via API (validation).
Pass: both reject. Fail: either accepts.

## TC-003 — Gate L2 (Secret←Binding)
Reference secret from a binding → delete secret → 409; remove binding → delete succeeds, Vault key gone.
Pass: gate holds then releases. Fail: deletes while referenced, or Vault key survives leaf delete.

## TC-004 — Gate L1 (Binding←VM)
Insert a `vm_secret_bindings` row → attempt binding delete → RESTRICT/409; remove row → delete succeeds.
Pass: FK gate holds then releases. Fail: binding deletes while used.

## TC-005 — Build all
`python3 .cicd/build-cloud-manager.py --target all`.
Pass: clean. Fail: any error.

## TC-006 — Migration applied + deploy healthy
Migration applied before api restart; `deploy --target all` completes.
Pass: `cloud_health_check` green; web reachable on :3000.
Fail: schema mismatch on api start, or smoke poll fails.

## TC-007 — Outputs + PRs
`Results/RESULT.md` + `Retro/LESSONS.md` exist before integrate; `hv_integrate` opened PRs.
RESULT includes the `/mcp` reload operator action.
Pass: files present, PR links reported. Fail: missing outputs or no PRs.
