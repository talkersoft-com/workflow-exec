# Test 0002 — MCP: cloud_secret_* tools

## TC-001 — Tools registered
List the MCP tool surface.
Pass: `cloud_secret_list`, `cloud_secret_get`, `cloud_secret_create`, `cloud_secret_delete` present.
Fail: any missing.

## TC-002 — Create + list + get round-trip (live API)
`cloud_secret_create` a manual secret → `cloud_secret_list` shows it → `cloud_secret_get` returns it.
Pass: public_id + vaultPath + status returned; **no secret value echoed** anywhere in tool output.
Fail: value leaked, Guid leaked, or round-trip mismatch.

## TC-003 — Delete-guard surfaced
Reference the created secret from a binding, then `cloud_secret_delete`.
Pass: the **409** guard message is surfaced verbatim (not a generic failure).
Fail: generic error, or deletion proceeds.

## TC-004 — Build
`python3 .cicd/build-cloud-manager.py --target mcp`.
Pass: npm ci lockfile-stable + build clean. Fail: lockfile drift or build error.
