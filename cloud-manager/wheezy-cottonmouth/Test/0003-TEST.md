# Test 0003 — Web: Secret Manager page + binding→managed-secret

## TC-001 — Page lists secrets
Open the Secret Manager page.
Pass: secrets render with name, vault_path, and a status badge; gated behind `marketplace` flag.
Fail: page errors, or not gated.

## TC-002 — Create form is prefix-locked
Open the create form.
Pass: `cloudmanager/data/manual/` shows as a fixed, non-editable label; only a slug + value (+
name/description) are editable; the composed path live-previews. There is **no free-form path field**.
Fail: a free-form path input exists, or the prefix is editable.

## TC-003 — Create round-trip
Create a secret (slug `web-demo`, a value).
Pass: it appears in the list with `vaultPath = cloudmanager/data/manual/web-demo` and a status badge.
Fail: not created or path wrong.

## TC-004 — Delete guard surfaced
Reference a secret from a binding, then delete it from the page.
Pass: a clear "can't delete — referenced by binding" message (the 409), not a generic error.
Fail: generic error or silent failure.

## TC-005 — Binding can reference a managed Secret; templated still works
In the Secret Bindings modal: reference an existing managed Secret (static) → saved binding carries
`secret_id`. Separately, author a templated `{vm_public_id}` binding.
Pass: both save; static shows a derived/read-only path; templated authoring is unchanged.
Fail: either path broken.

## TC-006 — Visual alignment + build
`python3 .cicd/build-cloud-manager.py --target web` clean. After deploy, **visually verify**
(screenshot) the create form and binding modal: controls in a row are vertically aligned (38px).
Pass: clean build + verified aligned. Fail: build error or visible misalignment.
