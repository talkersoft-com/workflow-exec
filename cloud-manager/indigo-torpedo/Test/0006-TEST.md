# Test 0006 — Shipped

## TC-001 — Results file exists
Run: `test -f /home/todd/workspace/cloud-manager/planning/execution-workflow/cloud-manager/indigo-torpedo/Results/RESULT.md && echo OK`
Pass: `OK`.
Fail: no output.

## TC-002 — Lessons file exists
Run: `test -f /home/todd/workspace/cloud-manager/planning/execution-workflow/cloud-manager/indigo-torpedo/Retro/LESSONS.md && echo OK`
Pass: `OK`.
Fail: no output.

## TC-003 — planning PR merged
Pass: hv_ship output shows `planning` PR merged.
Fail: PR not created, not merged, or `planning` repo skipped.

## TC-004 — execution-workflow PR merged (Results + Lessons)
Tasks/0006 writes `Results/RESULT.md` and `Retro/LESSONS.md` into this
workflow folder, which lives in the `execution-workflow` repo. That makes
the repo dirty at ship time, so `hv_ship` creates a second PR for it.
Pass: hv_ship output shows `execution-workflow` PR merged with both files
visible in the diff.
Fail: PR not created, not merged, or only one of the two files appears.

## TC-005 — All other deck repos skipped
Pass: hv_ship output shows every deck repo other than `planning` and
`execution-workflow` as skipped (clean / nothing ahead).
Fail: any unrelated repo had changes pushed.
