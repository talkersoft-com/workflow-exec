# Test 0003 — WEB-PLAN.md

## TC-001 — File exists
Run: `test -f /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/WEB-PLAN.md && echo OK`
Pass: `OK`.
Fail: no output.

## TC-002 — Code editor decision made
Run: `grep -iE 'monaco|codemirror' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/WEB-PLAN.md`
Pass: at least one hit AND the surrounding text contains a clear recommendation with rationale (manual confirmation).
Fail: no hit.

## TC-003 — Clean separation architectural decision stated
Run: `grep -iE 'VmPlaybooks|VmDetailPage' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/WEB-PLAN.md`
Pass: at least one hit AND the surrounding text indicates deletion/removal (manual confirmation — must say "delete" or "remove", not "keep" or "no change").
Fail: no mention of the deleted components.

## TC-004 — All ansible routes are under /ansible prefix
Run: `grep -E '/ansible' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/WEB-PLAN.md`
Pass: at least 5 hits (one per major sub-page).
Fail: fewer than 5.

## TC-005 — Run trigger page specified
Run: `grep -iE 'AnsibleRunPage|run.*page|trigger.*run|run.*trigger' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/WEB-PLAN.md`
Pass: at least one hit.
Fail: no output.

## TC-006 — History page with per-playbook and per-VM filtering specified
Run: `grep -iE 'history|by.*playbook|by.*vm' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/WEB-PLAN.md`
Pass: at least 2 hits covering both filter modes (manual confirmation).
Fail: fewer than 2 hits.

## TC-007 — Live run output / polling specified
Run: `grep -iE 'poll|live|stream' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/WEB-PLAN.md`
Pass: at least one hit.
Fail: no output.

## TC-008 — FeatureGate gating mentioned
Run: `grep -i 'FeatureGate' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/WEB-PLAN.md`
Pass: at least one hit.
Fail: no output.

## TC-009 — Phases section present
Run: `grep -E '^## Phases' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/WEB-PLAN.md`
Pass: at least one hit.
Fail: no output.

## TC-010 — No git-backed fields reintroduced
Run: `grep -E 'git_url|git_ref|playbook_path|last_refreshed_at' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/WEB-PLAN.md`
Pass: no output.
Fail: any hit.

## TC-011 — SettingsPage with FeatureFlagToggle specified
Run: `grep -iE 'SettingsPage|FeatureFlagToggle' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/WEB-PLAN.md`
Pass: both `SettingsPage` and `FeatureFlagToggle` appear.
Fail: either is missing.

## TC-012 — AttachedVmsPanel (reverse-direction view from playbook to VMs) specified
Run: `grep -iE 'AttachedVmsPanel|attached.*vms' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/WEB-PLAN.md`
Pass: at least one hit.
Fail: no output.

## TC-013 — RunCancelButton specified
Run: `grep -iE 'RunCancelButton|cancel.*button|cancel.*run' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/WEB-PLAN.md`
Pass: at least one hit.
Fail: no output.
