# Test 0004 — VORCH-PLAN.md

## TC-001 — File exists
Run: `test -f /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/VORCH-PLAN.md && echo OK`
Pass: `OK`.
Fail: no output.

## TC-002 — Python-vs-Go decision explicit
Run: `grep -iE 'python.*go|go.*python|subprocess' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/VORCH-PLAN.md`
Pass: at least one hit with a clear recommendation (manual confirmation).
Fail: no output.

## TC-003 — cloud-manager-worker disposition stated
Run: `grep -i 'cloud-manager-worker' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/VORCH-PLAN.md`
Pass: at least one hit.
Fail: no output.

## TC-004 — Health probe specified
Run: `grep -iE 'health.probe|healthy' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/VORCH-PLAN.md`
Pass: at least one hit.
Fail: no output.

## TC-005 — Vault scoped token mentioned
Run: `grep -iE 'child token|scoped.*token|vault.*token' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/VORCH-PLAN.md`
Pass: at least one hit.
Fail: no output.

## TC-006 — vorch-lib shared types section present
Run: `grep -i 'vorch-lib' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/VORCH-PLAN.md`
Pass: at least one hit.
Fail: no output.

## TC-007 — Phases section present
Run: `grep -E '^## Phases|^### Phases' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/VORCH-PLAN.md`
Pass: at least one hit.
Fail: no output.

## TC-008 — No git-backed fields reintroduced
Run: `grep -E 'git_url|git_ref|playbook_path|last_refreshed_at' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/VORCH-PLAN.md`
Pass: no output.
Fail: any hit.

## TC-009 — Two-token Vault strategy explicit
Run: `grep -iE 'two token|two distinct token|worker.s own token|child token' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/VORCH-PLAN.md | wc -l`
Pass: output ≥ 2.
Fail: less than 2 (means the worker-token vs child-token distinction isn't
clearly made).

## TC-010 — Worker Vault auth method picked
Run: `grep -iE 'AppRole|service token|VAULT_TOKEN.*env' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/VORCH-PLAN.md`
Pass: at least one hit AND the surrounding text recommends a specific method
for v1 (not just listing options).
Fail: no output.

## TC-011 — Cancel handling specified
Run: `grep -iE 'cancel|context.*cancel|playbook-run-cancel' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/VORCH-PLAN.md | wc -l`
Pass: output ≥ 2.
Fail: less than 2.

## TC-012 — secret_inputs resolution attributed to worker
Run: `grep -iE 'secret_inputs|resolved_secret_inputs|x-secret-path' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/VORCH-PLAN.md`
Pass: at least one hit, and the plan states the worker (not the API) resolves
these.
Fail: no output.
