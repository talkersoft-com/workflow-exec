# Test 0002 — API-PLAN.md

## TC-001 — File exists
Run: `test -f /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/API-PLAN.md && echo OK`
Pass: `OK`.
Fail: no output.

## TC-002 — Every uncovered entity is named in the plan
Run:
```
for ent in AnsibleRole RoleFile RoleFileRevision PlaybookRevision PlaybookGlobalRoleRef PlaybookRole PlaybookRoleFile PlaybookRoleFileRevision PlaybookRunTarget; do
  grep -q "$ent" /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/API-PLAN.md || echo "MISSING: $ent"
done
```
Pass: no output.
Fail: any `MISSING:` line.

## TC-003 — Materialization endpoint specified
Run: `grep -E '/materialized|materialization' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/API-PLAN.md`
Pass: at least one hit.
Fail: no output.

## TC-004 — Feature flag gating mentioned
Run: `grep -c 'RequireFeatureFlag' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/API-PLAN.md`
Pass: ≥ 1.
Fail: 0.

## TC-005 — ArgumentSpecsTranslator hook documented
Run: `grep -i 'ArgumentSpecsTranslator' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/API-PLAN.md`
Pass: at least one hit.
Fail: no output.

## TC-006 — Phases section present
Run: `grep -E '^## Phases' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/API-PLAN.md`
Pass: at least one hit.
Fail: no output.

## TC-007 — No git-backed fields reintroduced
Run: `grep -E 'git_url|git_ref|playbook_path|last_refreshed_at' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/API-PLAN.md`
Pass: no output.
Fail: any hit.

## TC-008 — Multi-VM run-trigger endpoint specified
Run: `grep -iE 'vmIds|multi-vm|/playbook/.*/run' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/API-PLAN.md`
Pass: at least one hit AND the surrounding text describes a POST endpoint
taking a list of VM IDs.
Fail: no output.

## TC-009 — Run-cancel endpoint specified
Run: `grep -iE '/run/.*/cancel|cancel.*endpoint|POST.*cancel' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/API-PLAN.md`
Pass: at least one hit.
Fail: no output.

## TC-010 — Materialization includes secret_inputs manifest
Run: `grep -E 'secret_inputs|x-secret-path' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/API-PLAN.md`
Pass: at least one hit.
Fail: no output.
