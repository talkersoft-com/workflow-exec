# Test 0005 — DATA-MODEL-DELTAS.md

## TC-001 — File exists
Run: `test -f /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/DATA-MODEL-DELTAS.md && echo OK`
Pass: `OK`.
Fail: no output.

## TC-002 — Either has a migration list OR explicitly says no changes needed
Run:
```
grep -iE 'no database changes required|no changes required|migration|column|index' \
  /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/DATA-MODEL-DELTAS.md
```
Pass: at least one hit.
Fail: no output.

## TC-003 — Per-plan audit covered
Run:
```
for plan in API-PLAN WEB-PLAN VORCH-PLAN; do
  grep -q "$plan" /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/DATA-MODEL-DELTAS.md || echo "MISSING: $plan"
done
```
Pass: no output.
Fail: any `MISSING:` line.

## TC-004 — No git-backed fields reintroduced
Run: `grep -E 'git_url|git_ref|playbook_path|last_refreshed_at' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/DATA-MODEL-DELTAS.md`
Pass: no output.
Fail: any hit.
