# Test 0001 — Bundle folder + README

## TC-001 — Folder exists
Run: `test -d /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible && echo OK`
Pass: prints `OK`.
Fail: no output.

## TC-002 — README exists with required sections
Run: `grep -E '^## (Problem statement|Bundle contents|What.s already shipped|Relationship to existing plans|Implementation order)' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/README.md | wc -l`
Pass: output is `5`.
Fail: any other number.

## TC-003 — README links to all four sibling plans
Run: `grep -cE 'API-PLAN\.md|WEB-PLAN\.md|VORCH-PLAN\.md|DATA-MODEL-DELTAS\.md' /home/todd/workspace/cloud-manager/planning/planning/cloud-manager-ansible/README.md`
Pass: output is `4` or higher.
Fail: less than `4`.
