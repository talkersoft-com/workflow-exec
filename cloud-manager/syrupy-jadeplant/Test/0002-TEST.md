# Test 0002 — Service playbooks per-port subnet allow
TC-001 Postgres playbooks (`pb_PG14JAMMY01`, `pb_EMEJCH08GG`) each carry a `community.general.ufw`
allow task for `to_port: 5432`, looped over the trusted sources. TC-002 RabbitMQ playbook(s) carry a
ufw allow for `5672` (and `15672` only if the operator chose mgmt-on-subnet); the chosen port set is
recorded. TC-003 sources are `10.0.150.0/24` + `10.0.144.140/32` and mirror `pg_hba_sources` with a
single source of truth per playbook (no divergent duplicate list). TC-004 `community.general` is a
declared collection requirement on each edited playbook and present on the target host. TC-005
`cloud_playbook_lint` / `cloud_playbook_check` green on every edited playbook. TC-006 deck.md records
whether the playbooks are DB-only or repo-seeded (PLAN open question resolved).
