# Test 0003 — E2E + build vorch + manual redeploy + ship
TC-001 vorch-lib `go build ./...` + `go test ./provision/...` and vorch-service `build-app.sh` green.
TC-002 vorch manually redeployed on the hypervisor; exact commands recorded in RESULT.md (deploy-app.sh
cannot ssh-to-self — binary installed locally). TC-003 plain VM: after provision, SSH (22) reachable
from the Mac, all other inbound ports blocked; on-VM `ufw status` shows `22/tcp ALLOW Anywhere` +
default deny incoming. TC-004 Postgres VM: `5432` reachable from an on-subnet peer (`10.0.150.0/24`)
AND blocked from the off-subnet Mac; SSH still reachable. TC-005 no lockout — provisioning SSH session
and post-boot porch/ansible runs never dropped; VM reached a healthy state. TC-006 playbook ufw rules
applied to the running VM(s) and confirmed via `ufw status`. TC-007 `Results/RESULT.md` and
`Retro/LESSONS.md` exist and were written BEFORE hv_integrate. TC-008 hv_integrate opened PRs and
reported their links.
