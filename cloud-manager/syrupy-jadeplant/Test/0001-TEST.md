# Test 0001 — vorch-lib cloud-init ufw baseline
TC-001 `go build ./...` and `go test ./provision/...` green. TC-002 generated user-data contains the
four lines `ufw allow 22/tcp`, `ufw --force default deny incoming`, `ufw --force default allow
outgoing`, `ufw --force enable` in that order. TC-003 lockout-safe: `ufw allow 22/tcp` appears
BEFORE `ufw --force enable`, and the whole block appears AFTER `netplan apply`. TC-004 byte-stable:
the empty-secrets golden (`TestBuildUserData_EmptyByteIdentical`) differs from the prior golden ONLY
by the ufw block — no `cm-seed` write_files, no other runcmd churn. TC-005 no new create-vm field /
no `buildUserData` signature change for the baseline; vorch-service untouched. TC-006 secret-bearing
path still emits the seal runcmd line (injection logic unchanged), no secret value in runcmd args.
