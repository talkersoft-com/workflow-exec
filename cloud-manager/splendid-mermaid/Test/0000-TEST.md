# Test 0000 — Workspace initialized
TC-001 every repo on the same feature branch, none on default. TC-002 all clean (scaffold runtime
edits in workflow-exec excepted). TC-003 deck.md Branch recorded and matches hv_status (not TBD).
