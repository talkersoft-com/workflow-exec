# Test 0003 — MCP passthrough
TC-001 mcp build green. TC-002 `cloud_marketplace_provision` schema exposes optional
`secretBindingParams` with a description; omitting it preserves current behavior. TC-003 when
supplied, the param round-trips to the Instantiate endpoint body verbatim (camelCase). TC-004 no
other MCP tool surface changed.
