# Test 0002 — API P1: instantiate-time parameter resolution
TC-001 api build green. TC-002 `InstantiateRequest` accepts optional `secretBindingParams`
(array of `{ secretBindingId, params }`), camelCase, public_ids only. TC-003 a templated
`{vm_public_id}` binding with a supplied source `vm_public_id` resolves to THAT VM's Vault path
(not the new VM's). TC-004 required param missing → 422 naming the binding; resolver NEVER falls
back to the new VM's id. TC-005 secret value never echoed in the response. TC-006 supplied param
resolves end-to-end: secret read + injected + sealed; `vm_secret_bindings` row recorded.
