# Test 0001 — API P0: resolve-before-create + clean failure
TC-001 api build green. TC-002 code review: in `MarketplaceController.Instantiate`, secret-binding
resolution now precedes the VM insert + `blueprint_instantiated` event. TC-003 missing-secret
resolution returns 422 with the binding name + Vault path and NO secret value in the body. TC-004
the 422 path creates no VM row (verify via db/psql: no new `vm.virtual_machines` row for the
attempt). TC-005 any post-create failure records a VM failure event — no eventless `orch=0` orphan.
TC-006 regression: static + concrete-path templated + no-binding instantiate paths unchanged.
