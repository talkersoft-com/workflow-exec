# Test 0004 — E2E + build + deploy + ship
TC-001 `--target all` build green (api + mcp). TC-002 api deployed; localhost smoke poll returns
2xx (record attempt count + HTTP code). TC-003 happy path: consumer blueprint provisioned with
`pg-test123` as the `vm_public_id` source → secret injected + sealed; `vm_secret_bindings` row AND
create-vm command both present; VM not orphaned. TC-004 clean 422: templated `{vm_public_id}`
blueprint with NO param → 422 (binding name + path) AND zero new VM rows (verify via psql). TC-005
regression: no-binding + static-binding blueprints still provision. TC-006 `Results/RESULT.md` and
`Retro/LESSONS.md` exist and were written BEFORE hv_integrate; RESULT.md carries the `/mcp` reload
operator instruction (mcp dist changed). TC-007 hv_integrate opened PRs and reported their links.
