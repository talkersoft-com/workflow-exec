# Task 0003 — MCP: secretBindingParams passthrough

Repo: `cloud-manager-mcp`. Mirror the marketplace/playbook MCP tool conventions.

## Steps
1. `cloud_marketplace_provision` gains an **optional** `secretBindingParams` argument matching the
   API shape: an array of `{ secretBindingId, params }` objects. Omitted → unchanged behavior.
2. Round-trip it to the `Instantiate` endpoint body verbatim (camelCase). Add the parameter
   description to the tool schema so operators know it supplies the source VM for templated bindings.
3. No other tool surface changes. Do NOT change unrelated tools.
4. Build: `python3 .cicd/build-cloud-manager.py --target mcp` green.

## Definition of done
`cloud_marketplace_provision` accepts optional `secretBindingParams` and round-trips it to the
endpoint; omitting it preserves current behavior; mcp build green. (MCP tool contract changed → the
final phase must flag the `/mcp` reload in RESULT.md.)
