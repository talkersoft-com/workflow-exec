# Test 0002 — MCP camelCase guard exemption

## TC-001 — `params` map keys with snake_case are accepted
Drive `apiRequest` (or the validation helper directly) with a `cloud_marketplace_provision`-shaped
payload containing `secretBindingParams:[{secretBindingId:"sb_…", params:{vm_public_id:"vm_…"}}]`.
Pass: no error is thrown; the request body passes the camelCase check with `vm_public_id` intact.
Fail: `snake_case key "vm_public_id" … not allowed` (or similar) is thrown.

## TC-002 — Structural/wire keys still guarded
Send a payload with a snake_case key OUTSIDE a `params` object — e.g. a top-level `bad_key`, or
`secretBindingParams:[{secret_binding_id:…}]` (snake_case where camelCase is required).
Pass: the guard still throws for the structural snake_case key.
Fail: the guard is silent (exemption leaked to structural fields).

## TC-003 — Exemption is scoped to `params` only
Send `secretBindingParams:[{secretBindingId:"sb_…", params:{vm_public_id:"vm_…"}, extra_field:1}]`.
Pass: `extra_field` (sibling of `params`, not inside it) is still rejected; `params` keys pass.
Fail: `extra_field` is accepted, or `params` keys are rejected.

## TC-004 — Build passes
Run `python3 .cicd/build-cloud-manager.py --target mcp`.
Pass: build completes with no errors.
Fail: any build error.
