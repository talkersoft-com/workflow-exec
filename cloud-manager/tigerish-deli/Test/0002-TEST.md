# Test 0002 — Blueprint Migration

## TC-001 — Migration compiles
Pass: `python3 .cicd/build-cloud-manager.py --target api` succeeds.

## TC-002 — Up seeds two blueprints
Pass: migration `Up` INSERTs `rabbitmq-jammy` and `rabbitmq-noble` into
`marketplace.blueprints`, each resolving its `virtual_machine_image_id` by image name.

## TC-003 — Up links playbooks
Pass: `Up` INSERTs two `marketplace.blueprint_playbooks` rows linking each blueprint to
the matching `vm.playbooks` row by name, `position=0`, `vars_override='{}'`.

## TC-004 — Down is reversible
Pass: `Down` deletes the join rows then the blueprint rows for both names; no orphan rows.

## TC-005 — No feature-flag recreation
Pass: migration does NOT INSERT/alter the `marketplace` feature flag.
