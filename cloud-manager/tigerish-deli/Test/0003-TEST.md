# Test 0003 — Verify, Deploy, Ship

## TC-001 — Migration applied
Pass: `dotnet ef database update` succeeds against the live DB before API deploy.

## TC-002 — API deployed healthy
Pass: `deploy-cloud-manager.py --target api` completes; `cloud_health_check` OK.

## TC-003 — Playbooks imported
Pass: after `import-playbooks.py`, `cloud_playbook_list` shows `rabbitmq-jammy` and
`rabbitmq-noble`.

## TC-004 — Marketplace lists both
Pass: `GET /api/v1/marketplace` (or marketplace MCP tool) returns both `rabbitmq-*`
blueprints.

## TC-005 — Instantiation works end-to-end
Pass: instantiating one blueprint provisions a VM, the playbook run succeeds
(`cloud_run_get` = success), RabbitMQ answers on 5672/15672, password in Vault.

## TC-006 — Outputs + ship
Pass: `Results/RESULT.md` and `Retro/LESSONS.md` exist before `hv_ship`; hv_ship opens
the PR and records its URL in RESULT.md.
