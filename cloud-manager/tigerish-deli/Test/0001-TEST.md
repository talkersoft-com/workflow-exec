# Test 0001 — RabbitMQ Playbooks

## TC-001 — Files exist
Pass: `rabbitmq-jammy.yaml`, `rabbitmq-jammy.meta.json`, `rabbitmq-noble.yaml`,
`rabbitmq-noble.meta.json` all present under `seed/playbooks/`.

## TC-002 — YAML parses
Pass: both `.yaml` files parse as valid YAML (e.g. `python3 -c "import yaml,sys;yaml.safe_load(open(f))"`).

## TC-003 — meta schema shape
Pass: each `.meta.json` has `name`, `description`, `argumentSchema` (with
`rabbitmq_user` default `admin`, `rabbitmq_vhost` default `/`), and `outputSchema`
(`password`, `management_url`). Matches the field shape of `pg-14-jammy.meta.json`.

## TC-004 — Required tasks present
Pass: each playbook contains tasks for apt-repo add, rabbitmq-server install,
management plugin enable, user/vhost create, Vault write, and firewall 5672/15672.

## TC-005 — Codename correctness
Pass: jammy playbook references `jammy`; noble references `noble`.
