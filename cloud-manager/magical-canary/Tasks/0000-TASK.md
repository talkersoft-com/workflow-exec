# Task 0000 — Initialize Workspace

## Goal
Verify the workspace is clean and on `magical-canary` across every repo in the cloud-manager deck.

## Steps
1. Run `hv_status deck: "cloud-manager"`
2. Confirm every repo is on branch `magical-canary` and clean
3. If any repo is dirty → STOP. Investigate before proceeding.
   - Exception: dirty repo with a merged PR → `hv_stash` → `hv_next` → `hv_unstash`

## Definition of done
All 15 repos on `magical-canary`, all clean.
