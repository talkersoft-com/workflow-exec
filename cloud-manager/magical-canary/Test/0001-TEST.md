# Test 0001 — Service layer

## TC-001 — Build clean
`python3 .cicd/build-cloud-manager.py --target api` exits 0.

## TC-002 — PagedResult<T> exists
`grep -R "record PagedResult" cloud-manager-api/src/Models/CloudManager.DTO/` returns the new record.

## TC-003 — Interface lists ListAsync
`grep "Task<.*PagedResult.*PlaybookRun.*> ListAsync" cloud-manager-api/src/Services/CloudManager.Data.Services/Interface/IPlaybookRunService.cs` matches.

## TC-004 — VaultRunToken scrub present
`grep -n "VaultRunToken = null" cloud-manager-api/src/Services/CloudManager.Data.Services/PlaybookRunService.cs` shows ≥ 2 occurrences (one for `ListByAssignmentAsync`, one for the new `ListAsync`).

## TC-005 — No N+1 lookup
`grep -c "PublicIdByGuidAsync" cloud-manager-api/src/Services/CloudManager.Data.Services/PlaybookRunService.cs` did not increase inside the new method. (Spot check: the new method uses `ToDictionaryAsync` for batched playbook public_id resolution.)
