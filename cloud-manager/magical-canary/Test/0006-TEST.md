# Test 0006 — Deploy + Ship

## TC-001 — Deploy clean
`python3 .cicd/deploy-cloud-manager.py --target all` exits 0. Smoke tests for api and web both pass (see deploy script output).

## TC-002 — Results + LESSONS exist before ship
`ls Results/RESULT.md Retro/LESSONS.md` — both files exist and are non-empty.

## TC-003 — hv_ship succeeds
PRs opened across `cloud-manager-api`, `cloud-manager-mcp`, `cloud-manager-web` (plus workflow-exec carrying this scaffold). URLs printed to operator.

## TC-004 — Deck auto-advances
After PR merges, `hv_status` shows all repos on a new branch (not `magical-canary`).

## TC-005 — Operator action surfaced
`grep -n "/mcp" Results/RESULT.md` matches — the "operator must `/mcp` reload" instruction is captured near the top, not buried.
