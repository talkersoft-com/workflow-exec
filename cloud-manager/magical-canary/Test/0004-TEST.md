# Test 0004 — Web grid

## TC-001 — Build clean
`python3 .cicd/build-cloud-manager.py --target web` exits 0 (vite build succeeds with no type errors).

## TC-002 — EmptyState removed
`grep -n "EmptyState" cloud-manager-web/src/pages/AnsibleHistoryPage.tsx` returns nothing (or only matches inside a fallback path, not the default render).

## TC-003 — api.runs.list exists
`grep -n "list: " cloud-manager-web/src/services/api.ts` shows a `list` member inside the `runs` block.

## TC-004 — Slice extended
`grep -n "page?" cloud-manager-web/src/store/slices/ansibleRunsSlice.ts` matches inside `FilterState`.

## TC-005 — New components present
The following files exist and are non-empty:
- `cloud-manager-web/src/components/RunHistoryGrid/RunHistoryGrid.tsx`
- `cloud-manager-web/src/components/VmFilterCombobox/VmFilterCombobox.tsx`
- `cloud-manager-web/src/components/Pagination/Pagination.tsx`

## TC-006 — Row click navigates to detail
`grep -n "/ansible/runs/" cloud-manager-web/src/components/RunHistoryGrid/RunHistoryGrid.tsx` matches a navigation call.
