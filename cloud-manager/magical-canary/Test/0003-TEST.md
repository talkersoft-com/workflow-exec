# Test 0003 — MCP tool

## TC-001 — Build clean
`python3 .cicd/build-cloud-manager.py --target mcp` exits 0.

## TC-002 — Tool registered in dist
`grep -c "cloud_playbook_run_list" cloud-manager-mcp/dist/tools/ansible.js` returns ≥ 1.

## TC-003 — Source registration matches
`grep -n "cloud_playbook_run_list" cloud-manager-mcp/src/tools/ansible.ts` shows the tool name + a `server.tool(...)` call directly above.

## TC-004 — Schema accepts expected args
`grep -A 10 '"cloud_playbook_run_list"' cloud-manager-mcp/src/tools/ansible.ts` includes `vmId`, `playbookId`, `status`, `page`, `pageSize`.
