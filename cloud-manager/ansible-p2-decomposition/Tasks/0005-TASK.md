# Task 0005 — MCP `graph.ts` + regression (cloud-manager-mcp)

## Steps
1. RESEARCH (read, no changes): how P1's `inventory.ts` module is structured and registered in
   the MCP profile; existing tool naming/arg conventions.
2. Add `graph.ts` with four tools, per contracts §4:
   - `cloud_entity_used_by` — wraps `GET entity/{publicId}/usedBy`
   - `cloud_entity_graph` — wraps `GET entity/{publicId}/graph?depth=N`
   - `cloud_playbook_play_list` — wraps `GET playbook/{pid}/play`
   - `cloud_playbook_task_list` — wraps `GET playbook/{pid}/play/{playId}/task`
3. Register the module in the profile; rebuild dist.
4. Build: `python3 .cicd/build-cloud-manager.py --target mcp` (and `--target api` if any API
   touch-ups landed here).
5. REGRESSION — existing flows byte-identical:
   - Existing playbook CRUD, revision list/get, role file CRUD, global role attach/detach,
     playbook run flows return byte-identical responses to main (the decomposer must be
     side-effect-only on these paths).
   - Run the decompose-all backfill against seed data and confirm every head revision gets
     records (or a `decompositionState: failed` mark) — used-by answers complete on day one.
6. Confirm zero diffs in cloud-manager-web, vorch-lib, vorch-service/porch.

Done when: both builds pass; the four tools exist in dist; regression checks pass; backfill
covered seed data.
