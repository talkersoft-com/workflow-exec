# Task 0003 — Part B edge graph (cloud-manager-api)

## Steps
1. RESEARCH (read, no changes): `PlaybookGlobalRoleRef` (`pgr`) — the only bespoke edge today;
   role `meta/main.yml` handling; P1 inventory entities (`inv`/`invg`/`invh`) for `targets`.
2. Add `EntityEdge` (`edge`, table `entity_edges`) per contracts §5:
   `{ fromPublicId, fromType, toPublicId, toType, edgeType }`, unique on (from, to, edgeType).
   Edge types (wire snake_case): `depends_on`, `includes`, `consumed_by`, `targets`,
   `defines`, `overrides`. Register prefix `edge`. Add the EF migration.
3. Write edges from the decomposer (same transaction as the record rebuild — reverse-index
   consistency is an invariant) plus existing mutation paths:
   - playbook → `includes` → role (from `roles:` lists AND existing `pgr` attach/detach)
   - role → `depends_on` → role (from `meta/main.yml` dependencies) — **cycle detection on
     insert, 400 on cycle**
   - var name → `consumed_by` → task (Jinja2 `{{ var }}` scan of task args); until P4, the
     from-slot carries the bare name prefixed `var:` (PLAN.md open question 1, default: keep
     pseudo-ids)
   - inventory/group/host (P1) → `targets` ← play, from `hosts:` patterns where matchable
   - var block → `defines` / `overrides` → var name
4. Build: `python3 .cicd/build-cloud-manager.py --target api`

Done when: build passes; saving a playbook that includes a role yields an `includes` edge;
inserting a role dependency cycle returns 400; edges rebuild in the decomposer's transaction.
