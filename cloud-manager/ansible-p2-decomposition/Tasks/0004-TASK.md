# Task 0004 — Part B APIs + backfill (cloud-manager-api)

## Steps
1. RESEARCH (read, no changes): how P1's controllers gate on the `ansible-studio` feature
   flag; existing DTO/mapper conventions (camelCase wire, public_ids only — convert via
   `PublicIdByGuidAsync` / `GuidByPublicIdAsync`).
2. Add endpoints, all gated on `ansible-studio`:
   - `GET api/v1/entity/{publicId}/usedBy` — incoming edges grouped by edgeType, with consumer
     summaries and counts (shape per PLAN.md C-GRAPH example)
   - `GET api/v1/entity/{publicId}/graph?depth=N` — bounded neighborhood; default depth 2,
     max 5 (PLAN.md open question 3, default: yes)
   - `GET api/v1/playbook/{pid}/play`, `GET api/v1/playbook/{pid}/play/{playId}/task`,
     `…/handler`, `…/varBlock` — read only
   - Do NOT add PATCH on task/play raw attrs — explicitly deferred to after P3; record the
     deferral in the coverage map.
3. Backfill: admin endpoint `POST api/v1/playbook/decompose-all` — decomposes every existing
   head revision, idempotent (re-running produces identical record/edge sets).
4. Record events on graph rebuild failures (best-effort try/catch, never blocking the primary
   write — VmEvent pattern).
5. Build: `python3 .cicd/build-cloud-manager.py --target api`

Done when: build passes; usedBy/graph/read endpoints return the PLAN.md shapes with public_ids
only; decompose-all is idempotent; no structured-edit PATCH exists.
