# Test 0004
TC-001 build (`--target api`) passes clean. TC-002 with `ansible-studio` disabled the new
endpoints return the flag-gated response; enabled, they serve. TC-003 `usedBy` on a role
included by a playbook returns an `includes` entry with `fromPublicId`/`fromType` and counts —
camelCase keys, public_ids only, no Guids. TC-004 `graph` defaults to depth 2 and rejects or
clamps depth > 5. TC-005 play/task/handler/varBlock endpoints are GET-only — no PATCH/PUT
routes exist on record entities. TC-006 `decompose-all` run twice yields identical record and
edge counts. TC-007 a forced rebuild failure records an event and does not fail the save.
