# Test 0002
TC-001 build (`--target api`) passes clean. TC-002 saving a playbook revision with valid YAML
produces `play`/`ptask`/`hdlr`/`vblk` rows for that revision; saving again replaces them
(record counts match the new content, no orphans from the old decomposition). TC-003 records
and (later) edges rebuild in the same transaction — grep confirms a single transaction scope
in the decomposer. TC-004 saving malformed YAML succeeds (blob stored, HTTP success) and the
revision is marked `decompositionState: failed`, visible via the API. TC-005 role file
revisions decompose too (a role tasks file yields `ptask` records). TC-006 no code path
recomposes records to YAML — no PATCH/PUT writes from record entities to revision content.
