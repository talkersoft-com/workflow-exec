# Test 0001
TC-001 build (`--target api`) passes clean. TC-002 migration creates exactly the four `ansible`
schema tables (`play` `ptask` `hdlr` `vblk` entities) with FKs per PLAN.md — PlaybookPlay →
PlaybookRevision (Cascade), PlaybookTask → play-or-handler parent, VarBlock owner type+id with
Kind constrained to `play_vars`/`role_defaults`/`role_vars`. TC-003 `PlaybookTask` carries
SourceSpan (start/end line+col). TC-004 `EntityPrefixRegistry` contains `play` `ptask` `hdlr`
`vblk` and grep shows no prefix collision. TC-005 no `edge` table in this migration. TC-006
zero diffs in cloud-manager-web, vorch-lib, vorch-service.
