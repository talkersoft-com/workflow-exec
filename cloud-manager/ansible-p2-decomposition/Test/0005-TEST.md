# Test 0005
TC-001 build (`--target mcp`) passes clean; dist contains `cloud_entity_used_by`,
`cloud_entity_graph`, `cloud_playbook_play_list`, `cloud_playbook_task_list` registered in the
profile. TC-002 existing playbook/role/run MCP tools and API responses are byte-identical to
main on the same seed data (decomposition is additive only). TC-003 decompose-all backfill run
against seed data: every head revision has records or a recorded `decompositionState: failed`
— none silently skipped. TC-004 `git diff` shows zero changes in cloud-manager-web, vorch-lib,
vorch-service.
