# Test 0006
TC-001 `dotnet ef database update` applied cleanly (new `ansible` tables + `entity_edges`
present in clouddb). TC-002 deploy script succeeded for api and mcp; API smoke poll returned
HTTP 200. TC-003 `Results/RESULT.md` exists with outcome, PR table, deploy + smoke details,
and the operator `/mcp` restart instruction. TC-004 `Retro/LESSONS.md` exists. TC-005 hv_ship
output shows a PR (or push to an existing PR) for cloud-manager-api, cloud-manager-mcp, and
workflow-exec; no repo left dirty. TC-006 `cloud_health_check` (or `GET api/v1/Host`) returns
healthy against the deployed API.
