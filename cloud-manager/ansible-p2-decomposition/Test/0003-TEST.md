# Test 0003
TC-001 build (`--target api`) passes clean. TC-002 migration creates `entity_edges` with the
unique (from, to, edgeType) constraint; prefix `edge` registered, no collision. TC-003 a
playbook with a `roles:` entry and a `pgr` attachment both yield `includes` edges; detaching
the global role removes its edge. TC-004 a role whose `meta/main.yml` depends on a role that
depends back on it → 400 on the insert that closes the cycle. TC-005 a task arg containing
`{{ api_token }}` yields a `consumed_by` edge whose from-slot is `var:api_token`. TC-006
edge writes happen inside the decomposer's transaction (record rebuild + edge rebuild are
atomic together). TC-007 zero diffs in cloud-manager-web, vorch-lib, vorch-service.
