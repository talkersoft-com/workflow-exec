# Test 0001
TC-001 build (`--target api`) passes clean. TC-002 migration creates exactly the seven `ansible`
schema tables with the unique constraints from PLAN.md (filtered unique on inventory name;
(inventory_id, name); (inventory_id, host_name); membership pair; (group_id, name);
(host_id, name)). TC-003 `EntityPrefixRegistry` contains `inv` `invg` `invh` `igm` `gvar` `hvar`
`invev` and grep shows no prefix collision. TC-004 migration seeds FeatureFlag `ansible-studio`
Enabled=false. TC-005 zero diffs in cloud-manager-web, vorch-lib, vorch-service.
