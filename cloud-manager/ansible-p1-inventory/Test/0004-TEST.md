# Test 0004
TC-001 build (`--target mcp`) passes clean; dist rebuilt. TC-002 `inventory.ts` exports all 16
tool names from Task 0004 and the `hv.cloud` profile registers the module. TC-003 every tool id
parameter uses `idSchema`; request bodies camelCase. TC-004 zero diffs outside cloud-manager-mcp
in this task.
