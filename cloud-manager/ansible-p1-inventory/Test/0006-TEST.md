# Test 0006
TC-001 migration applied (seven `ansible` tables exist in clouddb; flag row seeded). TC-002 API
deploy smoke passed (`http://localhost:5250/api/v1/Host` within 10s); MCP dist rebuilt with
stable lockfile. TC-003 `Results/RESULT.md` + `Retro/LESSONS.md` exist with the operator `/mcp`
reload instruction. TC-004 hv_ship opened PRs for cloud-manager-api, cloud-manager-mcp, and
workflow-exec; links recorded in RESULT.md PR table.
