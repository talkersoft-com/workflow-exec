# Test 0003
TC-001 build passes clean. TC-002 a CRUD round-trip (Test 0002 TC-002 sequence) produces the
matching event sequence on `api/v1/inventory/{id}/event`, newest first, types per contracts §6.
TC-003 the service interface exposes record + timeline only (no update/delete members).
TC-004 grep confirms event writes are wrapped best-effort (try/catch) at every mutation site.
