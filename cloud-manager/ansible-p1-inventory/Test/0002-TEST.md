# Test 0002
TC-001 build passes clean. TC-002 with `ansible-studio` enabled (local), full CRUD round-trip:
create inventory → group (nested child) → host (vm-backed + external) → membership → group/host
var PUT upsert + DELETE; responses camelCase with `inv_`/`invg_`/`invh_` public ids, no Guids.
TC-003 creating a parent-group cycle returns 400. TC-004 with the flag disabled, every
`api/v1/inventory*` route is gated (not reachable). TC-005 unique constraints enforced (duplicate
inventory name / group name / host name / var name → 409 or 400 per repo convention).
