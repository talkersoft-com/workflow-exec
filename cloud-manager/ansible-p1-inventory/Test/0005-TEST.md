# Test 0005
TC-001 with flag disabled, pre-existing endpoints respond exactly as before (no shape or status
changes); zero diffs in cloud-manager-web, vorch-lib, vorch-service. TC-002 with flag enabled,
C-INV example round-trips. TC-003 docs mention the seven entities, the inventory routes, and the
16 MCP tools. TC-004 full build passes clean.
