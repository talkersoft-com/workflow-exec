# Task 0000 — Initialize Workspace
1. `hv_status deck:"cloud-manager"`; clean on feature branch → `hv_next` (default → `hv_init`);
   dirty → STOP (exception: this scaffold's runtime edits in workflow-exec)
2. `hv_status` again; record the branch in `../deck.md` under "## Branch"
Done when: all repos on the recorded branch; deck.md Branch not TBD.
