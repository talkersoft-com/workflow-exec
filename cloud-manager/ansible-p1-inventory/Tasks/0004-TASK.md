# Task 0004 — MCP tools (cloud-manager-mcp)

## Steps
1. New module `inventory.ts`, registered in profile `hv.cloud`, following existing module
   conventions (idSchema for all ids, camelCase bodies, read/mutate separation):
   `cloud_inventory_list/get/create/update/delete`,
   `cloud_inventory_group_create/update/delete`,
   `cloud_inventory_host_attach/detach`,
   `cloud_group_var_set/list/delete`,
   `cloud_host_var_set/list/delete`.
2. Build: `python3 .cicd/build-cloud-manager.py --target mcp` (rebuilds dist).
3. Note for RESULT.md: tool contract changed → operator must `/mcp` reload after deploy.

Done when: build passes; all 16 tools present in the module and registered in `hv.cloud`.
