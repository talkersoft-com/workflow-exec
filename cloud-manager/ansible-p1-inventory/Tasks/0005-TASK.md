# Task 0005 — Regression + docs

## Steps
1. Regression: existing playbook/run/blueprint flows are byte-identical. With `ansible-studio`
   DISABLED, exercise the pre-existing surfaces (playbook list/get, vm playbook assignment list,
   run target list, blueprint list) and confirm responses are unchanged vs main behavior; porch's
   generated single-host inventory line is untouched (no vorch/porch diffs at all).
2. API smoke with the flag enabled locally: the C-INV wire example from PLAN.md round-trips
   (publicId/name/groups/hosts shape).
3. Update repo docs (cloud-manager-api + cloud-manager-mcp READMEs / docs folders) for the new
   entities, routes, and tools, matching existing doc conventions.
4. Build: `python3 .cicd/build-cloud-manager.py --target all` (api + mcp changed).

Done when: regression evidence captured for RESULT.md; docs updated; build passes.
