# Test 0004 — Vorch IP writeback

## TC-001 — PATCH call in CreateVMCommand
```bash
grep -nE 'ip-address|ipAddress|UpdateIpAddress' vorch-service/vorch-service/internal/vm/vm_handlers.go
```
Pass: at least one match referencing the PATCH URL pattern.

## TC-002 — Failure is non-fatal
```bash
grep -nE 'ip writeback failed|ip writeback returned' vorch-service/vorch-service/internal/vm/vm_handlers.go
```
Pass: log statements on failure, no `return err` / `log.Fatalf` in the writeback block.

## TC-003 — Build clean
```bash
cd vorch-service/vorch-service && go build ./... 2>&1 | tail -3
```
Pass: exit 0.
