# Test 0013 — Ship

## TC-001 — Results/RESULT.md filled out
```bash
test -f Results/RESULT.md && grep -cE '^TBD$|^TBD —' Results/RESULT.md
```
Pass: count is 0 (no remaining `TBD` placeholders) or only inside the PR table (which hv_ship fills).

## TC-002 — Retro/LESSONS.md filled out
```bash
test -f Retro/LESSONS.md && grep -cE '^TBD$' Retro/LESSONS.md
```
Pass: 0.

## TC-003 — hv_ship succeeds
Pass: PRs returned for `cloud-manager-api`, `vorch-service`, `workflow-exec`. Each PR URL recorded in `Results/RESULT.md`.
