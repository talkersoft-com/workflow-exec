# Test 0003 — Materialize under project/

## TC-001 — Every materialized RelativePath starts with project/
```bash
grep -nE 'RelativePath\s*=\s*' cloud-manager-api/src/Services/CloudManager.Data.Services/PlaybookMaterializationService.cs
```
Pass: every match's value string starts with `"project/` or `$"project/`.
Fail: any match without the prefix.

## TC-002 — Build clean
```bash
cd cloud-manager-api && dotnet build src/CloudManager.API/CloudManager.API.csproj -c Release -nologo -v q 2>&1 | tail -3
```
Pass: `0 Error(s)`.
