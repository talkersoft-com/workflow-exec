# Test 0001 — Migration + entity + DTO for VM ip_address

## TC-001 — Entity property exists
```bash
grep -nE 'IpAddress' cloud-manager-api/src/Models/CloudManager.Entities/Models/VirtualMachine.cs
```
Pass: at least one match.

## TC-002 — DbContext binding
```bash
grep -nE 'ip_address|IpAddress.*HasColumnName' cloud-manager-api/src/Models/CloudManager.Entities/CloudManagerDbContext.cs
```
Pass: at least one match.

## TC-003 — DTO property exists
```bash
grep -rnE 'public string\??\s+IpAddress' cloud-manager-api/src/Models/CloudManager.DTO/ | head -3
```
Pass: at least one match.

## TC-004 — Migration file exists
```bash
ls cloud-manager-api/src/Models/CloudManager.Entities/Migrations/*AddVmIpAddress*.cs
```
Pass: at least one file.

## TC-005 — Migration adds nullable varchar(45)
```bash
grep -A5 'AddVmIpAddress' cloud-manager-api/src/Models/CloudManager.Entities/Migrations/*AddVmIpAddress*.cs | grep -E 'AddColumn|nullable|maxLength' | head -5
```
Pass: `AddColumn`, `nullable: true`, `maxLength: 45` all present.

## TC-006 — Build clean
```bash
cd cloud-manager-api && dotnet build src/CloudManager.API/CloudManager.API.csproj -c Release -nologo -v q 2>&1 | tail -3
```
Pass: `0 Error(s)`.
