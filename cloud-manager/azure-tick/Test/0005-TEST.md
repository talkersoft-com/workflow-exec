# Test 0005 — Backfill script

## TC-001 — Script exists + executable
```bash
test -x cloud-manager-api/scripts/porch/backfill-vm-ip-addresses.py
```
Pass: file exists and is executable.

## TC-002 — Syntax valid
```bash
python3 -m py_compile cloud-manager-api/scripts/porch/backfill-vm-ip-addresses.py
```
Pass: exit 0.

## TC-003 — Uses domifaddr + PATCH endpoint
```bash
grep -E 'domifaddr|ip-address|UpdateIpAddress' cloud-manager-api/scripts/porch/backfill-vm-ip-addresses.py
```
Pass: at least one of each.
