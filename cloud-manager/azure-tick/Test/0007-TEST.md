# Test 0007 — Drop --project-dir

## TC-001 — Zero --project-dir matches
```bash
grep -- '--project-dir' vorch-service/vorch-service/internal/ansible/playbook_run_exec.go
```
Pass: zero output.

## TC-002 — Still has -p site.yml
```bash
grep -E '"-p", "site.yml"' vorch-service/vorch-service/internal/ansible/playbook_run_exec.go
```
Pass: at least one match.

## TC-003 — Build clean
```bash
cd vorch-service/vorch-service && go build ./... 2>&1 | tail -3
```
Pass: exit 0.
