# Test 0006 — Porch inventory generation

## TC-001 — target DTO has IpAddress
```bash
grep -nE 'IpAddress\s+string\s+`json:"ipAddress' vorch-service/vorch-service/internal/ansible/playbook_run_exec.go
```
Pass: at least one match.

## TC-002 — Inventory write logic exists
```bash
grep -nE 'inventory|hosts.*ansible_host|WriteFile.*hosts' vorch-service/vorch-service/internal/ansible/playbook_run_exec.go | head -5
```
Pass: at least one match for both `inventory` dir/file path and the host-line format.

## TC-003 — --inventory flag passed
```bash
grep -E '"--inventory"' vorch-service/vorch-service/internal/ansible/playbook_run_exec.go
```
Pass: at least one match.

## TC-004 — Empty IP fails the target
```bash
grep -nE 'ip_address not set|ipAddress.*empty|target.IpAddress\s*==\s*""' vorch-service/vorch-service/internal/ansible/playbook_run_exec.go
```
Pass: at least one match.

## TC-005 — Build clean
```bash
cd vorch-service/vorch-service && go build ./... 2>&1 | tail -3
```
Pass: exit 0.
