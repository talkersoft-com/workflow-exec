# Test 0008 — Redactor for subprocess streams

## TC-001 — internal/redact package exists
```bash
ls vorch-service/vorch-service/internal/redact/redact.go vorch-service/vorch-service/internal/redact/redact_test.go
```
Pass: both files exist.

## TC-002 — Unit tests pass
```bash
cd vorch-service/vorch-service && go test ./internal/redact/ 2>&1 | tail -3
```
Pass: `ok ... PASS` or `ok ... cached`.

## TC-003 — playbook_run_exec.go imports redact
```bash
grep -nE 'internal/redact|redact\.NewWriter|redact\.Scrub' vorch-service/vorch-service/internal/ansible/playbook_run_exec.go
```
Pass: at least one match.

## TC-004 — cmd.Stderr is wrapped
```bash
grep -nE 'cmd\.Stderr\s*=\s*redact\.NewWriter' vorch-service/vorch-service/internal/ansible/playbook_run_exec.go
```
Pass: at least one match.

## TC-005 — cmd/porch/main.go no longer has inline pattern
```bash
grep -nE 'hvsPattern|hvs\\\.\[A-Za-z' vorch-service/vorch-service/cmd/porch/main.go
```
Pass: zero output (the inline definitions moved to the new package).
Fail: still has the inline regex.

## TC-006 — Build clean
```bash
cd vorch-service/vorch-service && go build ./... 2>&1 | tail -3
```
Pass: exit 0.
