# Test 0002 — API IP-write endpoint + GET surfaces

## TC-001 — Service interface + impl
```bash
grep -nE 'UpdateIpAddressAsync' cloud-manager-api/src/Services/CloudManager.Data.Services/Interface/IVirtualMachineService.cs
grep -nE 'UpdateIpAddressAsync' cloud-manager-api/src/Services/CloudManager.Data.Services/VirtualMachineService.cs
```
Pass: each shows at least one match.

## TC-002 — Controller action
```bash
grep -nE '\[HttpPatch.*ip-address|UpdateIpAddressRequest' cloud-manager-api/src/CloudManager.API/Controllers/VirtualMachineController.cs
```
Pass: both route + request type present.

## TC-003 — Target DTO surfaces IpAddress
```bash
grep -nE 'IpAddress' cloud-manager-api/src/Models/CloudManager.DTO/Models/PlaybookRunTarget.cs
grep -nE 'IpAddress' cloud-manager-api/src/Services/CloudManager.Data.Services/PlaybookRunTargetService.cs
```
Pass: both show matches.

## TC-004 — Build clean
```bash
cd cloud-manager-api && dotnet build src/CloudManager.API/CloudManager.API.csproj -c Release -nologo -v q 2>&1 | tail -3
```
Pass: `0 Error(s)`.
