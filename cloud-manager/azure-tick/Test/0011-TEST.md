# Test 0011 — Stale-run cleanup

## TC-001 — Zero stale Queued runs after cleanup
```bash
PGPASSWORD='P@ssw0rd!' psql -h localhost -U cloudmanager -d clouddb -tAc \
  "SELECT count(*) FROM vm.playbook_runs WHERE status = 1 AND created < '2026-05-29'"
```
Pass: `0`.

## TC-002 — Cancelled rows have the marker error_message
```bash
PGPASSWORD='P@ssw0rd!' psql -h localhost -U cloudmanager -d clouddb -tAc \
  "SELECT count(*) FROM vm.playbook_runs WHERE error_message = 'abandoned during charmed-panda verification'"
```
Pass: count > 0 (matches the count from CHANGE-0011 before-snapshot).

## TC-003 — Counts recorded in CHANGE-0011
```bash
test -f planning/workflow-exec/cloud-manager/azure-tick/Retro/CHANGE-0011.md && grep -iE 'before|after|count' planning/workflow-exec/cloud-manager/azure-tick/Retro/CHANGE-0011.md | head -5
```
Pass: file exists, mentions before+after counts.

## TC-004 — Recent (legitimately Queued) runs untouched
```bash
PGPASSWORD='P@ssw0rd!' psql -h localhost -U cloudmanager -d clouddb -tAc \
  "SELECT count(*) FROM vm.playbook_runs WHERE status = 1 AND created >= '2026-05-29'"
```
Pass: any value is fine — just confirming the WHERE clause didn't sweep recent rows.
