# Task 0013 — Write Results + LESSONS; hv_ship

## Goal
Finalize `Results/RESULT.md` and `Retro/LESSONS.md` BEFORE `hv_ship`, then ship the deck.

## Target repo(s)
`cloud-manager-api`, `vorch-service`, `workflow-exec`

## Steps
1. Fill `Results/RESULT.md` with:
   - Outcome (SHIPPED / PARTIAL / FAILED).
   - Branch name (`azure-tick`).
   - PR table (left as "TBD" — `hv_ship` fills via its output).
   - One paragraph per phase summarizing what shipped, linking to `Retro/CHANGE-NNNN.md` files.
   - Constraints honored checklist.
2. Fill `Retro/LESSONS.md`:
   - What surprised us during execution.
   - Deviations from PLAN.md.
   - Any new follow-ups (link `[[follow-up-name]]`).
3. Call `hv_ship`:
   ```
   hv_ship  deck: "cloud-manager"
            message: "porch: inventory generation, redactor for subprocess streams, ansible-runner system install, materialize under project/"
            title:   "porch: pg playbook E2E — close all jazzy-herring follow-ups"
   ```
4. Record returned PR URLs in `Results/RESULT.md`.

## Definition of done
`hv_ship` completes successfully. Three PRs opened (cloud-manager-api, vorch-service, workflow-exec). PR URLs in `Results/RESULT.md`.
