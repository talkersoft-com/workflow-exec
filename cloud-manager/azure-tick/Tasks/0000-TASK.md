# Task 0000 — Initialize Workspace

## Goal
Confirm `azure-tick` branch is active across all repos and clean. Read source PLAN.md + LESSONS + FIX-002 so subsequent phases have full context.

## Target repo(s)
(no code changes — workspace verification only)

## Steps
1. Run `hv_status` on `cloud-manager` deck.
2. If every repo is already on `azure-tick` and clean → done.
3. If on a different branch → run `hv_next` (NOT `hv_init` — planning already shipped).
4. If any repo is dirty → STOP. Investigate.
5. Read these end-to-end:
   - `../../../../workflow-plans/cloud-manager/nutty-cobbler/PLAN.md`
   - `../../jazzy-herring/Retro/FIX-002.md`
   - `../../jazzy-herring/Retro/LESSONS.md`
6. Confirm branch name `azure-tick` matches `../deck.md`.

## Definition of done
All 15 repos on `azure-tick`, all clean. PLAN.md + LESSONS + FIX-002 read.
