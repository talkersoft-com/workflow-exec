# Test 0004
TC-001 fragment tolerance: a task-list chunk (starts at `- name:`, no document start, outer
indentation) renders in `fragmentMode` with full highlighting and zero diagnostics.
TC-002 position mapping: in the same fragment, a deliberate syntax error on line N produces a
diagnostic whose range falls on line N of the raw buffer — not offset by the synthetic
wrapper. TC-003 byte fidelity: load a fragment, make one edit, save — the saved text equals
the buffer exactly; an untouched fragment saves byte-identical to what was loaded (relative
indentation preserved). TC-004 client lint: an unclosed-quote fixture yields an error-severity
diagnostic with squiggle + gutter marker. TC-005 supplied diagnostics: passing two
`EditorDiagnostic`s (one warning, one info) renders both at their ranges with distinct
severity styling; updating the prop replaces them; passing `[]`/omitting the prop shows only
client lint (graceful absence). TC-006 panel: the lint panel lists client and supplied
findings together, `rule` shown when present. TC-007 `--target web` build passes cleanly.
