# Test 0006
TC-001 deploy: `deploy-cloud-manager.py --target web` exits 0; the post-deploy poll of
`http://localhost:3000/` succeeds within 10s. TC-002 smoke: at
https://ubuntu-server.talkersoft.com the editor page renders the CM6 component (not a
textarea), highlighting and theme switching work, a save round-trips byte-identical, a
fragment file shows no false document-start diagnostic, and a list page does not load the
editor chunk. TC-003 results: `Results/RESULT.md` exists with outcome, branch, deploy +
smoke detail, bundle budget, and a PR table; `Retro/LESSONS.md` exists. TC-004 ship:
hv_ship (stage "exec") opened a PR for every repo ahead of main — verify with `gh`/
hv_list_pulls if the ship output omits PR lines. TC-005 scope guard: the PR set touches only
`cloud-manager-web` and `workflow-exec` — no diffs in cloud-manager-api, cloud-manager-mcp,
vorch-lib, or vorch-service.
