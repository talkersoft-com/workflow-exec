# Test 0005
TC-001 inert by default: rendering without `decorations`/`hoverProvider` produces no
decoration ranges and registers no hover tooltip behavior differences. TC-002 decorations
live: supplying `[{ from, to, kind: "secret", varName: "api_token" }]` renders a mark over
that range styled by kind; all three kinds (`secret`, `required`, `vaultRef`) map to distinct
styles; updating the prop swaps ranges via the compartment without recreating the editor.
TC-003 hover bridge: with a `hoverProvider` stub, hovering a Jinja2 identifier calls it with
the correct `varName` and position and renders the returned content; a null resolution shows
no tooltip. TC-004 consumer parity: both `AnsiblePlaybookEditorPage` and
`AnsibleRoleDetailPage` compile and render with their pre-existing props untouched (grep the
page diffs: no prop renames/additions). TC-005 regression: on both pages — edit → dirty,
Cmd/Ctrl-S → save with change summary, beforeunload prompt when dirty, saved payload equals
buffer bytes. TC-006 bundle: the Task 0001 chunk-budget guard still passes. TC-007
`--target web` build passes cleanly.
