# Test 0003
TC-001 expression parse: `key: "{{ api_token }}"` yields a template node in the syntax tree
spanning the expression — the scalar is not one undifferentiated string token. TC-002 tags:
delimiters (`{{`/`}}`), the identifier (`api_token`), and a filter (`{{ x | default('y') }}`)
each carry their own highlight tag. TC-003 contexts: expressions parse inside plain, quoted,
and block (`|`) scalars; a fixture asserts all three. TC-004 statements: `{% if cond %}`
shows delimiter + keyword highlighting (deep statement parsing not required). TC-005
non-template scalars: a scalar without `{{`/`{%` contains no template nodes. TC-006
degradation: unbalanced `{{ foo` renders without thrown exceptions. TC-007 grammar is
in-repo: no new npm dependency provides the Jinja2 grammar (check package.json diff).
TC-008 `--target web` build passes cleanly.
