# Test 0002
TC-001 YAML structure: a fixture with keys, quoted/plain values, an anchor + alias, a block
scalar (`|`), and comments renders each with its distinct highlight tag (assert via the
syntax-tree/decoration API, not pixels). TC-002 Ansible keywords: in a task-list fixture,
`hosts`/`tasks`/`when`/`notify` in key position carry the keyword style while identical
strings in value position do not. TC-003 module position: in a task mapping with
`name` + `when` + `ansible.builtin.copy`, only `ansible.builtin.copy` carries the module-name
style. TC-004 indent guides: nested fixture shows guides at each indentation level.
TC-005 resilience: a fixture with a tab-indentation error and an unclosed quote renders
without thrown exceptions and still highlights the valid prefix. TC-006 `--target web` build
passes cleanly.
