# Test 0001
TC-001 C-ED surface: `AnsibleYamlEditor` accepts exactly the contracts §9 props —
`initialContent`, `onSave`, `onDirtyChange`, `fragmentMode`, `decorations`, `diagnostics`,
`hoverProvider`; the new props are optional and a render with only the legacy trio compiles
and runs. TC-002 dirty parity: editing the buffer fires `onDirtyChange(true)` on first change
and `onDirtyChange(false)` after save, matching the textarea component's transitions.
TC-003 save parity: Cmd/Ctrl-S invokes `onSave` with the exact buffer text (byte-identical,
no YAML re-serialization) and the change-summary flow works as before. TC-004 beforeunload:
the unload prompt is registered when dirty and removed when clean, exactly as today.
TC-005 theme: the editor renders with colors resolved from `--color-*` custom properties and
switches correctly when `[data-theme]` flips; grep the theme module for hardcoded hex colors
not sourced from tokens — zero hits. TC-006 lazy chunk: a production build shows CM6 packages
only in the lazy editor chunk, not in the entry chunk; the build-size guard passes and fails
when the budget is exceeded by >10% (verify the assertion exists and is wired in CI).
TC-007 `--target web` build passes cleanly.
