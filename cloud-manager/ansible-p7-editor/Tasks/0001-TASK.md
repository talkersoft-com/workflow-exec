# Task 0001 — CM6 foundation: packages, AnsibleYamlEditor shell, theme, lazy budget (cloud-manager-web repo)

## Steps
1. RESEARCH (read, no changes): `RoleFileEditor.tsx` — current props, dirty tracking,
   Cmd/Ctrl-S handling, change-summary field, beforeunload behavior; how both consumer pages
   (`AnsiblePlaybookEditorPage`, `AnsibleRoleDetailPage`) lazy-load it via `React.lazy`; the
   SCSS design-token system (`--color-*` custom properties under `[data-theme]`); the Vite
   build/chunking setup and any existing CI build-size checks.
2. Add CM6 packages: `@codemirror/state`, `@codemirror/view`, `@codemirror/language`,
   `@codemirror/lint`, `@codemirror/lang-yaml` (+ `@lezer/highlight` as needed). All imports
   live only in modules behind the existing lazy boundary.
3. Create `AnsibleYamlEditor` implementing the C-ED prop surface verbatim (contracts §9):
   `initialContent`, `onSave(content, changeSummary?)`, `onDirtyChange(dirty)`,
   `fragmentMode`, `decorations`, `diagnostics`, `hoverProvider`. New props are optional with
   inert defaults — this task wires the first three with behavior parity; later tasks light
   up the rest. Replace `RoleFileEditor` internals with a CM6 `EditorView`; the file keeps
   its lazy-loaded module boundary so consumers' imports do not change.
4. Behavior parity (byte-identical semantics): dirty tracking fires `onDirtyChange` on the
   same transitions as today; Cmd/Ctrl-S triggers save with the change-summary flow; the
   beforeunload prompt appears exactly when it does today; save sends the raw buffer text —
   the component never re-serializes YAML.
5. Theme: one CM6 theme (`EditorView.theme` + highlight style) reading the existing
   `--color-*` SCSS custom properties so dark/light follow `[data-theme]` automatically; no
   hardcoded second color system.
6. Bundle discipline: confirm CM6 lands only in the lazy editor chunk (list pages pay zero
   bytes). Add/extend the CI build-size check asserting the editor chunk within ±10% of the
   recorded budget; record the budget.
7. Component tests: mount, type, dirty transitions, Cmd/Ctrl-S → onSave with buffer text,
   prop parity for the legacy trio.
8. Build: `python3 .cicd/build-cloud-manager.py --target web` must pass.

Done when: `AnsibleYamlEditor` renders a CM6 editor behind the existing lazy boundary with
legacy-prop behavior parity, themed from SCSS tokens, bundle guard in place, web build clean.
