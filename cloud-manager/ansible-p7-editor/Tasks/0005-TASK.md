# Task 0005 — Extension points + regression: inert decorations/hoverProvider, consumer swaps, behavior parity (cloud-manager-web repo)

## Steps
1. RESEARCH (read, no changes): contracts §9 C-ED — the exact `EditorDecoration` shape
   (`{ from, to, kind: "secret" | "required" | "vaultRef", varName }`) and the
   `hoverProvider(varName, pos)` signature P8 will call with; CM6 `Compartment` and
   `hoverTooltip` APIs.
2. Decorations extension point: a `Compartment` mapping the `decorations` prop to
   `Decoration.mark`/`widget` ranges keyed by `kind`. Ships INERT — default empty array, no
   visual output until P8 supplies values — but a test fixture proves a supplied decoration
   renders, so P8 plugs in without editing this plan's files.
3. Hover extension point: a `hoverTooltip` bridge that resolves the hovered template
   identifier (`varName` from the Jinja2 tree) and position, calls `hoverProvider`, and
   renders returned `HoverContent`; inert when the prop is undefined or resolves null.
4. Swap both consumers: `AnsiblePlaybookEditorPage` and `AnsibleRoleDetailPage` use
   `AnsibleYamlEditor` through the unchanged lazy boundary with their exact existing props —
   consumers compile unchanged (legacy trio semantics).
5. Regression sweep (byte-identical behavior): dirty tracking transitions, Cmd/Ctrl-S save,
   change-summary flow, beforeunload prompt, save payload equals buffer text — across both
   pages, both themes, normal and fragment content.
6. Bundle re-check: editor chunk still within the Task 0001 budget guard.
7. Build: `python3 .cicd/build-cloud-manager.py --target web` must pass.

Done when: both extension points exist and are inert by default but exercisable from props,
both consumer pages run on the new editor with no prop changes, regression sweep green, web
build clean.
