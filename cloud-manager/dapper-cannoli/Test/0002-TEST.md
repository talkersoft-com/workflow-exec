# Test 0002 — Local Config Deployed

## TC-001 — ~/.hv/config.yaml has no active Bash(*)
Run: `grep 'Bash' ~/.hv/config.yaml`
Pass: line is absent or commented out (`# - "Bash(*)"`)
Fail: `- "Bash(*)"` appears uncommented.
