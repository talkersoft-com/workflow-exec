# Test 0000 — Workspace Initialized

## TC-001 — All repos on feature branch
Run `hv_status`. Pass: every repo on the same feature branch, none on default. Fail: any on default.

## TC-002 — All repos clean
Pass: no dirty repos. Fail: any uncommitted changes or unpushed commits.

## TC-003 — Branch name recorded
Open `../deck.md`. Pass: Branch field filled with actual branch name. Fail: still says `dapper-cannoli` placeholder.
