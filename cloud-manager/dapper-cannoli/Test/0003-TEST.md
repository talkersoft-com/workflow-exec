# Test 0003 — Deck Reinitialized, Bash Access Removed

## TC-001 — All repos on new feature branch
Run `hv_status`. Pass: all repos clean on new branch. Fail: any dirty or on wrong branch.

## TC-002 — settings.local.json has no Bash(*)
Run: `grep 'Bash' /Users/talker/workspace/cloud-manager/planning/execution-workflow/.claude/settings.local.json`
Pass: no output (Bash not present).
Fail: `Bash(*)` found.
