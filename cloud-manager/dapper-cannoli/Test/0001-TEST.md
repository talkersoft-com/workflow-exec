# Test 0001 — Bash(*) Commented Out

## TC-001 — config.yaml.example has Bash(*) commented out
Run: `grep 'Bash' hive-deck-pro/.hv/config.yaml.example`
Pass: line reads `# - "Bash(*)"` (commented).
Fail: line reads `- "Bash(*)"` (active).

## TC-002 — Commit pushed to warm-leviathan
Run: `git -C /path/to/hive-deck-pro log --oneline origin/warm-leviathan -1`
Pass: most recent commit references the Bash removal.
Fail: commit not present on remote.
