# Task 0001 — Comment out Bash(*) in config.yaml.example

## Goal
Remove unrestricted Bash access from the hive-deck-pro source config and commit it to the `warm-leviathan` branch.

## Target repo(s)
`hive-deck-pro` (outside the cloud-manager deck — commit manually per `../deck.md` Manual steps)

## Steps
1. Open `hive-deck-pro/.hv/config.yaml.example`
2. Find the `claude_settings.allow` section
3. Comment out the `- "Bash(*)"` line (prefix with `#`)
4. Commit and push to `warm-leviathan` using the git commands in `../deck.md` Manual steps

## Definition of done
`config.yaml.example` in hive-deck-pro has `Bash(*)` commented out and the commit is pushed to `warm-leviathan`.
