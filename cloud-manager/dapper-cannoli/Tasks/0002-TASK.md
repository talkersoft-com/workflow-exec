# Task 0002 — Deploy via make install-config FORCE=1

## Goal
Overwrite `~/.hv/config.yaml` with the updated `config.yaml.example` so the local config no longer contains `Bash(*)`.

## Steps
1. From the hive-deck-pro repo root, run: `make install-config FORCE=1`
2. Confirm `~/.hv/config.yaml` no longer contains `Bash(*)`

## Definition of done
`~/.hv/config.yaml` does not contain `Bash(*)` in `claude_settings.allow`.
