# Test 0004 — Provenance + chain panel

## TC-001 — Live chain to Succeeded
Playwright: instantiate `postgres-jammy` from the storefront, stay on the VM detail page —
chain step shows queued/running then **Succeeded** with NO manual reload (SignalR-driven).
Total wall-clock consistent with the backend's known timing (~2 min incl. SSH wait).

## TC-002 — Provenance
The same VM shows "Created from blueprint postgres-jammy" linking to the blueprint page; an
admin-created VM (wizard) shows no provenance block.

## TC-003 — Failure path renders
Simulate or locate a failed chain (e.g. blueprint with a playbook against an image where it
fails, or reuse evidence from a deliberately broken vars override): panel marks the failed step
and links to the run detail page. Destroy all test VMs after evidence.
