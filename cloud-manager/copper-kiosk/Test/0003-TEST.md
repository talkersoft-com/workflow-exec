# Test 0003 — Builder

## TC-001 — Compose + order
Playwright: create blueprint (image + defaults) → attach two playbooks → reorder via up/down →
positions persist after reload (`cloud_blueprint_get` confirms 0,1 order matching the UI).

## TC-002 — Lifecycle
Publish → badge updates AND appears on `/marketplace`; Archive → removed from `/marketplace`,
badge Archived; Delete on Published is blocked with the API's reason; Archive→Delete succeeds.

## TC-003 — Vars editor
Edit varsOverride JSON on one attached playbook; invalid JSON rejected client-side; valid JSON
persisted (confirm via `cloud_blueprint_get`). Clean up the test blueprint.
