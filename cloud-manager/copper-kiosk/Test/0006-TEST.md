# Test 0006 — E2E + ship

## TC-001 — Flag off = invisible + regression clean
Deployed app, flag disabled: zero marketplace surface; wizard create + direct attach/apply
unchanged (compare against lucky-engineblock's regression evidence).

## TC-002 — Flag on = full loop
Storefront browse (both seeds) → jammy instantiate to running VM → postgres-jammy instantiate
with chain panel live to Succeeded → builder create/publish/archive round-trip. All on the
DEPLOYED web app, all test artifacts cleaned up.

## TC-003 — Deploy + ship verified
Web smoke passed post-restart; RESULT.md + LESSONS.md committed in the ship; hv_ship opened PRs
for exactly cloud-manager-web, workflow-plans, workflow-exec (nothing else); all merged; flag
end-state noted.
