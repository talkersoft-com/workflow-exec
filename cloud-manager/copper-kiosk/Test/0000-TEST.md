# Test 0000 — Workspace Initialized

## TC-001 — All repos on the same feature branch
`hv_status`: every repo shows the same feature branch; none on the default branch.

## TC-002 — Clean (runtime scaffold edits excepted)
No dirty repos other than workflow-exec carrying this orchestration's runtime updates.

## TC-003 — Branch recorded + flag on
`../deck.md` "## Branch" carries the generated name (not TBD) and matches hv_status;
`cloud_feature_flag_list` shows `marketplace` enabled.
