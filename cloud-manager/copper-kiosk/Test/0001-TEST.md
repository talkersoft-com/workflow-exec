# Test 0001 — Plumbing

## TC-001 — Build passes
`--target web` exits 0.

## TC-002 — Flag gating
Against the locally served build (or deployed web) with playwright: flag DISABLED → no
Marketplace/Blueprints nav items, direct navigation to /marketplace shows the app's
flag-off/not-found behavior (same as /ansible with playbooks off). Flag ENABLED → both nav items
visible, placeholder pages render, no console errors.

## TC-003 — API client shapes
api.ts functions hit the documented endpoints (spot-check marketplace list + blueprint list
against the live API: 200 with seeded data; camelCase keys end-to-end; no internal GUIDs).
