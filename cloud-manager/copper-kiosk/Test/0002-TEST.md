# Test 0002 — Storefront

## TC-001 — Cards correct
Playwright against the running web app (flag on): `/marketplace` shows `jammy` ("image only") and
`postgres-jammy` ("1 playbook"; expandable detail names pg-14-jammy); OS badges from vmi config.

## TC-002 — Three-click instantiate
Card → modal (host pre-listed, defaults pre-filled, wizard name validation behaves identically —
reject invalid chars) → submit → redirected to `/virtualmachine/{id}`; `cloud_vm_get` confirms
blueprintId set and provisioning started. Destroy the test VM after evidence.

## TC-003 — Error surfacing
Instantiate with a duplicate VM name → modal shows the API error; no orphan VM created
(`cloud_vm_list` count unchanged).
