# Task 0005 — Vorch/porch status-surface assessment (findings only)

## Goal
Answer with evidence: does the existing status surface suffice for this UI? No code changes in
this task — anywhere.

## Steps
1. Assess against the implemented UI (Phases 2–4 are done by now):
   a. Provision visibility: orchestration status writeback granularity
      (Initialized → InProgress → Completed/Failed) + IP writeback timing — was anything missing
      for "watching my VM come up"?
   b. Chain visibility: run/target statuses + SignalR coverage — was the panel composable without
      gaps (e.g. the SSH-wait window between provision-complete and first run enqueue: does the
      UI show something honest there)?
2. Write `FINDINGS-vorch-status-surface.md` into
   `planning/workflow-plans/cloud-manager/copper-kiosk/` — verdict per question, evidence
   (event timelines, screenshots/logs from the Phase 4 tests), and any UX compromises made.
3. ONLY IF real gaps exist: include a drafted follow-up plan (PLAN-draft section in the findings
   doc) targeting the `vorch@cloud-manager` workflow — additive message-field changes at most.
   Expected outcome per the plan: no gaps, no draft.

## Definition of done
Findings doc committed in the plans repo; zero diffs in cloud-manager-api, cloud-manager-mcp,
vorch-lib, vorch-service.
