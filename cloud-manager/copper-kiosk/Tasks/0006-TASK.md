# Task 0006 — Deploy, full E2E, results, ship

## Goal
The deployed web app proves the whole feature; regressions are clean; everything ships.

## Steps
1. **Deploy web**: `python3 .cicd/deploy-cloud-manager.py --target web` (record smoke attempts/
   codes for RESULT.md). No api/mcp deploys — nothing changed there.
2. **Flag-off regression** (deployed app, flag disabled): no Marketplace/Blueprints nav; the
   Create VM wizard and every `/ansible/*` page behave byte-identically (spot-check create flow,
   attach/apply on a non-blueprint VM).
3. **Flag-on full pass** (playwright): browse storefront → instantiate `jammy` (lands on VM
   detail, provisions, no chain) → instantiate `postgres-jammy` (chain panel live to Succeeded)
   → builder round-trip (create/publish/archive a scratch blueprint). Destroy all test VMs.
4. Leave the `marketplace` flag in the state the operator wants (default: re-disable; note the
   choice in RESULT.md).
5. Write `Results/RESULT.md` (outcome, branch, PR table placeholders, deploy evidence, e2e
   evidence, flag end-state) and `Retro/LESSONS.md` — BEFORE shipping.
6. `hv_ship deck:"cloud-manager" stage:"exec"` with message/title from `../deck.md`; confirm PRs
   and record them in RESULT.md (post-ship commit if needed, as lucky-engineblock did).

## Definition of done
Web deployed and smoke-passed; both e2e cases proven on the live app; regressions clean; PRs
merged and recorded.
