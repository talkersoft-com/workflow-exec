# Task 0000 — Initialize Workspace

## Goal

Verify the workspace is clean and transition all repos to the next feature branch.

## Steps

1. Run `hv_status deck: "cloud-manager"`
2. All repos should be clean and on `brisk-finchling` (already transitioned after planning ship) — if so, proceed
3. If any repo is dirty → STOP and investigate
4. Run `hv_status` again to confirm
5. Record the branch name in `../deck.md` if it changed

## Definition of done

All repos on `brisk-finchling`, all clean.
