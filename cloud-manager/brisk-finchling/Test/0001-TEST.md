# Test 0001 — Service Layer

## TC-001 — Interface has UpdateHealthAsync

```bash
grep -r "UpdateHealthAsync" /Users/talker/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api/src/Services/CloudManager.Data.Services.Interface/
```
Pass: line found in `IFeatureFlagService.cs`.
Fail: no match.

## TC-002 — Implementation exists

```bash
grep -r "UpdateHealthAsync" /Users/talker/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api/src/Services/CloudManager.Data.Services/
```
Pass: line found in `FeatureFlagService.cs`.
Fail: no match.

## TC-003 — Clean build

```bash
cd /Users/talker/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api && dotnet build
```
Pass: `Build succeeded` with 0 errors.
Fail: any compile error.
