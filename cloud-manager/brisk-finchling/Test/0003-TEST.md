# Test 0003 — Tests, Smoke, Ship

## TC-001 — Full test suite passes

```bash
cd /Users/talker/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api && dotnet test
```
Pass: all tests pass, 0 failures.
Fail: any test failure.

## TC-002 — Unit tests for UpdateHealthAsync exist

```bash
grep -r "UpdateHealthAsync" /Users/talker/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api --include="*Test*" --include="*Tests*" -l
```
Pass: at least one test file found.
Fail: no match.

## TC-003 — Health PATCH via admin route (if API running)

```bash
curl -s -X PATCH http://localhost:5000/api/v1/admin/feature-flags/playbooks \
  -H "Content-Type: application/json" \
  -d '{"healthy": true, "last_probed_at": "2026-05-28T12:00:00Z"}' | jq '.healthy, .lastProbedAt'
```
Pass: returns `true` and the timestamp.
Fail: HTTP 404 or 500.

## TC-004 — Enabled PATCH does not change Healthy (if API running)

Set healthy to false, then PATCH enabled only, then GET and verify healthy is still false:
```bash
curl -s -X PATCH http://localhost:5000/api/v1/admin/feature-flags/playbooks \
  -H "Content-Type: application/json" -d '{"healthy": false}'
curl -s -X PATCH http://localhost:5000/api/v1/featureflags/playbooks \
  -H "Content-Type: application/json" -d '{"enabled": true}'
curl -s http://localhost:5000/api/v1/featureflags | jq '.playbooks.healthy'
```
Pass: `false` (healthy unchanged).
Fail: `true` (enabled PATCH accidentally reset healthy).

## TC-005 — Results and Lessons written

```bash
ls /Users/talker/workspace/cloud-manager/planning/workflow-exec/cloud-manager/brisk-finchling/Results/RESULT.md
ls /Users/talker/workspace/cloud-manager/planning/workflow-exec/cloud-manager/brisk-finchling/Retro/LESSONS.md
```
Pass: both files exist.
Fail: either file missing.

## TC-006 — hv_ship completes

Run `hv_ship deck: "cloud-manager"`.
Pass: output shows PR opened and merged for `cloud-manager-api`.
Fail: any error or dirty-repo block.
