# Test 0002 — Controller

## TC-001 — UpdateFlagRequest has nullable fields

```bash
grep -A5 "record UpdateFlagRequest" /Users/talker/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api/src/API/CloudManager.API/Controllers/FeatureFlagsController.cs
```
Pass: record contains `bool? Enabled`, `bool? Healthy`, `DateTime? LastProbedAt`.
Fail: `Enabled` is non-nullable or health fields are absent.

## TC-002 — Admin route alias present

```bash
grep "admin/feature-flags" /Users/talker/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api/src/API/CloudManager.API/Controllers/FeatureFlagsController.cs
```
Pass: line found with `api/v1/admin/feature-flags/{key}`.
Fail: no match.

## TC-003 — Clean build

```bash
cd /Users/talker/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api && dotnet build
```
Pass: `Build succeeded` with 0 errors.
Fail: any compile error.

## TC-004 — Empty request rejected

If API is running:
```bash
curl -s -o /dev/null -w "%{http_code}" -X PATCH http://localhost:5000/api/v1/featureflags/playbooks \
  -H "Content-Type: application/json" -d '{}'
```
Pass: HTTP 400.
Fail: HTTP 200 or 500.
