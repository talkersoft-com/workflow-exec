# Test 0000 — Workspace Initialized

## TC-001 — All repos on feature branch

Run `hv_status deck: "cloud-manager"`.
Pass: every repo shows `brisk-finchling`, no repo is on `main`.
Fail: any repo is on `main` or a different branch.

## TC-002 — All repos clean

Pass: `hv_status` shows no dirty repos (no uncommitted changes, no unpushed commits, no stash entries).
Fail: any repo shows dirty.
