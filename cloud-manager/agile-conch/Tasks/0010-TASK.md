# Task 0010 — Results + Lessons + hv_ship

## Goal
Document outcomes + lessons (including the historical `fix_*.sql` triage map) BEFORE `hv_ship`.

## Files
- `Results/RESULT.md`
- `Retro/LESSONS.md`

## Results/RESULT.md content

Sections (using the template the workflow rendered):
- Outcome: SHIPPED
- Branch: `agile-conch`
- Pull Requests table (filled from `hv_ship` output)
- Phase summary — one paragraph per phase 0001-0009

## Retro/LESSONS.md content — REQUIRED enumeration

This file must enumerate each of the six `fix_*.sql` triage patches we applied during the interactive troubleshooting session, what real bug each one masked, and which phase of THIS workflow replaced it:

| Triage patch | Masked bug | Replaced by phase |
|---|---|---|
| `fix_pg_run_id.sql` (replaced `{{ pg_run_id }}` with `{{ vm_public_id }}`) | porch didn't inject `run_public_id` into extravars | Phase 1 (porch extravars) + Phase 4 (playbook revert) |
| `fix_hvac.sql` (added `python3-hvac` to apt install on VM) | originally needed when vault task ran on the VM; later unnecessary because of `delegate_to: localhost` workaround | Phase 4 (playbook revert — both VM-side install and the delegate go away) |
| `fix_delegate.sql` (added `delegate_to: localhost`) | VAULT_ADDR was `127.0.0.1:8200`, unreachable from VM | Phase 2 (FQDN VAULT_ADDR) + Phase 4 (playbook revert) |
| `fix_become.sql` (added `become: false` on the delegated task) | follow-on from delegate — root-tmp-dir not writable | Phase 4 (the delegate goes away, become is irrelevant) |
| `fix_tmp.sql` (set `ansible_remote_tmp: /tmp/.ansible-porch-tmp`) | porch's systemd unit has `ProtectHome=yes`, blocking `/root/.ansible/tmp` | Phase 4 (delegate goes away → no temp-dir concern on porch side) |
| `fix_tls.sql` (set `validate_certs: false`) | Vault cert is for the FQDN, not `127.0.0.1` | Phase 2 (FQDN VAULT_ADDR → cert validates cleanly) |

Plus a "What would have helped" section per the template.

## Steps

1. Write `Results/RESULT.md` (Outcome, Branch, Pull Requests table, Phase summary)
2. Write `Retro/LESSONS.md` (the table above + freeform reflection)
3. Commit + push everything
4. Call `hv_ship`:

```
hv_ship deck: "cloud-manager"
        message: "porch: inject run_public_id + FQDN VAULT_ADDR; seed playbooks; bootstrap vault policies; retire the six fix_*.sql triage patches"
        title:   "harden playbook execution: porch extravars, vault config, seed playbooks, bootstrap policies"
```

## Definition of done
PRs opened across all affected repos; both files exist; the `fix_*.sql` → phase mapping table is complete.
