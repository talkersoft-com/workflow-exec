# Test 0010 — Ship

## TC-001 — RESULT + LESSONS exist BEFORE ship
`ls Results/RESULT.md Retro/LESSONS.md` — both files exist and are non-empty.

## TC-002 — LESSONS enumerates the six fix_*.sql patches
`grep -c "fix_" Retro/LESSONS.md` ≥ 6 (the six triage SQL files are named).

## TC-003 — hv_ship succeeds
PRs opened across affected repos; URLs printed.

## TC-004 — Deck auto-advances
Post-ship `hv_status` shows all repos on a new branch (not `agile-conch`).
