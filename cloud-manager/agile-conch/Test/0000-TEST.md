# Test 0000 — Workspace initialized

## TC-001 — All 15 repos on `agile-conch`
Run `hv_status deck: "cloud-manager"`. Pass: every repo shows `agile-conch`.

## TC-002 — All repos clean
Pass: no DIRTY entries.
