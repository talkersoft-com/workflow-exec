# Test 0001 — Porch extravars

## TC-001 — Build clean
`cd vorch-service && CGO_ENABLED=0 go build ./...` exits 0.

## TC-002 — Porch restarted
`systemctl status porch` shows active (running); PID is newer than build time.

## TC-003 — Fresh run's extravars JSON contains run_public_id
Trigger any playbook run. `cat /kvm-automator/playbook-runs/<runId>/env/extravars` contains `"run_public_id": "<runId>"` and `"secrets_prefix": "cloudmanager/data/playbook-secrets/<runId>"`.
