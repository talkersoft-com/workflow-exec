# Test 0009 — Teardown + rebootstrap

## TC-001 — bootstrap-worker.py completes
`python3 bootstrap-worker.py` exits 0. The workspace is replaced by a fresh checkout.

## TC-002 — Vault state matches pre-snapshot
`vault policy read playbook-run` matches pre-bootstrap content (modulo whitespace). `vault read auth/token/roles/playbook-run` matches.

## TC-003 — Seeded playbooks present
`cloud_playbook_list` returns both `pg-14-jammy` and `pg-16-noble` with content matching `seed/playbooks/*.yaml`.

## TC-004 — Fresh playbook run succeeds
Create a new Jammy VM, run the postgres playbook, observe `succeeded`. No manual ops.
