# Test 0006 — Playbook export + import

## TC-001 — export produces seed files
`python3 export-playbooks.py` creates `cloud-manager-api/seed/playbooks/pg-14-jammy.yaml` + `.meta.json`, `pg-16-noble.yaml` + `.meta.json`.

## TC-002 — Export is idempotent
Re-running produces no diff.

## TC-003 — Import recreates playbooks from scratch
Wipe `vm.playbooks` + `vm.playbook_revisions`. Run `python3 import-playbooks.py`. Verify both playbooks exist in the DB and `cloud_playbook_get` returns expected content.

## TC-004 — Import preserves operator-authored playbooks
Insert a new playbook via `cloud_playbook_create` (call it `op-test`). Run `import-playbooks.py`. The `op-test` playbook is still present (not deleted).
