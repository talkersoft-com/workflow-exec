# Test 0008 — E2E playbook run

## TC-001 — `cloud_playbook_run` returns succeeded
Final `cloud_run_get` status is `succeeded` (not `failed`).

## TC-002 — Vault has the per-run path
`vault kv get cloudmanager/playbook-secrets/<runId>/<host>/postgres` returns a structured secret with `host`, `port: 5432`, `db: app`, `user: appuser`, `password: <32-char string>`.

## TC-003 — UI surfaces the Vault path
Loading `http://localhost:3000/runs/<runId>` shows the Vault path in the run output (orchestrator policy from Phase 5 allows the read).

## TC-004 — No manual ops occurred
Grep the session transcript for `psql .* UPDATE`, `vault policy write`, `vault write auth/token/roles/`. Pass: no matches during this phase's wall-clock window.
