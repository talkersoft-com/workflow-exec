# Test 0003 — cloud_playbook_update accepts content

## TC-001 — API + MCP build clean
Both build per `@build-cloud-manager` fragment.

## TC-002 — Tool schema includes content
`grep content cloud-manager-mcp/src/tools/ansible.ts` shows it in the `cloud_playbook_update` Zod schema.

## TC-003 — Round-trip
`cloud_playbook_update playbookId=<id> content="<modified>" changeSummary="test"` succeeds; `cloud_playbook_get` returns the new content; a new row appears in `vm.playbook_revisions` with the supplied `changeSummary`.
