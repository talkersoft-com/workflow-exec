# Test 0004 — Playbooks reverted

## TC-001 — pg-14-jammy is clean
`cloud_playbook_get pb_PG14JAMMY01` content has NO `delegate_to:`, `become: false`, `ansible_remote_tmp`, `validate_certs: false`, `python3-hvac`, or `{{ vm_public_id }}` in the vault path.

## TC-002 — pg-14-jammy uses run_public_id
Content has `vault_secret_path: "cloudmanager/data/playbook-secrets/{{ run_public_id }}/{{ inventory_hostname }}/postgres"`.

## TC-003 — pg-16-noble is clean (same checks)
Same as TC-001 / TC-002 for `pb_EMEJCH08GG`.

## TC-004 — Revisions recorded
`vm.playbook_revisions` has a new entry per playbook with `change_summary` referencing the revert.
