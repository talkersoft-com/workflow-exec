# Test 0007 — Bootstrap-worker wiring

## TC-001 — Script parses
`python3 -m py_compile bootstrap-worker.py` exits 0.

## TC-002 — Steps 6b/6c are present
`grep bootstrap-policies.py bootstrap-worker.py` and `grep import-playbooks.py bootstrap-worker.py` both match.

## TC-003 — Missing VAULT_ROOT_TOKEN does not fail
Run the script with VAULT_ROOT_TOKEN unset. The Vault-bootstrap step prints a skip warning; does NOT exit non-zero.
