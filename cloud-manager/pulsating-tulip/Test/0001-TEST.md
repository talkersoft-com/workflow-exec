# Test 0001 — Claude-native schema in place

## TC-001 — ClaudePermissions struct exists
Run: `grep 'ClaudePermissions' /Users/talker/workspace/hive-deck-pro/hive-deck-pro/internal/config/config.go`
Pass: line found.
Fail: no output.

## TC-002 — No flat Allow/DefaultMode on ClaudeSettings
Run: `grep -A10 'type ClaudeSettings struct' /Users/talker/workspace/hive-deck-pro/hive-deck-pro/internal/config/config.go`
Pass: output shows `Permissions ClaudePermissions` but no standalone `Allow` or `DefaultMode` field.
Fail: `Allow` or `DefaultMode` present at top level.

## TC-003 — config.yaml.example uses permissions subtree
Run: `grep 'permissions:' /Users/talker/workspace/hive-deck-pro/hive-deck-pro/.hv/config.yaml.example`
Pass: line found.
Fail: no output.

## TC-004 — defaultMode is off
Run: `grep '^[^#]*defaultMode\|^[^#]*default_mode' /Users/talker/workspace/hive-deck-pro/hive-deck-pro/.hv/config.yaml.example ~/.hv/config.yaml`
Pass: no output (no active defaultMode line in either file).
Fail: uncommented line found.
