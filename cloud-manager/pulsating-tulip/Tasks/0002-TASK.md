# Task 0002 — Build, install, commit, ship, init

## Steps
1. `CGO_ENABLED=0 go build ./...` — verify clean build
2. `CGO_ENABLED=0 go install ./cmd/hv` — install updated binary
3. Commit and push hive-deck-pro to `warm-leviathan` per `../deck.md`
4. Write `Results/RESULT.md` and `Retro/LESSONS.md`
5. `hv_ship` per `../deck.md`
6. `hv_init  deck: "cloud-manager"`

## Definition of done
Build exits 0. PR merged. hv_init shows all repos on new branch with fresh settings files containing no `defaultMode`.
