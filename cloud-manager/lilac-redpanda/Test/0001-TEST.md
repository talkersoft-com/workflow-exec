# Test 0001 — Migration applied

## TC-001 — Entity files added
Run:
```
ls cloud-manager-api/src/Models/CloudManager.Entities/Models/{AnsibleCollection,HostAnsibleCollection,AnsibleCollectionInstallRun,PlaybookCollectionRequirement}.cs
```
Pass: all four files exist.
Fail: any missing.

## TC-002 — Enum file added
Run:
```
test -f cloud-manager-api/src/Models/CloudManager.Entities/Enums/CollectionInstallStatus.cs
```
Pass: exists.
Fail: missing.

## TC-003 — Migration file generated
Run:
```
ls cloud-manager-api/src/Models/CloudManager.Entities/Migrations/*AddAnsibleCollectionsSchema*
```
Pass: at least the `.cs` and `.Designer.cs` files exist.
Fail: missing.

## TC-004 — Build clean
Run:
```
cd cloud-manager-api/src && dotnet build CloudManager.sln -c Release
```
Pass: 0 errors.
Fail: any errors.

## TC-005 — Tables exist in clouddb
Run:
```
PGPASSWORD='P@ssw0rd!' psql -h localhost -U cloudmanager -d clouddb -c "\dt ansible.*"
```
Pass: rows for `collections`, `host_collections`, `collection_install_runs`, `playbook_collection_requirements`.
Fail: schema missing or fewer tables.

## TC-006 — Enum exists
Run:
```
PGPASSWORD='P@ssw0rd!' psql -h localhost -U cloudmanager -d clouddb -c "\dT ansible.*"
```
Pass: includes `collection_install_status` (or named per the migration).
Fail: missing.

## TC-007 — Seed rows present
Run:
```
PGPASSWORD='P@ssw0rd!' psql -h localhost -U cloudmanager -d clouddb -tAc "SELECT count(*) FROM ansible.collections WHERE namespace='community' AND name IN ('postgresql','hashi_vault');"
```
Pass: `2`.
Fail: anything else.

## TC-008 — CHANGE-0001.md recorded
Run:
```
test -s Retro/CHANGE-0001.md
```
Pass: exists, non-empty.
Fail: missing/empty.
