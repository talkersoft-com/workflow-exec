# Test 0003 — API endpoints

## TC-001 — Controllers exist
Run:
```
ls cloud-manager-api/src/CloudManager.API/Controllers/{AnsibleCollectionsController,HostCollectionsController,CollectionInstallRunsController}.cs
```
Pass: all three.
Fail: any missing.

## TC-002 — Ten routes declared
Run:
```
grep -hcE '\[Http(Get|Post|Delete|Patch)' cloud-manager-api/src/CloudManager.API/Controllers/{AnsibleCollections,HostCollections,CollectionInstallRuns}Controller.cs | paste -sd+ - | bc
```
Pass: 10.
Fail: anything else.

## TC-003 — Build clean
Run:
```
cd cloud-manager-api/src && dotnet build CloudManager.sln -c Release
```
Pass: 0 errors.
Fail: errors.

## TC-004 — CHANGE-0003.md recorded
Pass: file exists, non-empty.
Fail: missing/empty.
