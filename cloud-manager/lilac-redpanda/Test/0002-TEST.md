# Test 0002 — DTOs + AutoMapper + services + DI

## TC-001 — DTO files exist
Run:
```
ls cloud-manager-api/src/Models/CloudManager.DTO/Models/{AnsibleCollection,HostAnsibleCollection,AnsibleCollectionInstallRun,PlaybookCollectionRequirement}.cs
```
Pass: all four.
Fail: any missing.

## TC-002 — AutoMapper profile present
Run:
```
test -f cloud-manager-api/src/Services/CloudManager.Data.Services/MappingProfiles/AnsibleCollectionProfile.cs
```
Pass: exists.
Fail: missing.

## TC-003 — Service interfaces + implementations
Run:
```
grep -l "IAnsibleCollectionService\|IHostCollectionService\|ICollectionInstallRunService" cloud-manager-api/src/Services/CloudManager.Data.Services/Interface/*.cs | wc -l
```
Pass: ≥ 3 (at least one file mentions each interface).
Fail: < 3.

## TC-004 — DI registration
Run:
```
grep -n "AnsibleCollectionService\|HostCollectionService\|CollectionInstallRunService" cloud-manager-api/src/Services/CloudManager.Data.Services/ConfigureServices.cs
```
Pass: at least one registration line per service.
Fail: missing.

## TC-005 — Build clean
Run:
```
cd cloud-manager-api/src && dotnet build CloudManager.sln -c Release
```
Pass: 0 errors.
Fail: errors.

## TC-006 — CHANGE-0002.md recorded
Run:
```
test -s Retro/CHANGE-0002.md
```
Pass: exists.
Fail: missing.
