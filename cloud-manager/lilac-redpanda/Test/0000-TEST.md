# Test 0000 — Workspace Initialized

## TC-001 — All repos on feature branch
Run `hv_status` on the deck.
Pass: every repo on the same feature branch.
Fail: any repo on default or a different branch.

## TC-002 — All repos clean
Pass: hv_status shows no dirty repos.
Fail: any uncommitted changes / stash / unpushed commits.

## TC-003 — Branch name recorded
Open `../deck.md`.
Pass: Branch field is the actual generated execution branch name.
Fail: still says TBD or is a stale prior value.
