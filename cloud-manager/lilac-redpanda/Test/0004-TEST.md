# Test 0004 — API AMQP publishers

## TC-001 — Publisher classes exist
Run:
```
ls cloud-manager-api/src/Services/CloudManager.AMQP.Publisher/{CollectionInstallPublisher,CollectionRemovePublisher}.cs
```
Pass: both exist.
Fail: missing.

## TC-002 — Queue names declared
Run:
```
grep -n '"collection-installs"\|"collection-removes"' cloud-manager-api/src/Services/CloudManager.AMQP.Publisher/{CollectionInstallPublisher,CollectionRemovePublisher}.cs
```
Pass: one match per file (each declares its own queue).
Fail: missing.

## TC-003 — Wire body uses runId (camelCase)
Run:
```
grep -nE 'new \{ runId\b' cloud-manager-api/src/Services/CloudManager.AMQP.Publisher/{CollectionInstallPublisher,CollectionRemovePublisher}.cs
```
Pass: one match per file.
Fail: any snake_case (`run_id`).

## TC-004 — Call sites in HostCollectionsController
Run:
```
grep -n "Publish\(" cloud-manager-api/src/CloudManager.API/Controllers/HostCollectionsController.cs
```
Pass: at least 3 (Install + Reinstall + Remove).
Fail: < 3.

## TC-005 — DI registration
Run:
```
grep -n "CollectionInstallPublisher\|CollectionRemovePublisher" cloud-manager-api/src/Services/CloudManager.AMQP.Publisher/ConfigureServices.cs 2>/dev/null || grep -rn "CollectionInstallPublisher\|CollectionRemovePublisher" cloud-manager-api/src/Services/CloudManager.AMQP.Publisher/
```
Pass: registration line(s) found.
Fail: missing.

## TC-006 — Build clean
Pass: dotnet build → 0 errors.

## TC-007 — CHANGE-0004.md recorded
Pass: exists, non-empty.
