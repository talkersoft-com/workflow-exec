# Test 0006 — vorch subscribers + handlers + ReadWritePaths

## TC-001 — Subscriber files exist
Run:
```
ls vorch-service/vorch-service/messagesub/{collection_install_sub,collection_remove_sub}.go
```
Pass: both exist.
Fail: missing.

## TC-002 — Handler files exist
Run:
```
ls vorch-service/vorch-service/handlers/{collection_install_handler,collection_remove_handler}.go
```
Pass: both exist.
Fail: missing.

## TC-003 — Safety guard in remove handler
Run:
```
grep -n 'HasPrefix.*"/usr/share/ansible/collections/ansible_collections/"' vorch-service/vorch-service/handlers/collection_remove_handler.go
```
Pass: at least one match.
Fail: missing → unsafe `rm -rf` path.

## TC-004 — ansible-galaxy exec call
Run:
```
grep -nE 'exec\.Command\(.*"ansible-galaxy"|"ansible-galaxy"' vorch-service/vorch-service/handlers/collection_install_handler.go
```
Pass: at least one match.
Fail: handler doesn't invoke ansible-galaxy.

## TC-005 — Registration in app.go
Run:
```
grep -nE "CollectionInstall|CollectionRemove" vorch-service/vorch-service/app.go
```
Pass: at least 2 matches (one per subscriber).
Fail: not wired.

## TC-006 — ReadWritePaths widened in install script
Run:
```
grep -n "/usr/share/ansible/collections" cloud-manager-api/scripts/vorch/install-vorch-service.py
```
Pass: at least one match in the ReadWritePaths line.
Fail: missing.

## TC-007 — go build + go vet clean
Run:
```
cd vorch-service/vorch-service && go build ./... && go vet ./...
```
Pass: both clean.
Fail: errors.

## TC-008 — CHANGE-0006.md recorded
Pass: exists, non-empty.
