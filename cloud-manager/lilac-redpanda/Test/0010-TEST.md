# Test 0010 — Shipped

## TC-001 — Results/RESULT.md + Retro/LESSONS.md present
Run:
```
test -s Results/RESULT.md && test -s Retro/LESSONS.md
```
Pass: both non-empty.
Fail: either missing/empty.

## TC-002 — hv_ship succeeded
Inspect the Task 0010 hv_ship output.
Pass: ship output names PR URLs for each of cloud-manager-api, vorch-lib, vorch-service, cloud-manager-web, workflow-exec. No DIRTY warnings for unintended repos.
Fail: ship failed, or shipped a repo not named in deck.md.

## TC-003 — Deck advanced
Run `hv_status`.
Pass: branch is no longer the execution branch from Task 0000 (deck has rolled forward).
Fail: still on the execution branch.
