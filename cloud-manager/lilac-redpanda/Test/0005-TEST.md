# Test 0005 — vorch-lib message structs

## TC-001 — Files exist
Run:
```
ls vorch-lib/models/messages/{collection_install_run,collection_remove_run}.go
```
Pass: both exist.
Fail: missing.

## TC-002 — camelCase JSON tags
Run:
```
grep -E '`json:"runId"' vorch-lib/models/messages/collection_*.go
```
Pass: at least one match per file.
Fail: snake_case appears.

## TC-003 — go build + go vet clean
Run:
```
cd vorch-lib && go build ./... && go vet ./...
```
Pass: both commands exit 0 with no error output.
Fail: any error.

## TC-004 — CHANGE-0005.md recorded
Pass: exists, non-empty.
