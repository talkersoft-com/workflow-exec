# Test 0009 — Playwright e2e verification

## TC-001 — Flow A (catalog add)
Pass: `verify.md` records "row appears" and DB query returns the new row.
Fail: any step missing.

## TC-002 — Flow B (install community.postgresql) → Installed
Pass: DB shows `install_status=Installed`, `version` non-null, `install_path` under `/usr/share/ansible/collections/ansible_collections/community/postgresql`. Filesystem path exists. `Results/flow-b-installed.png` saved.
Fail: status stuck at Pending/Installing/Failed, or path missing.

## TC-003 — Flow C (remove) → Removed
Pass: DB shows `install_status=Removed`, `version=NULL`. Filesystem path no longer exists. `Results/flow-c-removed.png` saved.
Fail: status incorrect, or path still on disk.

## TC-004 — Flow D (reinstall after Removed) → Installed
Pass: DB back to `install_status=Installed`. `Results/flow-d-reinstalled.png` saved.
Fail: stuck or errored.

## TC-005 — Flow E (community.hashi_vault) → Installed
Pass: filesystem shows both collections under `/usr/share/ansible/collections/ansible_collections/community/`.
Fail: missing.

## TC-006 — Flow F (pg run no longer module-not-found)
Pass: `Results/pg-final-status.md` documents that the failure mode (whatever it is) is NOT "community.postgresql.postgresql_db module not found".
Fail: still module-not-found.

## TC-007 — verify.md written
Run:
```
test -s Results/verify.md
```
Pass: exists, non-empty.
Fail: missing/empty.

## TC-008 — No source code edits during Phase 9
Run:
```
git -C cloud-manager-web status --porcelain
git -C cloud-manager-api status --porcelain
git -C vorch-service status --porcelain
git -C vorch-lib status --porcelain
```
Pass: same dirty set as end of Phase 7.
Fail: any new file modified.
