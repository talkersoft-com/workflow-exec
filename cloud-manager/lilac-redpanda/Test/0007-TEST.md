# Test 0007 — Web Collections sub-nav + pages + slices

## TC-001 — Collections tab in AnsibleSubNav
Run:
```
grep -n 'label: "Collections"' cloud-manager-web/src/components/AnsibleSubNav/AnsibleSubNav.tsx
```
Pass: 1 match.
Fail: 0.

## TC-002 — Routes wired in App.tsx
Run:
```
grep -nE "/ansible/collections" cloud-manager-web/src/App.tsx | grep -E "AnsibleCollectionsPage|AnsibleCollectionDetailPage"
```
Pass: at least 2 matches.
Fail: routes missing.

## TC-003 — Page files exist
Run:
```
ls cloud-manager-web/src/pages/{AnsibleCollectionsPage,AnsibleCollectionDetailPage}.tsx
```
Pass: both.
Fail: missing.

## TC-004 — Three slices exist
Run:
```
ls cloud-manager-web/src/store/slices/{collections,hostCollections,collectionInstallRuns}Slice.ts
```
Pass: all three.
Fail: any missing.

## TC-005 — Polling thunk present
Run:
```
grep -nE "pollUntilTerminal|setTimeout.*5\\.?0?0?0|Installing|Removing" cloud-manager-web/src/store/slices/collectionInstallRunsSlice.ts
```
Pass: matches present.
Fail: no polling logic.

## TC-006 — TypeScript + build clean
Run:
```
cd cloud-manager-web && npx tsc --noEmit && npm run build
```
Pass: both exit 0.
Fail: errors.

## TC-007 — CHANGE-0007.md recorded
Pass: exists, non-empty.

## TC-008 — Out-of-scope files not edited
Run:
```
git -C cloud-manager-web status --porcelain | grep -vE "AnsibleSubNav\.tsx|App\.tsx|AnsibleCollectionsPage\.tsx|AnsibleCollectionDetailPage\.tsx|collectionsSlice\.ts|hostCollectionsSlice\.ts|collectionInstallRunsSlice\.ts|services/api\.ts|store/index\.ts"
```
Pass: zero output.
Fail: any other file appears.
