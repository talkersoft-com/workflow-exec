# Task 0010 — Finalize Results + Lessons; ship

## Goal
Final `Results/RESULT.md` + `Retro/LESSONS.md`, then `hv_ship`.

## Target repo(s)
All from Phase 1–7 plus `workflow-exec`.

## Steps

1. **Finalize `Results/RESULT.md`**:
   - Outcome: SHIPPED | PARTIAL | FAILED
   - Branch: the execution branch recorded in Task 0000
   - PRs: leave `TBD — filled from hv_ship output`
   - Phase summary: one paragraph per phase
   - Pointers to the screenshots and `verify.md` from Phase 9

2. **Write `Retro/LESSONS.md`** with what would have helped + any FIX files written + deviations from plan.

3. **Run `hv_status`** — verify only the expected repos are dirty (`cloud-manager-api`, `vorch-lib`, `vorch-service`, `cloud-manager-web`, `workflow-exec`).

4. **Run `hv_ship`**:
   ```
   hv_ship  deck: "cloud-manager"
            message: "feat: web-driven Ansible collection install / remove / reinstall"
            title:   "feat: web-driven Ansible collection install / remove / reinstall"
   ```

5. **Stop.**

## Definition of done
- `Results/RESULT.md` populated.
- `Retro/LESSONS.md` populated.
- `hv_ship` returned success with PR URLs.
- All ORCH.md task checkboxes checked.
