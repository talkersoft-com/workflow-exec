# Test 0004 — `cloud_vm_events`

## TC-001 — Tool registered
- Pass: `grep -r "cloud_vm_events" cloud-manager-mcp/src/` finds it in
  vms.ts and the index/barrel.
- Fail: missing.

## TC-002 — Default take
Stub HTTP client, call with `{ publicId: "vm-abc" }`.
- Pass: outgoing URL is `/api/v1/VirtualMachine/vm-abc/events` (no query
  string) OR `?take=100`.
- Fail: any other variant.

## TC-003 — Clamp at 500
Call with `{ publicId: "vm-abc", take: 9999 }`.
- Pass: outgoing query is `?take=500`.
- Fail: 9999 propagates to the wire.

## TC-004 — Build clean
`npm run build` exits 0.
