# Test 0003 — `cloud_vm_ip_address_update`

## TC-001 — Tool registered
- Pass: `grep -r "cloud_vm_ip_address_update" cloud-manager-mcp/src/`
  finds it in vms.ts and the index/barrel.
- Fail: missing in either.

## TC-002 — Wire body shape
Stub HTTP client, call with `{ publicId: "vm-abc", ipAddress: "10.0.0.5" }`.
- Pass: outgoing URL ends with `/vm-abc/ip-address`; body is exactly
  `{ ipAddress: "10.0.0.5" }`.
- Fail: `publicId` leaks into body, or `ip_address` snake_case appears.

## TC-003 — Description warns of operator-override intent
- Pass: tool description (or annotations) contains the phrase "operator"
  or "override" so the LLM doesn't auto-fire it.
- Fail: description is generic.

## TC-004 — Build clean
`npm run build` exits 0.
