# Test 0002 — `cloud_vm_update`

## TC-001 — Tool is registered
- Pass: `grep -r "cloud_vm_update" cloud-manager-mcp/src/` returns hits in
  both `tools/vms.ts` and the index/barrel.
- Fail: missing from index/barrel.

## TC-002 — publicId not in body
Stub the HTTP client and call the tool with `{ publicId: "vm-abc", name: "x" }`.
- Pass: outgoing body is `{ name: "x" }` (no `publicId` key).
- Fail: body contains `publicId`.

## TC-003 — Returns DTO unchanged
Stub the HTTP client to return `{ publicId: "vm-abc", name: "x", memory: 4 }`.
- Pass: tool output equals that object verbatim.
- Fail: tool reshapes or drops fields.

## TC-004 — Build clean
`npm run build` in `cloud-manager-mcp/` exits 0.
