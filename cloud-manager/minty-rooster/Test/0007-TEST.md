# Test 0007 — Ship

## TC-001 — RESULT.md present with operator notice
- Pass: `Results/RESULT.md` exists; first 30 lines contain the phrase
  "Restart the cloud-manager-mcp via `/mcp`" verbatim.
- Fail: file missing or notice absent / paraphrased.

## TC-002 — LESSONS.md present
- Pass: `Retro/LESSONS.md` exists with at least the "What would have
  helped" section filled in (a single "nothing went wrong" line is fine).
- Fail: file missing or section blank.

## TC-003 — PR merged
After `hv_ship`:
- Pass: cloud-manager-mcp PR returned by hv_ship is merged on GitHub.
- Fail: any other status (open, closed-unmerged, conflicts).

## TC-004 — Deck transitioned
Run `hv_status  deck: "cloud-manager"`.
- Pass: all repos on a NEW branch (no longer `minty-rooster`), clean.
- Fail: still on `minty-rooster` or any repo dirty.
