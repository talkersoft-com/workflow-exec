# Test 0000 — Workspace initialized

## TC-001 — All repos on feature branch
Run `hv_status  deck: "cloud-manager"`.
- Pass: every one of the 15 repos shows branch `minty-rooster`.
- Fail: any repo on `main` or a different feature branch.

## TC-002 — All repos clean
- Pass: `hv_status` reports no dirty repos (no uncommitted changes, no
  unpushed commits, no stash entries).
- Fail: any dirty reason reported.

## TC-003 — deck.md Branch field
Open `../deck.md`.
- Pass: Branch field is `minty-rooster` (not TBD).
- Fail: missing or different value.
