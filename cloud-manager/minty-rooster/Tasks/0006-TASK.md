# Task 0006 — Build + deploy verify (`@cloud-manager-deploy`)

## Goal
Run the deploy fragment's gate. Tier 1 has no api / web changes, so the
api/web deploy steps reduce to smoke tests. mcp dist is built and
committed but NOT reloaded in this session.

## Steps
1. **No EF migration** added this workflow — skip the `dotnet ef database update`
   step. Confirm via `git diff --name-only origin/main -- cloud-manager-api/src/Models/CloudManager.Entities/Migrations/`
   showing no new files.
2. **cloud-manager-api smoke test (no rebuild required):**
   ```
   curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:5250/api/v1/Host
   ```
   Expect 200/401/403. If 5xx or no response: surface (api was deployed
   from previous PR; if dead, that's an environment issue, not this PR).
3. **cloud-manager-web smoke test (no rebuild required):**
   ```
   curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:<web-port>/
   ```
   Expect 200/304.
4. **cloud-manager-mcp build:**
   ```
   cd /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-mcp && \
     npm ci && npm run build
   ```
   `npm ci` must leave `package-lock.json` byte-identical. If it modifies
   the lockfile: write `Retro/FIX-NNN.md` and stop.
5. **Stage the dist for commit** — confirm `dist/` is tracked or whatever
   the repo's convention is. Do NOT manually restart the running mcp.

## Definition of done
- API + web smoke tests green.
- `npm run build` in cloud-manager-mcp clean.
- Lockfile unchanged after `npm ci`.
- No api/web/vorch repo edits beyond what previous PRs introduced.
