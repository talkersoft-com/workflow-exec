# Task 0000 — Initialize workspace

## Goal
Confirm all 15 deck repos are on `minty-rooster` and clean. The branch was
already created by `hv_next` during planning.

## Steps
1. Call `hv_status  deck: "cloud-manager"`.
2. Verify every repo reports branch `minty-rooster` and clean state.
3. If any repo is dirty or on a different branch: STOP and surface to operator.
   - Exception: dirty repo whose previous PR merged → `hv_stash` → `hv_next` →
     `hv_unstash` (should not apply here; the deck transitioned in planning).
4. Confirm the branch name recorded in `../deck.md` is `minty-rooster`.

## Definition of done
- `hv_status` clean across 15 repos.
- All repos on `minty-rooster`.
- `../deck.md` Branch field reads `minty-rooster`.
