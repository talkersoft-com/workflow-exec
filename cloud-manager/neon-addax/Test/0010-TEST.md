# Test 0010 — API Phase 9 (run-cancel endpoint + IPlaybookRunCancelPublisher)

## TC-001 — dotnet build clean
Run: `cd /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api && dotnet build`
Pass: 0 errors.

## TC-002 — Cancel endpoint exists on RunController
Run: `grep -E 'HttpPost.*\{runId\}/cancel|/cancel.*HttpPost' /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api/src/CloudManager.API/Controllers/RunController.cs`
Pass: at least one match.

## TC-003 — IPlaybookRunCancelPublisher exists with correct queue name
Run: `grep -rE 'playbook-run-cancel' /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api/src/Services/CloudManager.AMQP.Publisher/`
Pass: at least one match (the queue name literal).
Fail: no match (means the publisher was implemented but uses the wrong queue name, OR the publisher wasn't implemented).

## TC-004 — Queued run cancels directly to Cancelled
Trigger a multi-VM run (Task 0009 endpoint). Immediately POST `/api/v1/run/{runId}/cancel`.
Pass: HTTP 200; run status is `Cancelled`; `completed_at` is set.
Fail: status still `Queued` or any 4xx.

## TC-005 — Already-terminal run rejects cancel
Cancel the same run again.
Pass: HTTP 409.
Fail: 200.

## TC-006 — Running run publishes message, does NOT flip status
Hand-update a run row's status to `running` via SQL. POST cancel.
Pass: HTTP 200; queue `playbook-run-cancel` depth increased by 1 (verify via RabbitMQ management UI); run status in DB remains `Running` (worker is expected to write final state later).
Fail: status flipped to `Cancelled` in DB (would be wrong), or no message published.

## TC-007 — Cleanup
Delete test runs and targets.
