# Test 0006 — API Phase 5 (playbook-local roles full surface)

## TC-001 — dotnet build clean
Run: `cd /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api && dotnet build`
Pass: 0 errors.

## TC-002 — Three local-role controllers exist with RequireFeatureFlag
Run: `for f in PlaybookRoleController PlaybookRoleFileController PlaybookRoleFileRevisionController; do
  grep -l 'RequireFeatureFlag("playbooks")' /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api/src/CloudManager.API/Controllers/$f.cs || echo "MISSING: $f"
done`
Pass: no `MISSING:` lines.
Fail: any missing.

## TC-003 — Local role CRUD happy path
Create playbook → create local role → create file → PATCH file → GET revisions.
Pass: every step returns 2xx, revision is created on PATCH.
Fail: any step returns 4xx/5xx, or no revision row appears.

## TC-004 — Argument-specs hook fires for owning playbook only
Create TWO playbooks. Create a local role under playbook A. Add `meta/argument_specs.yml` to it with an option.
Pass: playbook A's `argument_schema` includes the option; playbook B's `argument_schema` does not.
Fail: B's schema is affected.

## TC-005 — Delete local role cascades
DELETE the local role.
Pass: HTTP 204. Query `vm.playbook_role_files` and `vm.playbook_role_file_revisions` for that `playbook_role_id` → 0 rows.
Fail: rows remain.

## TC-006 — Cleanup
Delete both playbooks.
