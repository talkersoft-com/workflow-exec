# Test 0002 — API Phase 1 (AnsibleRole + RoleFile CRUD)

## TC-001 — dotnet build clean
Run: `cd /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api && dotnet build`
Pass: 0 errors.
Fail: any errors.

## TC-002 — Controllers exist
Run: `ls /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api/src/CloudManager.API/Controllers/ | grep -E '(AnsibleRoleController|RoleFileController)\.cs'`
Pass: both files present.
Fail: either missing.

## TC-003 — RequireFeatureFlag on both controllers
Run: `grep -l 'RequireFeatureFlag("playbooks")' /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api/src/CloudManager.API/Controllers/AnsibleRoleController.cs /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api/src/CloudManager.API/Controllers/RoleFileController.cs`
Pass: both files listed.
Fail: either missing the attribute.

## TC-004 — Create role happy path
Start the API (assume already running locally on the configured port). POST `/api/v1/ansibleRole` with `{"name":"test-role-tc004","description":"smoke"}`.
Pass: HTTP 201, response body has `publicId` starting with `ansr_`.
Fail: any other status.

## TC-005 — Create file happy path
Using the role from TC-004: POST `/api/v1/ansibleRole/{publicId}/file` with `{"file_path":"tasks/main.yml","content":"---\n"}`.
Pass: HTTP 201, response body has `publicId` starting with `rf_`.
Fail: any other status.

## TC-006 — Path traversal rejected
POST `/api/v1/ansibleRole/{publicId}/file` with `{"file_path":"../etc/passwd","content":"x"}`.
Pass: HTTP 400.
Fail: any 2xx.

## TC-007 — Oversize content rejected
POST a file with > 1 MiB content.
Pass: HTTP 400.
Fail: 2xx.

## TC-008 — Cleanup
DELETE the role: `/api/v1/ansibleRole/{publicId}`.
Pass: HTTP 204.
Fail: any other status (or if a `PlaybookGlobalRoleRef` blocks it — won't happen in this test).
