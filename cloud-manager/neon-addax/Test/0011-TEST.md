# Test 0011 — Shipped

## TC-001 — Results file exists
Run: `test -f /home/todd/workspace/cloud-manager/planning/execution-workflow/cloud-manager/neon-addax/Results/RESULT.md && echo OK`
Pass: `OK`.
Fail: no output.

## TC-002 — Lessons file exists
Run: `test -f /home/todd/workspace/cloud-manager/planning/execution-workflow/cloud-manager/neon-addax/Retro/LESSONS.md && echo OK`
Pass: `OK`.
Fail: no output.

## TC-003 — cloud-manager-api PR merged
Pass: hv_ship output shows `cloud-manager-api` PR merged.
Fail: PR not created, not merged, or `cloud-manager-api` repo skipped.

## TC-004 — execution-workflow PR merged (Results + Lessons)
Pass: hv_ship output shows `execution-workflow` PR merged with both files in the diff.
Fail: PR not created, not merged, or only one of the two files appears.

## TC-005 — All other deck repos skipped
Pass: hv_ship output shows every deck repo other than `cloud-manager-api` and `execution-workflow` as skipped (clean / nothing ahead).
Fail: any unrelated repo had changes pushed.
