# Test 0005 — API Phase 4 (PlaybookGlobalRoleRef + argument-specs hook)

## TC-001 — dotnet build clean
Run: `cd /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api && dotnet build`
Pass: 0 errors.

## TC-002 — PlaybookGlobalRoleRefController exists with RequireFeatureFlag
Run: `grep -l 'RequireFeatureFlag("playbooks")' /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api/src/CloudManager.API/Controllers/PlaybookGlobalRoleRefController.cs`
Pass: file listed.

## TC-003 — Attach happy path
Create a global role with `meta/argument_specs.yml` containing one option. Create a playbook. POST `/api/v1/playbook/{p}/globalRole` `{ ansibleRoleId: <r> }`.
Pass: HTTP 201 with `publicId` starting with `pgr_`.
Fail: other status.

## TC-004 — Attach is idempotent-reject (409 on duplicate)
POST same body again.
Pass: HTTP 409.
Fail: 201 (would mean duplicate ref created).

## TC-005 — argument_schema updated on attach
After TC-003: query `vm.playbooks.argument_schema` for the playbook.
Pass: schema includes the option from the role's argument_specs.yml.
Fail: schema is `{}` or missing the option.

## TC-006 — argument_schema updated on role file edit
PATCH the role's `meta/argument_specs.yml` to add a second option.
Pass: playbook's `argument_schema` now includes both options.
Fail: only the original option.

## TC-007 — argument_schema updated on detach
DELETE `/api/v1/playbook/{p}/globalRole/{refId}`.
Pass: HTTP 204; playbook's `argument_schema` no longer includes the role's options.
Fail: any other status or schema still contains them.

## TC-008 — Cleanup
Delete the playbook and the role.
