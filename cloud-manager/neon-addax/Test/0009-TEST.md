# Test 0009 — API Phase 8 (multi-VM run-trigger endpoint)

## TC-001 — dotnet build clean
Run: `cd /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api && dotnet build`
Pass: 0 errors.

## TC-002 — Endpoint exists on PlaybookController
Run: `grep -E 'HttpPost.*\{id\}/run|/run.*HttpPost' /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api/src/CloudManager.API/Controllers/PlaybookController.cs`
Pass: at least one match.
Fail: no match.

## TC-003 — Happy path creates run + N targets
Create playbook with content + 2 VMs (use existing fixtures). POST `/api/v1/playbook/{p}/run` `{ vmIds: [vm1, vm2], varsOverride: { foo: "bar" } }`.
Pass: HTTP 202 with `runId` starting with `run_`. Query DB: one `playbook_runs` row + 2 `playbook_run_targets` rows. Each target has `vars_override_snapshot = {"foo":"bar"}` and `assignment_id IS NULL`.
Fail: wrong status, wrong row counts, or wrong column values.

## TC-004 — Existing assignment inherits vars_override
Create a `VmPlaybookAssignment` for (vm1, p) with `vars_override = {"baz":"qux"}`. POST `/api/v1/playbook/{p}/run` `{ vmIds: [vm1, vm2], varsOverride: { foo: "bar" } }`.
Pass: target for vm1 has `vars_override_snapshot = {"baz":"qux"}` and non-null `assignment_id`; target for vm2 has `vars_override_snapshot = {"foo":"bar"}` and null `assignment_id`.
Fail: vm1's target doesn't carry the assignment's vars.

## TC-005 — RabbitMQ message published
Check the `playbook-runs` queue depth before and after a POST. (Use RabbitMQ management UI or `rabbitmqctl list_queues`.)
Pass: queue depth increased by 1 per POST.
Fail: no message landed.

## TC-006 — Validation errors
POST with empty `vmIds` → 400. POST with unknown vmId → 400. POST against a playbook with empty content → 400.
Pass: all three return 400 with `{ Message: ... }`.
Fail: any 2xx.

## TC-007 — Single-VM apply still works
POST to the existing `/api/v1/virtualmachine/{vmId}/playbook/{asgnId}/apply` endpoint.
Pass: still returns 202 with `runId` (unchanged behavior).
Fail: existing endpoint broken.

## TC-008 — Cleanup
Delete test runs, targets, assignment, and playbook.
