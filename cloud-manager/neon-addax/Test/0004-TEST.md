# Test 0004 — API Phase 3 (PlaybookRevision auto-create + read routes)

## TC-001 — dotnet build clean
Run: `cd /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api && dotnet build`
Pass: 0 errors.

## TC-002 — PlaybookRevisionController exists with RequireFeatureFlag
Run: `grep -l 'RequireFeatureFlag("playbooks")' /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api/src/CloudManager.API/Controllers/PlaybookRevisionController.cs`
Pass: file listed.
Fail: missing.

## TC-003 — PATCH on Playbook.content creates revision
Create playbook with `content: "---\nhosts: all\n"`. PATCH content to a new value.
Query `vm.playbook_revisions` for that playbook_id.
Pass: 1 row exists.
Fail: 0 or > 1.

## TC-004 — PATCH unchanged does NOT create revision
PATCH the same content again.
Pass: still 1 row.
Fail: 2 rows.

## TC-005 — List + get historic content
GET `/api/v1/playbook/{publicId}/revision` → array, newest first.
GET `/api/v1/playbook/{publicId}/revision/{revisionPublicId}` → original content.
Pass: both return correctly.
Fail: wrong order, wrong content, or wrong publicId prefix (must be `pbrev_`).

## TC-006 — Cleanup
Delete the playbook.
