# Test 0003 — API Phase 2 (revision write-side + read routes)

## TC-001 — dotnet build clean
Run: `cd /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api && dotnet build`
Pass: 0 errors.
Fail: any errors.

## TC-002 — RoleFileRevisionController exists with RequireFeatureFlag
Run: `grep -l 'RequireFeatureFlag("playbooks")' /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api/src/CloudManager.API/Controllers/RoleFileRevisionController.cs`
Pass: file listed.
Fail: file missing or attribute absent.

## TC-003 — PATCH file creates revision
Create role + file. PATCH file content to a NEW value.
Then query: `db_execute_sql_query SELECT COUNT(*) FROM vm.role_file_revisions WHERE role_file_id = (SELECT id FROM vm.role_files WHERE public_id = '<filePublicId>')`
Pass: count is 1.
Fail: 0 or > 1.

## TC-004 — PATCH with unchanged content does NOT create revision
PATCH the same content again.
Pass: count of revisions is still 1.
Fail: count is now 2.

## TC-005 — Three PATCHes → three revisions, monotonic numbering
PATCH 3 distinct contents in sequence. Query `revision_number` values.
Pass: revisions exist with `revision_number` 1, 2, 3.
Fail: any duplicate or gap.

## TC-006 — List endpoint returns newest first
GET `/api/v1/ansibleRole/{roleId}/file/{fileId}/revision`.
Pass: array of 3, first element has `revision_number: 3`.
Fail: wrong order or wrong count.

## TC-007 — Restore creates a new revision (does not delete)
POST `/api/v1/ansibleRole/{roleId}/file/{fileId}/revision/{revision1Id}/restore`.
Pass: HTTP 200 returns a new revision with `revision_number: 4` whose content matches revision 1's. Total revision count is now 4.
Fail: any other behavior.

## TC-008 — Cleanup
Delete the role.
