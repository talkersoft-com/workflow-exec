# Test 0008 — API Phase 7 (materialization endpoint)

## TC-001 — dotnet build clean
Run: `cd /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api && dotnet build`
Pass: 0 errors.

## TC-002 — Endpoint exists
Run: `grep -E 'materialized' /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api/src/CloudManager.API/Controllers/PlaybookController.cs`
Pass: at least one match referencing the materialization route.
Fail: no match.

## TC-003 — Returns expected shape
Create a global role with `tasks/main.yml` and `meta/argument_specs.yml` (with an `x-secret-path` annotation). Create a playbook, attach the role, give content. GET `/api/v1/playbook/{publicId}/materialized`.
Pass: response includes `playbook_public_id`, `playbook_revision_id`, `files` (array), `secret_inputs` (array). `files` contains the playbook content + role files. `secret_inputs` has 1 entry with the right `extravar_name` and `vault_path`.
Fail: missing fields or wrong values.

## TC-004 — Revision query param uses historic content
PATCH the playbook content to a new value (creates revision 2). GET with `?revision={oldRevisionPublicId}`.
Pass: `files` contains the OLD playbook content (matching revision 1), but the role files come from current head.
Fail: uses latest playbook content (ignored the query param).

## TC-005 — secret_inputs absent when no x-secret-path annotations
Test against a playbook with no `x-secret-path` anywhere.
Pass: `secret_inputs` is an empty array `[]`.
Fail: missing key or wrong type.

## TC-006 — Cleanup
Delete test rows.
