# Test 0007 — API Phase 6 (PlaybookRunTarget reads)

## TC-001 — dotnet build clean
Run: `cd /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api && dotnet build`
Pass: 0 errors.

## TC-002 — PlaybookRunTargetController exists with RequireFeatureFlag
Run: `grep -l 'RequireFeatureFlag("playbooks")' /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api/src/CloudManager.API/Controllers/PlaybookRunTargetController.cs`
Pass: file listed.

## TC-003 — DTOs serialize started_at + completed_at
Insert a synthetic `PlaybookRunTarget` row with non-null `started_at` and `completed_at`. GET `/api/v1/run/{runPublicId}/target/{targetPublicId}`.
Pass: response JSON includes both fields as ISO-8601 strings.
Fail: missing or wrong format.

## TC-004 — List endpoint omits bulky fields
GET `/api/v1/run/{runPublicId}/target` (list).
Pass: response array entries do NOT include `stats` or `output` (only summary fields).
Fail: bulky fields present (would mean using detail DTO for list).

## TC-005 — PATCH updates timestamps
PATCH a target with `{ status: "running", started_at: "2026-05-27T20:00:00Z" }`.
Pass: GET shows the updated values.
Fail: values unchanged.

## TC-006 — Cleanup
Delete the synthetic run + target rows.
