# Test 0000 — Workspace Verified

## TC-001 — All 14 repos on the same feature branch
Run `hv_status  deck: "cloud-manager"`.
Pass: every repo shows the same `[<branch>]` tag and that branch is not the
default branch (i.e. not `main`).
Fail: branches differ across repos, or every repo is on the default branch
(then Task 0000 should have called `hv_next`).

## TC-002 — All repos clean
Pass: hv_status reports `14/14 clean`.
Fail: any repo shows uncommitted changes, unpushed commits, or stash entries.

## TC-003 — Branch recorded in deck.md
Run: `grep -E '^## Branch' -A 1 /home/todd/workspace/cloud-manager/planning/execution-workflow/cloud-manager/neon-addax/deck.md`
Pass: line under `## Branch` contains a branch name in backticks, not the literal `TBD`.
Fail: line still says `TBD` or is missing.
