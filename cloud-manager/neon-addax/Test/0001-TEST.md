# Test 0001 — Migration AddAnsibleConstraintsAndTargetTimestamps

## TC-001 — Migration file exists
Run: `ls /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api/src/Models/CloudManager.Entities/Migrations/ | grep AddAnsibleConstraintsAndTargetTimestamps`
Pass: at least one matching file (the migration `.cs` + Designer).
Fail: no match.

## TC-002 — dotnet build clean
Run: `cd /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api && dotnet build`
Pass: exit 0, output "Build succeeded" with 0 Error(s).
Fail: any errors.

## TC-003 — started_at + completed_at columns present
Use the database-toolkit MCP: `db_get_table_schema` against `clouddb`, schema `vm`, table `playbook_run_targets`.
Pass: response includes `started_at` (timestamp with time zone, nullable) and `completed_at` (timestamp with time zone, nullable).
Fail: either column missing.

## TC-004 — Unique index on role_files (role_id, file_path)
Run via db_execute_sql_query against clouddb:
```
SELECT indexname FROM pg_indexes WHERE schemaname = 'vm' AND tablename = 'role_files' AND indexdef LIKE '%UNIQUE%';
```
Pass: at least one unique index covering both `role_id` and `file_path`.
Fail: no unique index covering both columns.

## TC-005 — Unique index on playbook_role_files (playbook_role_id, file_path)
Same query against `playbook_role_files`.
Pass: at least one unique index covering both `playbook_role_id` and `file_path`.
Fail: no matching index.
