# Test 0002 — hive-deck-pro Builds

## TC-001 — Clean build
Run: `CGO_ENABLED=0 go build ./...` from `/Users/talker/workspace/hive-deck-pro/hive-deck-pro/`
Pass: exits 0, no output.
Fail: any compile error printed to stderr.
