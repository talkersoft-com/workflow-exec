# Test 0001 — force_overwrite Implemented

## TC-001 — ForceOverwrite field exists in ClaudeSettings
Run: `grep 'ForceOverwrite' /Users/talker/workspace/hive-deck-pro/hive-deck-pro/internal/config/config.go`
Pass: line found containing `ForceOverwrite`.
Fail: no output.

## TC-002 — MaybeWrite uses the field
Run: `grep -n 'ForceOverwrite\|force_overwrite' /Users/talker/workspace/hive-deck-pro/hive-deck-pro/internal/claude/claude.go`
Pass: at least one match (the field is referenced in the overwrite logic).
Fail: no output.

## TC-003 — config.yaml.example documents force_overwrite
Run: `grep 'force_overwrite' /Users/talker/workspace/hive-deck-pro/hive-deck-pro/.hv/config.yaml.example`
Pass: line found with `force_overwrite: true`.
Fail: no output.
