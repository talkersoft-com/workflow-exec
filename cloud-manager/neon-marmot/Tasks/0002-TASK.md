# Task 0002 — Build hive-deck-pro

## Goal
Verify the implementation compiles cleanly.

## Target repo
`hive-deck-pro`

## Steps
1. Run: `CGO_ENABLED=0 go build ./...` from the hive-deck-pro repo root
2. Fix any compile errors before proceeding

## Definition of done
`CGO_ENABLED=0 go build ./...` exits 0 with no errors.
