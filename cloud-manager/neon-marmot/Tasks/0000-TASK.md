# Task 0000 — Initialize Workspace

## Goal
Verify the workspace is clean and confirm all repos are on the feature branch.

## Steps
1. Run `hv_status` on the deck (see `../deck.md` for the exact call)
2. All repos should already be on `neon-marmot` — confirm no repo is dirty
3. If any repo is dirty → STOP. Investigate before proceeding.
4. Record the branch name in `../deck.md` if not already filled in

## Definition of done
All repos on `neon-marmot`, all clean. Branch name recorded in deck.md.
