# Test 0002 — ops/ship.go Dispatch

## TC-001 — No AutoMerge bool references in ops/ship.go
```bash
grep -c "\.AutoMerge" /Users/talker/workspace/hive-deck-pro/hive-deck-pro/ops/ship.go
```
Pass: 0. Fail: > 0.

## TC-002 — PRMode switch present
```bash
grep -c "PRMode" /Users/talker/workspace/hive-deck-pro/hive-deck-pro/ops/ship.go
```
Pass: ≥ 1. Fail: 0.

## TC-003 — Build and tests pass
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro && CGO_ENABLED=0 go build ./... && CGO_ENABLED=0 go test ./...
```
Pass: exits 0, 0 failures. Fail: any error.
