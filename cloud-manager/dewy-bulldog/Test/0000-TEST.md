# Test 0000 — Workspace Verified

## TC-001 — hive-deck-pro clean
`hv_status deck: "hive-deck-pro"` — Pass: all clean. Fail: any dirty.

## TC-002 — cloud-manager clean
`hv_status deck: "cloud-manager"` — Pass: all clean on `dewy-bulldog`. Fail: dirty or wrong branch.
