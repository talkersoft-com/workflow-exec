# Test 0004 — MCP hv_await_merge

## TC-001 — await_merge.ts exists
```bash
ls /Users/talker/workspace/hive-deck-pro/hive-deck-pro/mcp/src/tools/await_merge.ts
```
Pass: exists. Fail: not found.

## TC-002 — hv_await_merge registered in index
```bash
grep -c "await_merge" /Users/talker/workspace/hive-deck-pro/hive-deck-pro/mcp/src/index.ts
```
Pass: ≥ 1. Fail: 0.

## TC-003 — BACKGROUND TASK in descriptor
```bash
grep -c "BACKGROUND TASK" /Users/talker/workspace/hive-deck-pro/hive-deck-pro/mcp/src/tools/await_merge.ts
```
Pass: 1. Fail: 0.

## TC-004 — MCP build passes
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro/mcp && npm run build 2>&1
```
Pass: exits 0. Fail: TypeScript errors.

## TC-005 — dist file exists
```bash
ls /Users/talker/workspace/hive-deck-pro/hive-deck-pro/mcp/dist/tools/await_merge.js
```
Pass: exists. Fail: not found.
