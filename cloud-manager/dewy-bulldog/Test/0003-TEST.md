# Test 0003 — ops/await_merge.go + Contract

## TC-001 — await_merge.go exists
```bash
ls /Users/talker/workspace/hive-deck-pro/hive-deck-pro/ops/await_merge.go
```
Pass: exists. Fail: not found.

## TC-002 — RunAwaitMerge defined
```bash
grep -c "func RunAwaitMerge" /Users/talker/workspace/hive-deck-pro/hive-deck-pro/ops/await_merge.go
```
Pass: 1. Fail: 0.

## TC-003 — await_merge in dispatch
```bash
grep -c '"await_merge"' /Users/talker/workspace/hive-deck-pro/hive-deck-pro/ops/dispatch.go
```
Pass: 1. Fail: 0.

## TC-004 — CONTRACT.md updated
```bash
grep -c "await_merge" /Users/talker/workspace/hive-deck-pro/hive-deck-pro/ops/contract/CONTRACT.md
```
Pass: ≥ 1. Fail: 0.

## TC-005 — Build passes
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro && CGO_ENABLED=0 go build ./...
```
Pass: exits 0. Fail: any error.
