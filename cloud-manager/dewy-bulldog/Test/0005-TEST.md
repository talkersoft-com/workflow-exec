# Test 0005 — Configs + Final

## TC-001 — Mac config uses pr_mode
```bash
grep "pr_mode" ~/.hv/config.yaml
```
Pass: line found. Fail: missing (still using old booleans).

## TC-002 — Full test suite passes
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro && CGO_ENABLED=0 go test ./...
```
Pass: 0 failures. Fail: any failure.

## TC-003 — JSON status still works
```bash
echo '{"op":"status","deck":"cloud-manager"}' | hv 2>&1 | tail -3
```
Pass: shows clean repo count. Fail: parse error or empty output.

## TC-004 — Results and Lessons exist
```bash
ls /Users/talker/workspace/cloud-manager/planning/workflow-exec/cloud-manager/dewy-bulldog/Results/RESULT.md
ls /Users/talker/workspace/cloud-manager/planning/workflow-exec/cloud-manager/dewy-bulldog/Retro/LESSONS.md
```
Pass: both exist. Fail: either missing.

## TC-005 — hive-deck-pro shipped
`hv_ship deck: "hive-deck-pro"` — Pass: PR merged. Fail: error.

## TC-006 — cloud-manager planning shipped
`hv_ship deck: "cloud-manager"` — Pass: workflow-exec PR merged. Fail: error.
