# Test 0001 — Config Struct

## TC-001 — PRMode type exists
```bash
grep -c "type PRMode string" /Users/talker/workspace/hive-deck-pro/hive-deck-pro/internal/config/config.go
```
Pass: 1. Fail: 0.

## TC-002 — Three constants defined
```bash
grep -c "PRMode" /Users/talker/workspace/hive-deck-pro/hive-deck-pro/internal/config/config.go
```
Pass: ≥ 4 (type + 3 constants + field). Fail: fewer.

## TC-003 — AutoMerge bool removed
```bash
grep -c "AutoMerge.*bool" /Users/talker/workspace/hive-deck-pro/hive-deck-pro/internal/config/config.go
```
Pass: 0. Fail: > 0.

## TC-004 — Build passes
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro && CGO_ENABLED=0 go build ./...
```
Pass: exits 0. Fail: any error.

## TC-005 — Old config still parses
```bash
echo 'ship:\n  auto_merge: true\n  require_merged_pr: true' | CGO_ENABLED=0 go run ./cmd/hv/ status cloud-manager 2>&1 | grep -v "error"
```
Pass: no parse error. Fail: "parse error" or "cannot unmarshal".
