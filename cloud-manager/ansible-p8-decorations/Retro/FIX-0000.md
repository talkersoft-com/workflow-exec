# FIX-0000 — Task 0000 series-order gate failed: P4 and P5 not merged

## Failure
Test `0000-TEST.md` TC-004 (dependency contracts present) **fails**.

Verified against `origin/main` (fetched 2026-06-12) and the deployed API
(`https://ubuntu-server.talkersoft.com`):

| Prerequisite | Evidence | Status |
|---|---|---|
| **P7 C-ED editor** | `cloud-manager-web` origin/main has `src/components/AnsibleYamlEditor/` with `decorations` + `hoverProvider` props, decorations compartment, `jinjaHover` bridge, extension-point tests | ✅ merged |
| **P4 C-VAR typing (`vdef`/`sref`/`varev`)** | No `VariableDefinition`/`SecretRef` entity, no controller, no `api/v1/variable` route anywhere on cloud-manager-api origin/main (`git grep` over all of origin/main: zero hits for `VariableDefinition`, `api/v1/variable`); deployed API returns 404 | ❌ **missing** |
| **P5 C-RES preview** | No resolution controller/service/route on cloud-manager-api origin/main (zero hits for `api/v1/resolution`); deployed `POST api/v1/resolution/preview` returns 404 | ❌ **missing** |

cloud-manager-api main history confirms the merged series so far is
P1 (PR #28) → P2 (PR #29) → P3 (PR #30) → P6 (PR #31); P7 merged web-side.
P4 and P5 were skipped:

- `workflow-exec/cloud-manager/ansible-p5-resolution/` stopped at its own Task 0000 with
  `Retro/FIX-0000.md` ("P4 not merged", branch `mushy-walleye`) — the exact failure mode
  this task's step 2 cites.
- `workflow-exec/cloud-manager/ansible-p4-variables/` has since been scaffolded
  (PLAN.md now exists in workflow-plans) but was never executed — no Results/, no Retro/,
  deck.md branch still TBD.
- The epic-wide `ansible-studio` feature flag exists on the deployed API but is
  **disabled** — and flag state is moot while the routes themselves don't exist.

## Why this blocks P8
P8 is a pure consumer of all three contracts (init.md constraints; contracts §10 row P8):
`useVarDecorations` folds C-VAR `vdef` classification into decoration kinds, and the hover
provider renders C-RES preview responses verbatim. With no `api/v1/variable` and no
`api/v1/resolution/preview` there is nothing to fetch, nothing to classify against, and no
provenance to render — and the no-heuristics constraint ("classification comes from C-VAR
records only, no name-pattern guesses") forbids any stand-in. Code changes are restricted
to cloud-manager-web; building the missing API surfaces here is out of scope by
construction.

## Action taken
- Task 0000 steps 1, 3–6 completed: deck verified clean on feature branch `soft-heater`,
  branch `fawn-parrotfish` minted via hv_next across all 18 repos, hv_status re-run
  (18/18 clean on `fawn-parrotfish`), branch recorded in `deck.md`
  (TC-001/TC-002/TC-003 pass).
- P7/C-ED verified present on origin/main (the one prerequisite that holds).
- Task 0000 box left **unchecked**; TC-004 prescribes: "any of the three contract
  surfaces missing — the workflow must have stopped at Task 0000 step 2 rather than
  proceeding."
- No retry possible at this layer: the fix is to execute + merge `ansible-p4-variables`,
  then `ansible-p5-resolution`, enable/verify the `ansible-studio` flag surfaces, then
  re-run this workflow from Task 0000.

## Resolution
STOPPED — surfaced to operator per Task 0000 step 2 and the improvisation policy
(prerequisite gate, not a recoverable implementation failure).
