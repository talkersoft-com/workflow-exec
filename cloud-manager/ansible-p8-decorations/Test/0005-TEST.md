# Test 0005
TC-001 no-context page: an editor page with no context selected renders classification-only
decorations, hover shows definition info, and the network layer records zero
resolution-preview calls. TC-002 unregistered silence: an unregistered variable renders with
ordinary template coloring and only the quiet tooltip hint — no badges, no registration
dialogs or harvest UX. TC-003 degradation: with (a) an empty registry, (b) a C-RES endpoint
returning an error, and (c) no P4/P5 data at all, the editor pages render and edit normally
with zero console errors and no error toasts attributable to absent metadata. TC-004 perf:
rapid typing triggers re-scan only at the 300ms debounce; repeated hovers in one context
produce exactly one preview call per revision; editor input latency is not measurably worse
than the pre-P8 baseline. TC-005 C-DECO export: `useVarDecorations`, `<SecretValue>`, and the
context-bar component import cleanly from their public module paths in an isolation test.
TC-006 P7 regression: dirty tracking, Cmd/Ctrl-S save, change-summary flow, beforeunload, and
lint diagnostics behave exactly as before P8; both `[data-theme]` values render correctly.
TC-007 `--target web` build passes cleanly.
