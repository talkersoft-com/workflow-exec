# Test 0004
TC-001 `<SecretValue>` contract: renders `***` + lock chip for a secret-classified name and
the plain value for an unclassified/plain name. TC-002 ref-only writes: in each vars editor
(assignment, blueprint, host/group var forms) a secret-classified row renders masked with the
lock chip and the "set via vault ref" affordance; using the affordance writes the
`$secretRef` form to the API — capture the request and assert no literal secret value is ever
sent. TC-003 run vars panel: the panel's data source is `rsnap`, not raw `varsSnapshot` —
assert the raw snapshot is no longer referenced by the panel and rendered values arrive
masked. TC-004 run detail chips: published-secret paths in `RunDetailPage` render lock chips.
TC-005 audit grep: the documented grep for var-value render sites outside `<SecretValue>`
returns zero hits across cloud-manager-web. TC-006 isolation: `git diff` shows no changes to
cloud-manager-api, cloud-manager-mcp, vorch, or porch. TC-007 `--target web` build passes
cleanly.
