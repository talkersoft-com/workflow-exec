# Test 0002
TC-001 secret rendering: a registered secret variable shows the lock badge and the secret
color token in the editor; a plain registered variable shows ordinary template coloring with
no badge. TC-002 required rendering: a required variable shows the required badge; without a
selected context NO missing-state gutter dot appears (missingness is contextual). TC-003
vaultRef rendering: `$secretRef` values and `vault_*` lookups show the vault icon. TC-004
theme correctness: flipping `[data-theme]` re-resolves every decoration color from `--color-*`
tokens; grep decoration styling for hardcoded hex colors not sourced from tokens — zero hits.
TC-005 graceful degradation: a document whose owner has an empty C-VAR registry renders with
zero decorations and zero console errors. TC-006 isolation: `git diff` shows the P7 editor
component's files untouched — decorations arrive exclusively through the C-ED `decorations`
prop from the consumer pages. TC-007 `--target web` build passes cleanly.
