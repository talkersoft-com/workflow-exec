# Test 0001
TC-001 batched fetch: opening a document triggers exactly one C-VAR definitions call for the
owner closure; a re-render or re-scan at the same revision triggers zero additional calls
(cache hit); a revision bump triggers exactly one new call. TC-002 classification fidelity:
a registered secret variable yields `kind: "secret"`, a registered required variable yields
`kind: "required"`, a `$secretRef` value / `vault_*` lookup yields `kind: "vaultRef"` — each
decoration conforms to the C-ED `EditorDecoration` shape (contracts §9). TC-003 no
heuristics: an UNREGISTERED variable named `db_password` or `api_token` receives NO
decoration — grep the hook for name-pattern matching on sensitivity-suggesting substrings —
zero hits; unregistered names are surfaced on the hook result for the tooltip hint only.
TC-004 re-scan cadence: editing the document re-emits decorations after the 300ms debounce,
not on every keystroke. TC-005 isolation: `git diff` shows no changes under the P7 editor
component's files and none outside cloud-manager-web. TC-006 `--target web` build passes
cleanly.
