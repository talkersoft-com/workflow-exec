# Test 0006
TC-001 deploy verified: deploy-cloud-manager.py --target web completed; the smoke poll
returned HTTP 200; the deployed UI at https://ubuntu-server.talkersoft.com serves the new
behavior (decorations visible, hover provenance with context, masked secrets on vars + run
pages). TC-002 outputs before ship: `Results/RESULT.md` and `Retro/LESSONS.md` exist in the
ship commit (not added after), with deploy target + smoke-test results recorded. TC-003 PRs:
an exec-stage PR exists for every deck repo ahead of origin/main (at minimum cloud-manager-web
and workflow-exec); verified via `gh pr list`/`gh pr view`, not hv_ship output alone; the PR
table in RESULT.md lists real links + merge state. TC-004 scope: the cloud-manager-web PR
touches only cloud-manager-web files; no PR exists for cloud-manager-api, cloud-manager-mcp,
vorch, or porch. TC-005 every box in `../Execution/Exec.md` is checked.
