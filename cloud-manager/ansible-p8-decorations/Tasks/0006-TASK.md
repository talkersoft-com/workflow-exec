# Task 0006 — Final: deploy, results, retro, ship

## Steps
1. Confirm Tasks 0000–0005 are all checked in `../Execution/Exec.md` and their tests passed.
2. Build everything that changed: `python3 .cicd/build-cloud-manager.py --target web`
   (web-only plan; api/mcp must be untouched — verify with `git diff` across the deck).
3. Deploy: `python3 .cicd/deploy-cloud-manager.py --target web`. Record smoke-test attempt
   counts + HTTP codes. A failed deploy is a stop condition — write `Retro/FIX-NNN.md`,
   resolve, retry.
4. Verify the deployed UI at https://ubuntu-server.talkersoft.com (NOT localhost:3000, which
   has no API proxy): decorations render on an editor page, hover provenance works with a
   chosen context, a secret value renders masked in a vars editor and on a run page.
5. Write `Results/RESULT.md` — outcome, branch, PR table (TBD rows filled from hv_ship
   output), per-phase summary, deploy target + smoke-test results.
6. Write `Retro/LESSONS.md` — what would have helped, FIX file index (or a single line if
   nothing went wrong).
7. Ship (see `../deck.md`):
   ```
   hv_ship  deck: "cloud-manager"
            message: "feat: semantic decorations — secret/required badges, hover provenance, masking (P8)"
            title:   "Ansible Epic P8: metadata-driven decorations + provenance hover + masking"
            stage:   "exec"
   ```
8. Record the PR links in `Results/RESULT.md`. Auto-merge is on for this deck — hv_ship
   output may omit PR lines; verify the PRs and their merge state with `gh` and fill the
   table from that. Check the final box in `../Execution/Exec.md` only after the PR table is
   real.

## Definition of done
Web deployed and smoke-tested, RESULT.md + LESSONS.md written BEFORE the ship call, exec-stage
PRs opened (and merge state recorded) for every repo ahead of main, PR table filled.
