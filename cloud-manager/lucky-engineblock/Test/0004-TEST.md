# Test 0004 — MCP marketplace tools

## TC-001 — Build green
`python3 .cicd/build-cloud-manager.py --target mcp` passes; dist rebuilt without lockfile drift.

## TC-002 — Tools in dist
The built dist registers all 12 tools (`cloud_blueprint_list/get/create/update/delete`,
`cloud_blueprint_publish/archive`, `cloud_blueprint_playbook_attach/detach/reorder`,
`cloud_marketplace_list`, `cloud_marketplace_provision`) under the `hv.cloud` profile.
Verify by grepping the dist or running the server's tool-list locally.

## TC-003 — Contract conformance
Tool input schemas use camelCase keys and public ids; `assertCamelCaseKeys` (or the repo's
equivalent guard) applied on payloads; error handling matches `vms.ts` patterns.

## TC-004 — Smoke against live API (flag enabled)
With the API from Phase 0002 running, invoke the dist directly (node script or MCP inspector —
the operator-reloaded MCP session is NOT required for this test):
`cloud_marketplace_list` returns the seeded published blueprints; `cloud_blueprint_get` on
`postgres-jammy` shows its ordered playbook.
