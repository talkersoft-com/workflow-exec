# Test 0000 — Workspace Initialized

## TC-001 — All repos on the same feature branch
Run `hv_status` on the deck.
Pass: every repo shows the same feature branch name; no repo on the default branch.
Fail: mixed branches or any repo on the default branch.

## TC-002 — Repos clean (scaffold excepted)
Pass: no dirty repos other than workflow-exec carrying this orchestration's own scaffold files.
Fail: any other uncommitted changes, unpushed commits, or stash entries.

## TC-003 — Branch recorded
Open `../deck.md`.
Pass: "## Branch" carries the generated branch name (not TBD) and matches hv_status.
Fail: still TBD or mismatched.
