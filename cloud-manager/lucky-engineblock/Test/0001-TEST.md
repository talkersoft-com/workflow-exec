# Test 0001 — Data model + migration + seeds

## TC-001 — API build green
Run `python3 .cicd/build-cloud-manager.py --target api`.
Pass: build completes with zero errors.

## TC-002 — Migration is reversible
On a disposable local database (db toolkit `clouddb_local` or equivalent), run
`dotnet ef database update` to HEAD, then `dotnet ef database update <previous-migration>`,
then back to HEAD.
Pass: all three transitions succeed; Down() leaves no orphan objects (marketplace schema tables
and vm.virtual_machines.blueprint_id are gone after the down step).

## TC-003 — Schema shape
Inspect the migrated database (`db_get_table_schema` / psql):
Pass: `marketplace.blueprints`, `marketplace.blueprint_playbooks`, `marketplace.blueprint_events`
exist with the columns, FKs, and unique constraints from the task (incl. filtered unique on
blueprint name where deleted_at is null; unique (BlueprintId, PlaybookId) and
(BlueprintId, Position) on the join); `vm.virtual_machines.blueprint_id` exists, nullable, FK
Restrict.

## TC-004 — Prefixes registered
Pass: `EntityPrefixRegistry` maps Blueprint→`bp`, BlueprintPlaybook→`bpp`, BlueprintEvent→`bpev`,
and created rows get public ids with those prefixes (verify via seed rows).

## TC-005 — Seeds present
Query the seeded database (psql).
Pass: blueprints `jammy` (0 playbooks) and `postgres-jammy` (1 playbook: pg-14-jammy at
position 0) exist with Status=Published and valid `bp_` public ids; feature flag `marketplace`
exists and is disabled.

## TC-006 — Existing VM surface untouched
Pass: VM list/get API responses are byte-identical in shape to before (BlueprintId is internal;
no new field leaks unless deliberately added per plan); existing migrations untouched.
