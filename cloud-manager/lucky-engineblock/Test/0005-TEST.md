# Test 0005 — End-to-end verification

All TCs run against the DEPLOYED api/mcp dist with the `marketplace` flag enabled.

## TC-001 — Deploy healthy
deploy script smoke tests passed (api: /api/v1/Host poll OK; mcp: build OK). cloud_health_check
(or GET /health) returns healthy.

## TC-002 — jammy end-to-end (degenerate case)
Instantiate `jammy` from the marketplace (API or MCP dist) on a live host → VM provisions through
the normal orchestration path and reaches OrchestrationStatus=Completed; zero playbook runs
enqueued; `instantiated` events present (BlueprintEvent + VmEvent).

## TC-003 — postgres-jammy end-to-end (ordered auto-run)
Instantiate `postgres-jammy` → VM provisions; after Completed the sequencer auto-enqueues the
pg-14-jammy run (position 0) WITHOUT a manual apply; the run reaches Succeeded on the live VM;
no further runs enqueued afterwards.

## TC-004 — Direct attach/apply regression
On a wizard-created (non-blueprint) VM: attach a playbook, apply it, detach — identical behavior
and response shapes to pre-feature. BlueprintId remains null throughout.

## TC-005 — Marketplace listing
`GET /api/v1/marketplace` (and `cloud_marketplace_list` via dist) lists exactly the Published
blueprints with name, description, image summary, playbook count.

## TC-006 — Artifacts
`Results/RESULT.md` exists with outcome/deploy/smoke details + operator /mcp instruction;
`Retro/LESSONS.md` exists; every ORCH.md checkbox is checked before hv_ship.
