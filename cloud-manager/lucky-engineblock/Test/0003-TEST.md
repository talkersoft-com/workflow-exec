# Test 0003 — Sequencer logic

## TC-001 — Build + unit tests
`python3 .cicd/build-cloud-manager.py --target api` and the api test suite.
Pass: green, including the new sequencer derivation tests covering: next = first
non-Succeeded position; chain complete when all Succeeded; Failed stops (no further enqueue);
VM without BlueprintId is a strict no-op; empty blueprint (no playbooks) completes immediately.

## TC-002 — Writeback path safety
Code review check (grep-level): both hook sites wrap sequencer calls in try/catch and the original
writeback persists state BEFORE the sequencer is invoked.
Pass: a thrown sequencer exception cannot prevent the status update or the AMQP ack.

## TC-003 — No vorch/porch diffs
`git -C <deck>/vm-infra/cloud-manager/vorch-lib status --porcelain` and same for vorch-service —
plus `git diff --stat origin/main...HEAD` in both.
Pass: zero changes in both repos.
