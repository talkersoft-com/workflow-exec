# Task 0001 — Data model, migration, seeds

## Goal
The three new entities + VM provenance column exist in EF Core and the database, with seeds and
the feature flag, exactly per PLAN.md's Design tables.

## Steps (cloud-manager-api)
1. Entities in `src/Models/CloudManager.Entities/Models/`: `Blueprint.cs`, `BlueprintPlaybook.cs`,
   `BlueprintEvent.cs` — field-for-field from PLAN.md (Audit base; Blueprint has DeletedAt;
   BlueprintEvent is append-only, no DeletedAt). New enum `BlueprintStatus` (Draft|Published|
   Archived) in `Enums/` with the Npgsql enum annotation pattern used by existing enums.
2. `VirtualMachine.cs`: add nullable `BlueprintId` (Guid?, FK → Blueprint, Restrict). Nothing else.
3. Register prefixes in `PublicId/EntityPrefixRegistry.cs`: Blueprint→`bp`, BlueprintPlaybook→`bpp`,
   BlueprintEvent→`bpev`.
4. `CloudManagerDbContext.cs`: DbSets + OnModelCreating config — schema `marketplace`, tables
   `blueprints`, `blueprint_playbooks`, `blueprint_events`; Blueprint unique Name filtered
   `deleted_at IS NULL` + global query filter (copy the VirtualMachine pattern); BlueprintPlaybook
   unique(BlueprintId,PlaybookId) + unique(BlueprintId,Position) + index(BlueprintId,Position),
   Cascade from Blueprint, Restrict to Playbook; BlueprintEvent indexes (BlueprintId,OccurredAt
   DESC) and (EventType, OccurredAt DESC).
5. Migration: `dotnet ef migrations add AddMarketplaceBlueprints` — review generated Up/Down by
   hand; Down must cleanly drop the three tables, the VM column, and the enum.
6. Seeds (existing seed mechanism in `seed/`): `marketplace` feature flag row (disabled);
   blueprint `jammy` (the ubuntu jammy `vmi`, zero playbooks, Published, defaults 2048MB/10GB);
   blueprint `postgres-jammy` (same vmi + the seeded `pg-14-jammy` playbook at position 0,
   Published). Resolve the vmi/playbook by name at seed time — do not hardcode GUIDs.
7. Build: `python3 .cicd/build-cloud-manager.py --target api` must pass.

## Definition of done
Build green; migration reviewed (Up and Down); seeds idempotent (safe to run twice).
