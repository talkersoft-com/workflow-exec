# Task 0004 — MCP marketplace tools

## Goal
`cloud-manager-mcp` exposes the blueprint/marketplace surface as MCP tools, registered in the
`hv.cloud` profile, dist rebuilt.

## Repo
`cloud-manager-mcp` (`/home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-mcp`)

## Steps
1. Study `src/tools/vms.ts` for the client/error/camelCase patterns (`assertCamelCaseKeys`).
2. New `src/tools/marketplace.ts` with tools:
   - `cloud_blueprint_list`, `cloud_blueprint_get`, `cloud_blueprint_create`,
     `cloud_blueprint_update`, `cloud_blueprint_delete`
   - `cloud_blueprint_publish`, `cloud_blueprint_archive`
   - `cloud_blueprint_playbook_attach`, `cloud_blueprint_playbook_detach`,
     `cloud_blueprint_playbook_reorder`
   - `cloud_marketplace_list`
   - `cloud_marketplace_provision` (instantiate)
3. Register the tools in the `hv.cloud` profile.
4. camelCase payloads; public ids only; same error handling as the existing tool files.
5. Build: `python3 .cicd/build-cloud-manager.py --target mcp` — must pass cleanly (dist rebuilt).

## Definition of done
All 12 tools present in the built dist, profile-registered, payload shapes matching the API
contract; mcp build green. Run `Test/0004-TEST.md`.

> Note for RESULT.md: the running MCP server needs an operator `/mcp` reload to pick up the new
> dist — record this as an operator action.
