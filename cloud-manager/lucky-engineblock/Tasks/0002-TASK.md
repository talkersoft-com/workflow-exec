# Task 0002 — Blueprint + Marketplace API

## Goal
The full HTTP surface from PLAN.md exists, feature-flagged, following the Playbook/
PlaybookAssignment exemplar stack (DTOs, AutoMapper profile, service, controller).

## Steps (cloud-manager-api)
1. DTOs in `src/Models/CloudManager.DTO/Models/`: Blueprint (public-id wire shape incl. status,
   image public id, defaults), BlueprintPlaybook (position, varsOverride, playbook public id +
   name), marketplace listing item (name, description, image summary from vmi config, playbook
   count), instantiate request/response.
2. AutoMapper profiles (Guid↔PublicId pattern from the Playbook profile).
3. `IBlueprintService`/`BlueprintService`: CRUD with soft-delete rules (delete only
   Draft/Archived), lifecycle transitions (Draft→Published→Archived, Archived→Published) each
   recording a BlueprintEvent, ordered playbook attach/detach/reorder (reorder renumbers
   atomically in one transaction; position defaults to end on attach).
4. `IMarketplaceService`/`MarketplaceService`: published listing; `InstantiateAsync(blueprintId,
   req)` — transactional: create VM via the EXISTING VM-create path (same MAC/IP allocation,
   VmEvent logging; overrides win over blueprint defaults; set VM.BlueprintId), copy each
   BlueprintPlaybook → VmPlaybookAssignment (VarsOverride copied), trigger provision via existing
   `IProvisionService` flow, record `instantiated` BlueprintEvent + VmEvent. Returns
   `{virtualMachineId, runPlan: [assignment public ids in position order]}`.
5. Controllers: `BlueprintController` (`/api/v1/blueprint`) and `MarketplaceController`
   (`/api/v1/marketplace`) with the exact routes/verbs from PLAN.md, every endpoint under
   `[RequireFeatureFlag("marketplace")]`. camelCase wire, public ids only.
6. DI registration in Program.cs alongside the existing services.
7. Build: `--target api` green. NO sequencer in this task (that is 0003); runs happen via the
   existing explicit apply.

## Definition of done
Build green; all endpoints respond correctly against the scratch DB with the flag enabled and 404/
gated with it disabled; instantiate produces VM + assignments + provision message without touching
the direct-attach code paths.
