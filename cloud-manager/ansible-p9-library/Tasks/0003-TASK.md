# Task 0003 — Part B: role-interface composer (cloud-manager-web repo)

## Steps
1. RESEARCH (read, no changes): contracts §7 (C-VAR) — role-scoped `vdef` records
   (kind: required/default/internal, sensitivity); contracts §8 (C-RES) — the dry-resolve /
   preview call and its gap reporting; the blueprint-attach interaction pattern (ordered
   role list UX to mirror); existing `@rjsf/core` usage (read-only argument schemas) and the
   playbook create endpoints; C-DECO's `<SecretValue>` and secret-ref-picker exports from P8.
2. Build `/ansible/compose`: role search/pick → ordered list (reorder/remove, mirroring the
   blueprint-attach pattern).
3. Per picked role, render its C-VAR interface as a validated form: generate an rjsf schema
   from the role's definitions — `required` fields prominent, `default` fields collapsed
   behind an override affordance, `internal` fields hidden entirely. Secret-classified params
   render through C-DECO's `<SecretValue>`/ref-picker and write the `$secretRef` form ONLY —
   no secret value ever enters component state or the wire.
4. Output path: save creates a NEW playbook via existing create endpoints, with vars written
   through P4's typed forms. Recompose-existing is out of scope (plan open question 2 default
   — explicit deferral).
5. Validate before save: every `required` param satisfied in the form OR explicitly deferred
   to inventory/runtime layers — gap-awareness via a C-RES dry resolve; unresolved required
   params block save with a clear per-field message.
6. Unit tests: schema generation per kind (required/default/internal placement), secret params
   produce `$secretRef` payloads only, validate-before-save blocks on gaps and passes when
   deferred, save payload matches the existing create-endpoint contract.
7. Build: `python3 .cicd/build-cloud-manager.py --target web` must pass.

Done when: composing roles yields a valid new playbook with typed vars, required-param
validation backed by C-RES, and ref-only secrets; web build clean. No API changes in this task.
