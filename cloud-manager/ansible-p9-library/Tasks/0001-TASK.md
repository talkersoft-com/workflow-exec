# Task 0001 — Part A: role library metadata (cloud-manager-api repo — the only non-web diff)

## Steps
1. RESEARCH (read, no changes): the `AnsibleRole` entity, its DTOs, and the list/get/patch
   endpoints in cloud-manager-api; contracts §1 (P9 introduces NO new entities or prefixes —
   `ansr` unchanged); how existing string-array columns are mapped in EF (for
   `Tags`/`SupportedPlatforms`); the project's camelCase + public-id conventions.
2. Add additive fields to `AnsibleRole`: `Tags string[]`, `SupportedPlatforms string[]`,
   `Maintainer`, `Version`. Generate an EF Core migration on `ansible_roles` — additive only,
   no destructive column changes, existing rows get empty/null defaults.
3. Extend DTOs (response + PATCH request) with the new fields; omitted PATCH fields leave
   values unchanged. camelCase on the wire; public_ids only — no Guid leakage.
4. Extend the role list endpoint with filters `?tag=&platform=&q=` (q matches name/description;
   tag and platform match membership in the new arrays). Filters compose; absent filters
   return the unfiltered list exactly as today.
5. Unit/integration tests: migration applies cleanly; PATCH round-trips each new field and
   leaves omitted fields untouched; each filter and their combination returns the right rows;
   a request with no filters is byte-identical to the pre-change response shape plus the new
   fields.
6. Build: `python3 .cicd/build-cloud-manager.py --target api` must pass.

Done when: `ansible_roles` carries the four metadata fields end-to-end (entity → migration →
DTOs → PATCH → list filters), api build clean. No web, MCP, or vorch changes in this task; no
new entities or prefixes.
