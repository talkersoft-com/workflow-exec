# Task 0006 — Final: migrate, deploy, results, retro, ship

## Steps
1. Confirm Tasks 0000–0005 are all checked in `../Execution/Exec.md` and their tests passed.
2. Build everything that changed: `python3 .cicd/build-cloud-manager.py --target all`
   (api + web changed in this plan; mcp/vorch must be untouched — verify with `git diff`
   across the deck).
3. Apply the Task 0001 EF migration to the live DB BEFORE deploying the api (a restarted
   service against the old schema fails to start):
   ```bash
   cd /home/todd/workspace/cloud-manager/vm-infra/cloud-manager/cloud-manager-api/src/Models/CloudManager.Entities && \
     dotnet ef database update \
       --connection "Host=localhost;User ID=cloudmanager;Password=P@ssw0rd!;Port=5432;Database=clouddb;SslMode=Prefer;Trust Server Certificate=true"
   ```
4. Deploy: `python3 .cicd/deploy-cloud-manager.py --target api` then `--target web`. Record
   smoke-test attempt counts + HTTP codes. A failed deploy is a stop condition — write
   `Retro/FIX-NNN.md`, resolve, retry.
5. Verify the deployed UI at https://ubuntu-server.talkersoft.com (NOT localhost:3000, which
   has no API proxy): library browser searches with facets and used-by counts, graph view
   renders, composer builds a playbook with masked secret refs, preview shows provenance +
   gaps, a delete on a consumed role shows the used-by modal, run console tail-follows.
6. Write `Results/RESULT.md` — outcome, branch, PR table (TBD rows filled from hv_ship
   output), per-phase summary, migration + deploy targets + smoke-test results.
7. Write `Retro/LESSONS.md` — what would have helped, FIX file index (or a single line if
   nothing went wrong).
8. Ship (see `../deck.md`):
   ```
   hv_ship  deck: "cloud-manager"
            message: "feat: library browser, composer, preview, guardrails, run console (P9)"
            title:   "Ansible Epic P9: library UX — browse, compose, preview, guard"
            stage:   "exec"
   ```
9. Record the PR links in `Results/RESULT.md`. Auto-merge is on for this deck — hv_ship
   output may omit PR lines; verify the PRs and their merge state with `gh` and fill the
   table from that. Check the final box in `../Execution/Exec.md` only after the PR table is
   real.

## Definition of done
Migration applied, api + web deployed and smoke-tested, RESULT.md + LESSONS.md written BEFORE
the ship call, exec-stage PRs opened (and merge state recorded) for every repo ahead of main,
PR table filled.
