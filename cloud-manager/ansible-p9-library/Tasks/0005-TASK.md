# Task 0005 — Part B: run console tail-follow + regression sweep (cloud-manager-web repo)

## Steps
1. RESEARCH (read, no changes): P6's chunk endpoint `GET …/log/chunk?afterSeq=` and chunk
   content shape; current `RunDetailPage` polling; how run targets report status/progress;
   how check-mode (`mode: "check"`) is flagged on a run.
2. Upgrade `RunDetailPage` to tail-follow log chunks: poll `afterSeq` on a 2s cadence (plan
   open question 4 default — SignalR hub push is an explicit follow-up, not this task),
   appending incrementally without re-fetching history; per-task grouping derived from chunk
   content; per-target status/progress visible while running.
3. Check-mode labelling: runs with `mode: "check"` are labelled "dry-run — would change"
   wherever run results render.
4. Regression sweep: existing `/ansible/*` CRUD pages and blueprint/marketplace pages must be
   byte-identical to pre-P9 — verify via `git diff` that none of their files changed; where a
   shared file had to change, justify each hunk in the task notes and confirm rendered output
   is unchanged.
5. E2E: composer → preview → run — compose a playbook from library roles, satisfy required
   params, preview per-target effective config, trigger a run, watch the console tail-follow
   to completion. Also run an existing pre-P9 playbook to confirm no regression in the run
   path.
6. Unit tests: afterSeq pagination (no duplicate or skipped chunks), per-task grouping,
   check-mode label presence, 2s poll cancellation on unmount.
7. Build: `python3 .cicd/build-cloud-manager.py --target web` must pass.

Done when: the run console tail-follows chunks with task grouping and dry-run labelling, the
e2e composer→preview→run path works, and existing pages are demonstrably untouched; web build
clean.
