# Test 0005
TC-001 tail-follow: during a live run the console appends new chunks via `afterSeq` polling
(2s) without duplicating or skipping sequences and without re-fetching already-rendered
history; polling stops on unmount/navigation. TC-002 grouping + status: chunks group per task
and per-target status/progress updates while the run is in flight. TC-003 check-mode: a
`mode: "check"` run renders the "dry-run — would change" label; a normal run does not.
TC-004 regression: `git diff origin/main` over cloud-manager-web shows NO changes to existing
`/ansible/*` CRUD, blueprint, or marketplace page files (any shared-file hunk is justified in
task notes with unchanged rendered output); no SignalR hub changes. TC-005 e2e: composer →
preview → run completes against the live system, and a pre-P9 playbook still runs and renders
correctly. TC-006 `--target web` build passes cleanly.
