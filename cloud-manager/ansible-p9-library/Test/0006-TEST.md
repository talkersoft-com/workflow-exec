# Test 0006
TC-001 migration + deploy verified: the EF migration is applied to live clouddb before the
api restart; deploy-cloud-manager.py ran for api and web; smoke polls returned HTTP 200; the
deployed UI at https://ubuntu-server.talkersoft.com serves the new behavior (library browser +
graph, composer with masked refs, preview with provenance/gaps, used-by modal, console
tail-follow). TC-002 outputs before ship: `Results/RESULT.md` and `Retro/LESSONS.md` exist in
the ship commit (not added after), with migration + deploy targets and smoke-test results
recorded. TC-003 PRs: an exec-stage PR exists for every deck repo ahead of origin/main (at
minimum cloud-manager-api, cloud-manager-web, workflow-exec); verified via `gh pr list`/
`gh pr view`, not hv_ship output alone; the PR table in RESULT.md lists real links + merge
state. TC-004 scope: the cloud-manager-api PR touches only the additive role-metadata change;
no PR exists for cloud-manager-mcp, vorch, or porch. TC-005 every box in
`../Execution/Exec.md` is checked.
