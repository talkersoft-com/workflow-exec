# Test 0002
TC-001 unified search: a query string returns matching roles, playbooks, and collections (and
inventories in results) in one view; tag chip and platform badge facets narrow results and
compose with the query. TC-002 used-by counts: counts come from C-GRAPH usedBy, batched or
cached so a page of N results does not issue N sequential calls; a role used by 3 playbooks
shows 3. TC-003 graph view: `graph?depth=2` renders as SVG with d3-force used for layout math
only (grep imports: no d3 DOM/selection modules, no graph component library); depth and
edge-type filters change the rendered set; clicking a node navigates to the entity page.
TC-004 conventions: house components and SCSS tokens reused; no new component-library
dependency in package.json beyond d3-force. TC-005 isolation: `git diff` shows
cloud-manager-web changes only; existing `/ansible/*` pages unmodified. TC-006 `--target web`
build passes cleanly.
