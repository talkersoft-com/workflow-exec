# Test 0001
TC-001 additive migration: the new EF migration touches only `ansible_roles`, adds
tags/supported_platforms/maintainer/version, and applies cleanly to a database holding
existing role rows (which survive with empty/null metadata). TC-002 PATCH semantics: PATCHing
one metadata field updates it and leaves the other three (and name/description) unchanged;
response JSON is camelCase and contains public ids only — grep the new DTO/endpoint code for
Guid exposure: zero hits. TC-003 list filters: `?tag=` returns only roles whose Tags contain
the value; `?platform=` likewise for SupportedPlatforms; `?q=` matches name/description;
combined filters intersect; no filters → same row set as before the change. TC-004 contract
discipline: no new entities, no prefix-registry changes, `ansr` prefix untouched. TC-005
isolation: `git diff` across the deck shows changes in cloud-manager-api only. TC-006
`--target api` build passes cleanly.
