# Test 0003
TC-001 interface rendering: a role with required/default/internal vars renders required
prominent, default collapsed behind an override affordance, internal absent from the DOM;
the form schema comes from C-VAR records, not hardcoded shapes. TC-002 secrets ref-only: a
secret-classified param renders via `<SecretValue>`/ref-picker; the saved payload contains the
`$secretRef` form and `git grep` of the composer shows no path that submits a raw secret
value; no secret cleartext in component state. TC-003 validation: with an unsatisfied required
param, save is blocked with a per-field message; satisfying it or explicitly deferring it
(verified against a C-RES dry resolve) unblocks save. TC-004 output: save creates a NEW
playbook through the existing create endpoints with P4-typed vars; no "recompose existing"
affordance exists. TC-005 ordering: picked roles form an ordered, reorderable list following
the blueprint-attach pattern. TC-006 isolation: cloud-manager-web changes only; `--target web`
build passes cleanly.
