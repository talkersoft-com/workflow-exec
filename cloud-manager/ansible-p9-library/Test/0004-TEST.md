# Test 0004
TC-001 preview pane: choosing inventory + host in the (reused, not forked) P8 context bar
renders resolved vars with winner + overridden chain + gaps from C-RES; secret values are
masked via `<SecretValue>` — no cleartext in the DOM or network responses. TC-002 gap
behavior: with `enforceRequired` on, gap chips block the action; off, they are advisory.
TC-003 used-by guardrail: deleting a role with consumers shows the "used by N playbooks /
M tasks" modal listing consumers from C-GRAPH usedBy (grep: no client-side reference
inference); a role with zero consumers deletes without the modal; the confirm uses the
existing ConfirmModal danger pattern. TC-004 breaking change: renaming/removing a required
var on a consumed role surfaces the explicit breakage list before save; an unconsumed role
does not warn. TC-005 lint gate: with the toggle on and head-revision plint errors, the run
trigger is blocked with the errors surfaced; clean lint or toggle off → run allowed. TC-006
isolation: cloud-manager-web changes only; `--target web` build passes cleanly.
