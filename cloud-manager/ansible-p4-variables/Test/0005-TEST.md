# Test 0005
TC-001 tool surface: `cloud_variable_list/get/create/update/delete` and
`cloud_variable_secret_ref_set` are registered, schema-valid, and round-trip C-VAR shapes
against the running API. TC-002 read/mutate separation honored per the existing profile
convention; all ids validated via `idSchema`. TC-003 exclusions: no MCP tool exposes
`GET /run/{runId}/secret`; grep the dist tool registry — zero hits. TC-004 MCP-boundary
rejection: secret-with-default via `cloud_variable_create` and cleartext-on-secret-name via
an override-mutating tool both surface the API error (not a silent success). TC-005
regression byte-identical: with zero `vdef` rows, a plain run and a blueprint-sequencer run
produce materialized payloads identical to the pre-P4 baseline (no SecretInputs delta, no
extravar delta). TC-006 `--target mcp` build passes cleanly (lockfile stable).
