# Test 0006
TC-001 `dotnet ef database update` applied cleanly — `vdef`/`sref`/`varev` tables present in
the live `clouddb`, CHECK constraint active. TC-002 deploy script succeeded for api (systemd
restart + smoke poll passed) and mcp (dist rebuilt; lockfile gate passed);
`cloud_health_check` healthy. TC-003 smoke through the deployed API: secret `vdef` +
secretRef created (HTTP 2xx), cleartext write to the secret-classified name returns 409,
`requiredVars` returns the known gap (HTTP 200); fixtures cleaned up. TC-004
`Results/RESULT.md` and `Retro/LESSONS.md` exist and were committed by the ship (not left
for the next branch); RESULT.md contains the operator `/mcp` restart instruction. TC-005
hv_ship opened PRs for exactly the repos ahead of main (cloud-manager-api,
cloud-manager-mcp, workflow-exec, and workflow-plans if touched) — and NO PR for
cloud-manager-web, vorch-lib, or vorch-service. TC-006 PR table in RESULT.md filled with
real PR links (verify with `gh` if ship output omitted them).
