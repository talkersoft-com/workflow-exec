# Test 0002
TC-001 C-VAR shape verbatim: GET of a created `vdef` returns exactly the contracts §7 fields
(`publicId`, `name`, `scope`, `ownerPublicId`, `kind`, `sensitivity`, `secretRef`,
`defaultValue`, `description`), camelCase, public ids only. TC-002 secret⇒null-default:
create/update with `sensitivity: "secret"` and a non-null `defaultValue` returns 4xx with
guidance; nothing persisted. TC-003 one-way classification: plain→secret succeeds and records
`classified_secret`; secret→plain WITHOUT the force flag is rejected; WITH the force flag it
succeeds and the event carries the force marker. TC-004 secretRef lifecycle: PUT with a
`cloudmanager/…` path succeeds and records `secret_ref_set`; PUT with a non-`cloudmanager/`
prefix is rejected; DELETE records `secret_ref_removed`. TC-005 every CRUD mutation produces
its matching `varev` row; the event write failing does not fail the primary write. TC-006
harvest: against a P2-decomposed fixture owner, unregistered var names are proposed with
kind `internal` and sensitivity `plain` — never `secret`; already-registered names are not
re-proposed. TC-007 all routes reject callers without `ansible-studio`. TC-008
`--target api` build passes cleanly.
