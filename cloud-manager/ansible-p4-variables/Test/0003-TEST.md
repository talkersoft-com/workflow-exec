# Test 0003
TC-001 `$secretRef` accepted on every surface: assignment override, blueprint-playbook
override, and P1 `gvar`/`hvar` value writes each persist `{ "$secretRef": "sref_…" }` for a
secret-classified name. TC-002 cleartext rejected on every surface: a plain string assigned
to a secret-classified name returns 409 with guidance on each of the same write paths;
nothing persisted. TC-003 dangling/mismatched refs rejected: a `$secretRef` to a nonexistent
`sref`, or to a ref whose `vdef` is plain-classified, is a 4xx. TC-004 materialization: for a
playbook whose override carries `$secretRef`, the materialized response contains a
`SecretInputs` entry (ExtravarName + VaultPath) and the variable does NOT appear as a
cleartext extravar — grep the full materialized payload for the Vault secret fixture value,
zero hits. TC-005 plain variables in the same override materialize exactly as before
(byte-identical for an override with no `$secretRef`). TC-006 scan endpoint: a seeded
cleartext-looking override is reported; re-running the scan changes no rows (report-only).
TC-007 no diffs under vorch-lib, vorch-service, or porch paths. TC-008 `--target api` build
passes cleanly.
