# Test 0004
TC-001 closure correctness: a required `vdef` owned by a role reached only via a P2
`depends_on` edge appears in the missing list; one on an unrelated role does not. TC-002
satisfaction layers: the same required var disappears from the missing list when satisfied
by (a) a group var at any nesting depth, (b) a host var, (c) an assignment override, and
(d) a blueprint override — exercised as separate fixtures. TC-003 a `$secretRef` override
satisfies a required secret var; a secret `vdef` with no ref and no override stays missing.
TC-004 enforceRequired: run trigger with `enforceRequired: true` and a non-empty gap is
rejected with the gap list in the response body; with `enforceRequired: false` or the field
omitted the run enqueues exactly as today. TC-005 additivity: a pre-P4 run-trigger payload
(no new fields) round-trips byte-identically. TC-006 response is camelCase with public ids
only. TC-007 `--target api` build passes cleanly.
