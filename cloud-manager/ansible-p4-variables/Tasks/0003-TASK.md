# Task 0003 — Typed overrides (cloud-manager-api repo)

## Steps
1. RESEARCH (read, no changes): every jsonb override write path —
   `VmPlaybookAssignment.VarsOverride`, `BlueprintPlaybook.VarsOverride`, P1 `gvar`/`hvar`
   value writes — and how `MaterializedPlaybook.SecretInputs`
   (`[]SecretInputRef{ExtravarName, VaultPath}`) is assembled today on
   `GET /playbook/{id}/materialized`. Porch resolves SecretInputs with the worker token at
   materialization time — that mechanism is reused verbatim, NOT replaced.
2. Accept `{ "$secretRef": "sref_…" }` as a value on every override surface. The ref must
   exist and belong to a secret-classified `vdef`.
3. Shared validation service on every override write path: reject writes where a value is a
   plain string assigned to a secret-classified variable name — 409 with guidance pointing
   at the `$secretRef` form. Never silently accept or strip cleartext.
4. Materialization expansion: when assembling the materialized response, expand each
   `$secretRef` value into a `SecretInputs` entry (ExtravarName = the variable name,
   VaultPath = the ref's path) instead of an extravar value. Porch behavior unchanged —
   no vorch/porch code edits.
5. Retroactive secret scan (PLAN.md open question 2 default): report-only admin endpoint
   that scans existing `varsOverride` blobs for token-looking strings assigned to
   secret-classified names and reports them; NO auto-migration of values.
6. Build: `python3 .cicd/build-cloud-manager.py --target api` must pass.

Done when: `$secretRef` accepted and validated on every override surface, cleartext-to-secret
writes rejected with 409, materialization expands refs into SecretInputs, scan endpoint
reports without mutating, api build clean.
