# Task 0000 — Initialize Workspace

## Steps
1. `hv_status deck: "cloud-manager"`
2. Clean on feature branch → `hv_next`; clean on default → `hv_init`; dirty → STOP
   (exception: this scaffold's own runtime edits in workflow-exec)
3. `hv_status` again; record the branch in `../deck.md` under "## Branch"

## Definition of done
All repos on the recorded branch; deck.md Branch not TBD.
