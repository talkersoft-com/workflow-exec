# Task 0002 — Deploy, verify, ship

## Steps
1. Deploy: `python3 .cicd/deploy-cloud-manager.py --target web` (record smoke evidence).
2. Ensure `marketplace` flag enabled for verification (restore prior state after; note it).
3. Playwright on the deployed app: /blueprints rows show Edit → click → lands on the composer;
   create a scratch blueprint via the modal → lands directly on its composer; regression —
   publish/archive/delete from the list still work; clean up the scratch blueprint.
4. Write `Results/RESULT.md` + `Retro/LESSONS.md` BEFORE shipping.
5. `hv_ship stage:"exec"` per ../deck.md; confirm + record PRs.

## Definition of done
Deployed; both affordances proven; regression green; PRs merged and recorded.
