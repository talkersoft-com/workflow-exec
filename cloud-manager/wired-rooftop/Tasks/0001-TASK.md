# Task 0001 — Navigation affordances

## Steps (cloud-manager-web only)
1. BlueprintsPage list rows: add an Edit action navigating to `/blueprints/{id}` — match the
   existing VMs/Playbooks list→detail pattern (clickable name and/or explicit button; copy
   whichever those pages use). Existing publish/archive/delete row actions unchanged.
2. Create modal: on successful create, `navigate('/blueprints/' + publicId)` from the create
   response instead of staying on the list. Error path unchanged (errors stay in the modal).
3. Build: `python3 .cicd/build-cloud-manager.py --target web` green.

## Definition of done
Build green; both affordances work against a local serve (or deferred to 0002's deployed check).
