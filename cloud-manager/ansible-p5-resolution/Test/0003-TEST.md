# Test 0003
TC-001 migration creates `vm.resolution_snapshots` (or the contracts-§1 table name for
`rsnap`) with FK to the runs table — verify via `db_get_table_schema` /
`db_get_parent_relations`. TC-002 enqueue a run with the flag on → one resolution report per
target persisted; jsonb shape matches C-RES §8. TC-003 SQL string-search the stored jsonb
for the fixture secret cleartext → zero hits; `"***"` present. TC-004
`GET api/v1/run/{runId}/resolution` → 200 with per-target reports matching what was stored;
unknown runId → 404; run predating the feature → 200 + empty collection. TC-005 snapshot
failure does not block enqueue: with the snapshot path forced to throw (test seam), the run
still enqueues and executes. TC-006 `PlaybookRun.VarsSnapshot` behaviour unchanged — existing
runs still populate it identically (compare against a pre-change fixture). TC-007 retention:
deleting/pruning a run removes its snapshots (no orphans — `db_get_child_relations` clean).
TC-008 `--target api` build passes.
