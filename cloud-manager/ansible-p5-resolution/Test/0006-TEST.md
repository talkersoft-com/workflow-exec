# Test 0006
TC-001 `dotnet ef database update` applied cleanly — the `rsnap` table present in the live
`clouddb`. TC-002 deploy script succeeded for api (systemd restart + smoke poll passed) and
mcp (dist rebuilt; lockfile gate passed); `cloud_health_check` healthy. TC-003 one preview
call through the deployed API returns C-RES with `"***"` masking (HTTP 200, flag on); one
`enforceRequired: true` run with a known gap returns 422. TC-004 `Results/RESULT.md` and
`Retro/LESSONS.md` exist and were committed by the ship (not left for the next branch);
RESULT.md contains the operator `/mcp` restart instruction. TC-005 hv_ship opened PRs for
exactly the repos ahead of main (cloud-manager-api, cloud-manager-mcp, workflow-exec, and
workflow-plans if touched) — and NO PR for cloud-manager-web, vorch-lib, or vorch-service.
TC-006 PR table in RESULT.md filled with real PR links (verify with `gh` if ship output
omitted them).
