# Test 0001
TC-001 unit suite green: for every adjacent layer pair in the precedence table, the higher
layer's value wins and the lower appears in the overridden chain (role defaults → group vars
by nesting depth → host vars → play vars → assignment override → blueprint override → extra
vars). TC-002 states correct: variable consumed but defined nowhere → `undefined`; C-VAR
required kind with no winner → `missingRequired`; defined → `resolved`. TC-003 masking on
every path: secret `vdef` winner, `$secretRef` winner, and secret-overridden-by-plain all
report `effectiveValue: "***"` and NO test can extract cleartext from any engine output —
grep the resolution result object graph for the fixture secret value, zero hits. TC-004
provenance carries public ids only (regex `^[a-z]+_` on every sourcePublicId; no Guids).
TC-005 precedence table is data: unmodeled Ansible levels present as explicit out-of-scope
entries, not absent. TC-006 group-nesting fixture (≥3 levels deep) resolves by nesting depth
without recursion-depth failure. TC-007 `PlaybookRun.VarsSnapshot` untouched (no schema or
write-path change in this phase). TC-008 `--target api` build passes cleanly.
