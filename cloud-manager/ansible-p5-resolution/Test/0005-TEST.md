# Test 0005
TC-001 perf test exists, is CI-runnable, and passes: synthetic 1k-var / 200-host
whole-inventory preview completes within the threshold recorded in the test itself. TC-002
query-count assertion passes — bounded per context, no per-host or per-var query explosion
(check the test captures actual EF query counts, not wall-time only). TC-003 200 hosts →
200 OK; 201 hosts → 413 (cap constant shared with the endpoint, not duplicated). TC-004
flag-off regression: porch-bound extravars payload, `VarsSnapshot`, run DTOs, and event
stream byte-identical to pre-change fixtures; zero rsnap rows written. TC-005 flag-on /
no-opt-in regression: run flows identical except additive rsnap rows + additive DTO fields.
TC-006 `--target api` build passes with the perf suite included in the default test run.
