# Test 0002
TC-001 `POST api/v1/resolution/preview` with a concrete playbook+inventory+host context →
200; response matches C-RES §8 field-for-field (camelCase, `variables[]`, `gaps[]`, winner /
overridden shapes with `precedenceLevel`/`source`/`sourcePublicId`). TC-002 secret variable
→ `effectiveValue` is the literal `"***"` and the cleartext appears NOWHERE in the raw HTTP
response body (string search). TC-003 missing required var → `state: "missingRequired"` for
the variable AND a matching entry in `gaps[]` with `reason: "missingRequired"`. TC-004
`hostId: null` → per-host matrix for every host of the inventory. TC-005 inventory with more
than the cap (200 hosts) → 413. TC-006 `ansible-studio` flag off → endpoint not available
(same behaviour as the other gated ansible endpoints). TC-007 unknown public ids → 404;
host not in inventory → 422. TC-008 side-effect-free: no new rows in events or run tables
after a preview call (row counts identical before/after). TC-009 `--target api` build passes.
