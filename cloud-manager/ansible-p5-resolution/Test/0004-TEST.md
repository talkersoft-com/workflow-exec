# Test 0004
TC-001 run trigger with `enforceRequired: true` and a missing required var → 422; body
contains the C-RES gap list naming the variable, the kind, and the affected host(s); run NOT
enqueued (no run row created). TC-002 same context with all required vars present → run
enqueues normally. TC-003 `enforceRequired` omitted or false → behaviour identical to
pre-change (no 422 path, run enqueues; compare response + side effects against a pre-change
fixture). TC-004 run-list DTO: old-shape golden file still deserializes (additive field
only); new field present where snapshots exist. TC-005 `cloud_resolution_preview` returns
C-RES-shaped output for a real context; secret variables show `"***"`; cleartext absent from
the entire tool output (string search). TC-006 tool is read-only — no state change after
invocation (row counts identical). TC-007 `resolution.ts` registered: tool appears in the
built dist's tool index alongside existing modules. TC-008 `--target all` build passes
(api + mcp).
