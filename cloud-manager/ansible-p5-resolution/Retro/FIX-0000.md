# FIX-0000 — Task 0000 series-order gate failed: P4 not merged

## Failure
Test `0000-TEST.md` TC-004 (series prerequisites merged) **fails**.

Verified against `origin/main` (fetched 2026-06-12):

| Prerequisite | Evidence | Status |
|---|---|---|
| P1 inventory layers (`gvar`/`hvar`) | cloud-manager-api PR #28 (`4fa4b7c`); `GroupVar.cs`, `HostVar.cs`, `Inventory*.cs` present | ✅ merged |
| P2 `vblk` variable blocks | cloud-manager-api PR #29 (`01b8ce3`); `VarBlock.cs`, `EntityEdge.cs` present | ✅ merged |
| P3 (not required, noted) | cloud-manager-api PR #30 (`f36134d`) | ✅ merged |
| **P4 C-VAR typing (`vdef`/`sref`/`varev`)** | No `VariableDefinition`/`SecretRef` entity, no `vdef`/`sref` prefix registration, no migration in cloud-manager-api; no `variables.ts` module in cloud-manager-mcp (`origin/main`). Only reference is a forward-looking comment in `EntityEdge.cs`. | ❌ **missing** |

Additionally, no `ansible-p4-variables` plan folder exists under
`workflow-plans/cloud-manager/` — P4 appears not to have been planned/executed yet
(series jumped P1 → P2 → P3; P5 scheduled out of ledger order).

## Why this blocks P5
The resolution engine consumes C-VAR verbatim (contracts §7): sensitivity drives
engine-level masking, `kind: "required"` drives `missingRequired` gap detection, and
`$secretRef` overrides drive provenance. None of those records exist to fold.

## Action taken
- Task 0000 steps 1–6 completed: deck clean, branch `mushy-walleye` minted via hv_next
  across all 18 repos, branch recorded in `deck.md` (TC-001/002/003 pass).
- Task 0000 box left **unchecked**; TC-004 prescribes: "any prerequisite missing —
  workflow must stop at Task 0000."
- No retry possible at this layer: the fix is to plan + execute + merge
  `ansible-p4-variables` first, then re-run this workflow from Task 0000.

## Resolution
STOPPED — surfaced to operator per task 0000 step 7 and the improvisation policy.

---

## Re-verification — second attempt, 2026-06-12 (branch `country-hammockstand`)

Same failure recurs. Verified against `origin/main` (fetched 2026-06-12, second attempt):

| Check | Evidence | Status |
|---|---|---|
| P1 inventory layers | `GroupVar.cs`, `HostVar.cs` present | ✅ merged |
| P2 `vblk` blocks | `VarBlock.cs` present | ✅ merged |
| **P4 C-VAR typing** | cloud-manager-api main still ends at PR #30 (`f36134d`, P3). No `VariableDefinition`/`SecretRef` entity; only `vdef`/`sref` hit is the forward-looking comment in `EntityEdge.cs`. No `variables.ts` in cloud-manager-mcp. No open PRs. | ❌ **still missing** |

What changed since the first attempt: `ansible-p4-variables` now has a plan folder under
`workflow-plans/cloud-manager/` and an exec scaffold under `workflow-exec/cloud-manager/`,
but its Exec.md task list is entirely unchecked — P4 is planned, not executed or merged.

## Action taken (second attempt)
- Task 0000 steps 1–6 completed: deck clean (18/18), branch `country-hammockstand` minted via
  hv_next across all 18 repos, branch recorded in `deck.md` (TC-001/002/003 pass).
- TC-004 fails again; Task 0000 box left unchecked.
- Operator instruction for this run was "do not start any other plan", so executing
  `ansible-p4-variables` inline was out of scope.

## Resolution (second attempt)
STOPPED — second failed recovery of the same gate. Per the improvisation policy
("two failed recoveries → stop and surface to operator"): execute and merge
`ansible-p4-variables` (exec scaffold is ready), then re-run this workflow from Task 0000.
