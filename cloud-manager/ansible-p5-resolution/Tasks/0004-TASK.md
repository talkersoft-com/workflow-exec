# Task 0004 — Gap wiring + MCP tool (cloud-manager-api + cloud-manager-mcp repos)

## Steps
1. `enforceRequired` wiring (api): `POST …/run` with P4's `enforceRequired: true` consults
   `IVariableResolutionService` before enqueue; any `missingRequired` gap → 422 with the
   C-RES gap list in the error body. The flag stays OPT-IN this plan (PLAN.md open question
   1 default — do not flip the default; revisit in P9). `enforceRequired` absent/false →
   behaviour byte-identical to today.
2. Per-target enforcement: the 422 gap list identifies WHICH host is missing WHAT (per-host
   matrix from the engine), not a flattened union.
3. Gap surfacing in run-list DTOs (api): additive field only — existing consumers must
   deserialize unchanged. Surface gap counts/summary on run-list rows where snapshots exist.
4. MCP (cloud-manager-mcp): new module `resolution.ts` exposing `cloud_resolution_preview`
   (read-only, contracts §4). Input mirrors the preview body (playbookId, inventoryId,
   hostId nullable, extraVars); output mirrors C-RES. Secrets arrive pre-masked from the
   engine — the tool performs NO masking of its own and has no path to cleartext. Register
   in the profile/tool index the same way the existing modules do.
5. Tests: 422 path (single host + matrix), opt-in default regression (no `enforceRequired`
   → no engine consult on the hot path beyond what 0003 added), additive DTO check
   (golden-file old-shape deserialization), MCP tool happy path + masked output.
6. Build: `python3 .cicd/build-cloud-manager.py --target all` (api + mcp changed) must pass.

Done when: enforceRequired 422 path live with per-host gaps, run-list DTOs additively
extended, `cloud_resolution_preview` registered and returning masked C-RES, builds clean.
