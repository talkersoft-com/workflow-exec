# Task 0000 — Verify Workspace

## Goal
Confirm cloud-manager deck is clean on `icy-omelette` and hive-deck-pro is ready for changes.

## Steps

1. Run `hv_status deck: "cloud-manager"` — all repos should be clean on `icy-omelette`
2. Run `hv_status deck: "hive-deck-pro"` — confirm it is clean and on a feature branch

## Definition of done

Both decks clean. cloud-manager on `icy-omelette`.
