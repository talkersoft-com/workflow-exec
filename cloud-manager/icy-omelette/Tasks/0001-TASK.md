# Task 0001 — Implement plan_paths in workflow.ts

## Goal

Replace `plan_path` (string) with `plan_paths` (string array, min 1) in `hv_workflow` and update the assembly logic to prepend all plans in order.

## Target repo

`hive-deck-pro` — file: `mcp/src/tools/workflow.ts`

## Steps

1. Open `/Users/talker/workspace/hive-deck-pro/hive-deck-pro/mcp/src/tools/workflow.ts`

2. Replace the `plan_path` parameter with `plan_paths`:
```typescript
plan_paths: z.array(z.string()).min(1).describe(
  "Array of absolute paths to PLAN.md files to prepend as context before the workflow instructions. " +
  "Must be absolute paths (e.g. [\"/Users/you/workspace/cloud-manager/planning/workflow-plans/cloud-manager/thorny-catapult/PLAN.md\"]). " +
  "Plans are prepended in array order, each separated by --- so the agent reads all context before executing tasks. " +
  "At least one path is required."
),
```

3. Replace the plan-reading + assembly block:
```typescript
const planSections: string[] = [];
for (const planPath of plan_paths) {
  let planContent: string;
  try {
    planContent = readFileSync(planPath, "utf8");
  } catch (e) {
    return err(`Could not read plan file at ${planPath}: ${(e as Error).message}`);
  }
  const label = plan_paths.length > 1
    ? `## Plan ${planSections.length + 1} of ${plan_paths.length}`
    : `## Plan`;
  planSections.push(`${label}\n\n> Source: ${planPath}\n\n${planContent.trim()}`);
}

const combined =
  planSections.join("\n\n---\n\n") +
  `\n\n---\n\n## Workflow: ${name}\n\n` +
  output;
```

4. Update the tool description to mention `plan_paths` (plural) instead of `plan_path`.

5. Build:
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro/mcp && npm run build
```

## Definition of done

`npm run build` passes with no errors. `dist/tools/workflow.js` contains `plan_paths`.
