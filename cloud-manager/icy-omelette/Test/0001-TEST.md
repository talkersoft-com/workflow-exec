# Test 0001 — plan_paths Implemented

## TC-001 — plan_paths present in source
```bash
grep -c "plan_paths" /Users/talker/workspace/hive-deck-pro/hive-deck-pro/mcp/src/tools/workflow.ts
```
Pass: count > 0.
Fail: 0.

## TC-002 — plan_path (singular) removed from source
```bash
grep -c "plan_path[^s]" /Users/talker/workspace/hive-deck-pro/hive-deck-pro/mcp/src/tools/workflow.ts
```
Pass: 0.
Fail: count > 0 (old parameter still present).

## TC-003 — Build succeeds
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro/mcp && npm run build 2>&1
```
Pass: exits 0, no TypeScript errors.
Fail: any compile error.

## TC-004 — plan_paths in compiled output
```bash
grep -c "plan_paths" /Users/talker/workspace/hive-deck-pro/hive-deck-pro/mcp/dist/tools/workflow.js
```
Pass: count > 0.
Fail: 0.
