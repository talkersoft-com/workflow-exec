# Test 0002 — sample consumption per-OS
TC-001 `bash -n deploy.sh` clean (shellcheck clean if available). TC-002 deploy detects the target
VM's systemd version over SSH and installs the matching unit. TC-003 ≥250 target → unit has
`LoadCredentialEncrypted=DBPASSWD` (reads `/etc/credstore.encrypted/DBPASSWD`). TC-004 <250 target →
unit has `LoadCredential=DBPASSWD:/etc/credstore/DBPASSWD` (reads the root-only `0600` fallback).
TC-005 `server.js` stays credential-only — reads `$CREDENTIALS_DIRECTORY/DBPASSWD`; no env-password
path, no hardcoded password, no Vault fetch. TC-006 `PGPASSWORD` stays optional in `deploy.sh`; the
`.env` written is non-secret connection settings only (`0600 root`), no password by default. TC-007
`tests/test_secret_injection.py` assertions/gate untouched; no API/web/DB-schema change.
