# Test 0003 — E2E verify + build vorch + manual redeploy + ship
TC-001 vorch-lib `go build ./...` + `go test ./provision/...` and vorch-service `build-app.sh` green.
TC-002 vorch-service manually redeployed on the host (binary installed locally — deploy-app.sh cannot
ssh-to-self) and `vorch_service` restarted; exact commands recorded in RESULT.md. TC-003
`cloud-manager-api/tests/test_secret_injection.py` run on ubuntu-server exits **0** with **both** the
jammy (22.04) AND noble (24.04) cases PASS; the final RESULT block is pasted into RESULT.md. TC-004
each case serves `Database connection: SUCCESS` through haproxy on 443 with the password read ONLY
from the injected systemd credential. TC-005 no plaintext left in `/run/cm-seed` on EITHER VM after
boot; the jammy fallback `/etc/credstore/DBPASSWD` is `0600 root:root` — evidence in RESULT.md.
TC-006 no API/web/DB-schema change; the haproxy/wildcard-cert path still works. TC-007
`Results/RESULT.md` and `Retro/LESSONS.md` exist and were written BEFORE hv_integrate. TC-008
hv_integrate opened PRs and reported their links.
