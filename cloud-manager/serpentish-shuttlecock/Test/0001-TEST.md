# Test 0001 — vorch-lib version-aware seal
TC-001 `go build ./...` and `go test ./provision/...` green. TC-002 the rendered seal script branches
on the guest systemd version. TC-003 ≥250 branch emits `systemd-creds encrypt` →
`/etc/credstore.encrypted/<name>` (unchanged behaviour). TC-004 <250 branch writes the secret to a
**root-only `0600`** file `/etc/credstore/<name>` (install -m 0600 -o root -g root), on local disk,
not `/run`, not world-readable. TC-005 `/run/cm-seed` shred is UNCONDITIONAL — present in a
trap/EXIT path so it runs on success AND failure; no decoded plaintext survives. TC-006 a seal
failure is LOUD — non-zero exit / surfaced marker, never silent. TC-007 no secret VALUE appears in
plaintext in the rendered script except the documented base64 `write_files` transport; nothing
logged. TC-008 scope: no API/web/DB-schema change; cert/`x-secret-path` path and empty-binding
user-data untouched.
