# Test 0004 — E2E + ship

## TC-001 — Golden: single-layer behavior byte-identical (decks list, registry list, one
assembled plan, one assembled workflow).

## TC-002 — Two-layer demo: layer resolution + machine shadow proven on the temp HV_HOME; real
workflow-configuration untouched (zero diffs there).

## TC-003 — Shipped: PRs for exactly hive-deck-pro + workflow-exec, merged; RESULT/LESSONS
committed in the ship; operator /mcp note present.
