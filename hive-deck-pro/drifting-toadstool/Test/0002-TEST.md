# Test 0002 — Layer resolution

## TC-001 — Shadow matrix (temp HV_HOME fixture)
Layer has decks A,B + feature@A; machine overrides deck B + adds deck C + shadows one fragment:
hv decks lists A,B,C (B = machine copy); registry resolves feature@A from layer; the shadowed
fragment renders the machine copy; modules/profiles/rulesets fall back per file.

## TC-002 — config.yaml never falls back
Fixture with config.yaml ONLY in the layer → hard error (machine identity missing), not silent
adoption.

## TC-003 — Single-layer short-circuit
Same fixture without deck_config: behavior identical to pre-change (golden diff on hv decks +
one assembled workflow).
