# Test 0003 — Machine identity

## TC-001 — Profile selection
Fixture: HV_PROFILE=alpha with profiles/alpha/config.yaml (deck_config: ../..) → decks resolve
from the layer; machine root = profiles/alpha (an override placed there shadows the layer).

## TC-002 — Fallbacks
HV_PROFILE unset, or set with no profile config → root config.yaml used; behavior matches
pre-change golden.

## TC-003 — Docs present
README documents the model, tokens, and the one-file override recipe.
