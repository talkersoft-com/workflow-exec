# Test 0001 — Token engine

## TC-001 — Unit matrix green
go test ./internal/config/ ./internal/mcp/ — all expandPath forms covered and passing.

## TC-002 — Alias equivalence
A temp mcps.yaml using {{WorkspaceRoot}} and an identical one using {{DecksRoot}} produce
identical resolved server args/env (drive via the Go test or a scratch HV_HOME + hv mcp run).

## TC-003 — Legacy untouched
Existing repo configs (workflow-configuration as-is) resolve to the same values as before the
change (spot-check plan_folder/exec_folder and one mcps entry via a golden capture).
