# Task 0004 — Verification, rebuild, results, ship

## Goal
Prove back-compat and the two-layer model end-to-end; ship.

## Steps
1. **Golden back-compat**: with the real workflow-configuration (no deck_config anywhere):
   capture pre-change vs post-change output of `hv decks`, `hv promote --list hive-deck-pro`,
   and one full assembled plan + one assembled workflow (orchestrate ops) — byte-identical
   (modulo nothing).
2. **Two-layer live demo** on a temp HV_HOME copy: introduce base/ + thin machine config with
   deck_config; verify decks/registry/fragments resolve; override one deck file machine-side and
   verify the shadow.
3. Rebuild + install artifacts per internal rules (Go binary via make install, mcp dist via npm
   run build) — only now that all tests pass; note the operator /mcp restart in RESULT.md.
4. Write `Results/RESULT.md` (incl. golden evidence + PR table) and `Retro/LESSONS.md`.
5. `hv_ship stage:"exec"` per ../deck.md; confirm PRs; record links (post-ship commit if needed).

## Definition of done
Golden identical; two-layer demo passes; artifacts rebuilt; PRs merged and recorded.
