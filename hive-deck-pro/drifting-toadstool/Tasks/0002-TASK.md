# Task 0002 — Shared layer resolution

## Goal
deck_config two-layer resolution exactly per PLAN.md §R7.3/R7.4.

## Steps (hive-deck-pro repo)
1. `Setup.DeckConfig string yaml:"deck_config"`; expanded via expandPath relative to config.yaml.
2. `layerPaths(rel string) []string` central helper: [machineRoot/rel, deckConfig/rel]; single
   entry when DeckConfig empty.
3. Per-file fallback through layerPaths in: deck yaml loading (LoadDeck), workflow extension
   loading (loadExtensionBytes), modules.yaml, claude-profiles.yaml, gitignore-rulesets.yaml,
   mcps.yaml (decide: mcps.yaml is listed shared-layer content? R7.3 names four artifacts — keep
   mcps.yaml machine-first WITH layer fallback for consistency, note in docs).
4. `config.yaml` exempt — only machineRoot, never the layer.
5. Union scans, machine wins on filename: hv decks listing; `LoadRegistry` (both
   decks/workflows/** trees — machine shadow beats layer); `listWorkflowNames`;
   `LoadFragmentWithFallback` becomes machine deck-fragments → layer deck-fragments → builtin.
6. Unit tests: shadowing matrix (machine-only, layer-only, both→machine wins) per artifact type;
   registry dedupe (same name@deck in both layers = one entry, machine path).
7. Build green; binary still not installed.

## Definition of done
Two-layer temp fixture passes the matrix; empty DeckConfig short-circuits to today's behavior.
