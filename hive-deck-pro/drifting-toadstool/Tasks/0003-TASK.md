# Task 0003 — Machine identity via HV_PROFILE + docs

## Goal
Per decided default: the machine's config.yaml can live at profiles/$HV_PROFILE/config.yaml.

## Steps
1. `configRoot()`/config loading: when $HV_PROFILE set AND $HV_HOME/profiles/$HV_PROFILE/
   config.yaml exists → that file is the machine config (its dir is the machine root for
   layerPaths); else $HV_HOME/config.yaml exactly as today.
2. The profile config declares deck_config (e.g. `deck_config: ../..`) — relative-to-declaring-
   file makes this natural; document the pattern.
3. README: "Machine truth vs shared truth" section — the two-layer model, resolution order,
   token table, HV_PROFILE selection, override-by-copying recipe.
4. Unit test: profile-config selection + fallback; build green.

## Definition of done
Both selection paths tested; docs in README; no behavior change when HV_PROFILE unset or profile
config absent.
