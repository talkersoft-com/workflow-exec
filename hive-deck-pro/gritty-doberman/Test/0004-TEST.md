# Test 0004 — ship

## TC-001 — ship uses targetBranch from deck config
```bash
grep -n "targetBranch\|DeckFile\.Branch\|l\.DeckFile" \
  /Users/talker/workspace/hive-deck-pro/hive-deck-pro/internal/ship/ship.go
```
Pass: matches show deck branch used for PR base, not `defaultBranch(r.Dest)`.

## TC-002 — EnsureRemoteBranch called before commitsAhead
```bash
grep -n "EnsureRemoteBranch\|commitsAhead" \
  /Users/talker/workspace/hive-deck-pro/hive-deck-pro/internal/ship/ship.go
```
Pass: `EnsureRemoteBranch` line number is less than `commitsAhead` line number in the PR loop.

## TC-003 — Pre-flight uses targetBranch
```bash
grep -n "targetBranch\|on target branch\|on default branch" \
  /Users/talker/workspace/hive-deck-pro/hive-deck-pro/internal/ship/ship.go
```
Pass: pre-flight error message references target branch, not default branch.

## TC-004 — Build clean
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro && go build ./... 2>&1
```
Pass: no output.
