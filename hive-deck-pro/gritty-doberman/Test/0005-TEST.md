# Test 0005 — YAML updates

## TC-001 — config.yaml has no dead keys
```bash
grep -n "default_target_branch\|target_branches" \
  /Users/talker/workspace/hive-deck-pro/workflow-configuration/config.yaml
```
Pass: no output.

## TC-002 — Active deck YAMLs have branch field
```bash
grep -l "^branch:" \
  /Users/talker/workspace/hive-deck-pro/workflow-configuration/decks/*.yaml
```
Pass: lists cloud-manager.yaml, hive-deck-pro.yaml, hive-deck.yaml, and other active decks.

## TC-003 — YAML parses cleanly
```bash
cd /Users/talker/workspace/hive-deck-pro/workflow-configuration && \
python3 -c "
import yaml, glob
for f in glob.glob('decks/*.yaml'):
    yaml.safe_load(open(f))
    print('ok:', f)
"
```
Pass: all files print `ok:` with no exceptions.
