# Test 0006 — Cleanup & build

## TC-001 — No dead references remain
```bash
grep -rn "DefaultTargetBranch\|TargetBranches\|BranchFor\|branchFor" \
  /Users/talker/workspace/hive-deck-pro/hive-deck-pro --include="*.go"
```
Pass: no output.

## TC-002 — Full build passes
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro && go build ./... 2>&1
```
Pass: no output.

## TC-003 — Binary installed
```bash
which hv && hv list decks
```
Pass: lists available decks with no error.

## TC-004 — Results and Retro written
```bash
ls /Users/talker/workspace/hive-deck-pro/planning/workflow-exec/hive-deck-pro/gritty-doberman/Results/RESULT.md \
   /Users/talker/workspace/hive-deck-pro/planning/workflow-exec/hive-deck-pro/gritty-doberman/Retro/LESSONS.md
```
Pass: both files exist.
