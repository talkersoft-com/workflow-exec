# Test 0002 — provision & init

## TC-001 — EnsureRemoteBranch exists in git package
```bash
grep -n "func EnsureRemoteBranch" \
  /Users/talker/workspace/hive-deck-pro/hive-deck-pro/internal/git/git.go
```
Pass: one match.

## TC-002 — provision calls EnsureRemoteBranch after fallback clone
```bash
grep -n "EnsureRemoteBranch" \
  /Users/talker/workspace/hive-deck-pro/hive-deck-pro/internal/provision/provision.go
```
Pass: at least one match in the fallback clone block.

## TC-003 — ops/init.go calls EnsureRemoteBranch
```bash
grep -n "EnsureRemoteBranch" \
  /Users/talker/workspace/hive-deck-pro/hive-deck-pro/ops/init.go
```
Pass: at least one match.

## TC-004 — Build clean
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro && go build ./... 2>&1
```
Pass: no output.
