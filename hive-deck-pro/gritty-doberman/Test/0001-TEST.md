# Test 0001 — Config & resolve

## TC-001 — Removed fields gone
```bash
grep -rn "DefaultTargetBranch\|TargetBranches\|BranchFor\|branchFor" \
  /Users/talker/workspace/hive-deck-pro/hive-deck-pro --include="*.go"
```
Pass: no output.
Fail: any matches.

## TC-002 — DeckFile.Branch exists
```bash
grep -n "Branch.*yaml:.*branch" \
  /Users/talker/workspace/hive-deck-pro/hive-deck-pro/internal/config/config.go
```
Pass: at least one match inside `DeckFile` struct.

## TC-003 — Plan.Branch exists
```bash
grep -n "Branch string" \
  /Users/talker/workspace/hive-deck-pro/hive-deck-pro/internal/resolve/resolve.go
```
Pass: at least one match inside `Plan` struct.

## TC-004 — Build clean
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro && go build ./... 2>&1
```
Pass: no output (zero errors).
