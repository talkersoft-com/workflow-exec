# Test 0003 — branch & next

## TC-001 — createAll uses deckBranch parameter
```bash
grep -n "deckBranch\|plan\.Branch\|DeckFile\.Branch" \
  /Users/talker/workspace/hive-deck-pro/hive-deck-pro/internal/branch/branch.go
```
Pass: matches show deckBranch used in remoteRef construction.

## TC-002 — createAll no longer calls defaultBranch unconditionally
```bash
grep -n "defaultBranch(r\.Dest)" \
  /Users/talker/workspace/hive-deck-pro/hive-deck-pro/internal/branch/branch.go
```
Pass: no matches (or only inside an empty-branch fallback, not the primary path).

## TC-003 — Build clean
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro && go build ./... 2>&1
```
Pass: no output.
