# Task 0006 — Cleanup, build, ship

## Goal
Final dead-code sweep, full build, install, write results, ship.

## Steps

### 1. Dead code sweep
Search for any remaining dead code from removed fields:
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro
grep -rn "DefaultTargetBranch\|TargetBranches\|BranchFor\|branchFor" --include="*.go" .
```
Remove anything that remains.

### 2. Full build
```bash
go build ./...
```
Must pass with zero errors.

### 3. Install binary
```bash
make install
```

### 4. Write Results/RESULT.md and Retro/LESSONS.md
Write both files BEFORE calling hv_ship.

### 5. Ship
```
hv_ship  deck: "hive-deck-pro"
         message: "add per-deck branch field, remove global target branch config"
         title:   "Add per-deck branch field, remove global target branch config"
```

## Definition of done
`go build ./...` and `make install` pass. Results and Retro written. `hv_ship` succeeds and returns PR URLs.
