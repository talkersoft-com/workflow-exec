# Task 0001 — Config & resolve

## Goal
Remove `DefaultTargetBranch` and `TargetBranches` from the Go `Setup` struct and `config.yaml` schema. Add `Branch string` to `DeckFile`. Update `resolve.Build` to source every `RepoPlan.Branch` from `DeckFile.Branch`. Add `Branch string` to `Plan`. Delete `branchFor`, `BranchFor`, and all callers.

## Repo
`hive-deck-pro` at `/Users/talker/workspace/hive-deck-pro/hive-deck-pro`

## Steps

### 1. internal/config/config.go
- Remove `DefaultTargetBranch string` and `TargetBranches map[string]string` from `Setup` struct
- Add `Branch string \`yaml:"branch"\`` to `DeckFile` struct

### 2. internal/resolve/resolve.go
- Add `Branch string` field to `Plan` struct
- In `Build()`, set `plan.Branch = l.DeckFile.Branch` after constructing the plan
- In `collectRepoRefs` and `collectModuleRefs`, replace `branchFor(repoName, l.Setup)` with `l.DeckFile.Branch` (pass it in or read from `l`)
- Delete `branchFor()` and `BranchFor()` functions

### 3. Fix any compile errors from removed fields
- Search for any remaining references to `Setup.DefaultTargetBranch`, `Setup.TargetBranches`, `BranchFor`, `branchFor`
- Remove or update each one

### 4. go build
```bash
cd /Users/talker/workspace/hive-deck-pro/hive-deck-pro && go build ./...
```
Must pass with zero errors before marking done.

## Definition of done
`go build ./...` passes. `grep -r "DefaultTargetBranch\|TargetBranches\|BranchFor\|branchFor" --include="*.go"` returns nothing in the source tree.
